# CC0 1.0 Universal (Public Domain Dedication)

The author has dedicated this fixture (`main.c`, `Makefile`, `gen_font.py`, the
generated `font_data.h` / `testfont.ttf`, and the built `.elf`/`.self` artifacts)
to the public domain, waiving all rights worldwide under copyright law to the
extent allowed.

The font (`testfont.ttf`) is a synthetic creation generated by `gen_font.py`
(two plain rectangular glyphs) — it contains no third-party font data.

Full text: https://creativecommons.org/publicdomain/zero/1.0/legalcode

Keeps the behavior-freeze fixture corpus free of third-party-licensed code so
the built binaries can serve as canonical oracles.
