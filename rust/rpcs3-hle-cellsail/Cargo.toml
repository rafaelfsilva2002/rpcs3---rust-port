[package]
name = "rpcs3-hle-cellsail"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
description = "Rust port of rpcs3/Emu/Cell/Modules/cellSail.cpp — streaming AV interface library HLE (player FSM + descriptors + elementary streams)."

[lib]
name = "rpcs3_hle_cellsail"
crate-type = ["staticlib", "rlib"]

[dependencies]
rpcs3-emu-types = { path = "../rpcs3-emu-types" }

[lints]
workspace = true
