[package]
name = "rpcs3-memory"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
description = "Rust port of rpcs3/Emu/Memory/vm.h — Wave 4a: safe layout types, page flags, address-range constants. Raw pointer access + reservation lives in Wave 4b."

[lib]
name = "rpcs3_memory"
crate-type = ["staticlib", "rlib"]

[lints]
workspace = true
