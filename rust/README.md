# Rust workspace — Phase 0 scaffolding

Esta pasta é o ponto de entrada do porte gradual do RPCS3 para Rust, conforme descrito em [../behavior-freeze/docs/PORT_PLAN.md](../behavior-freeze/docs/PORT_PLAN.md).

**Regra:** cada commit mantém o binário `rpcs3` funcional. Esta pasta é compilada **apenas** quando `-DUSE_RUST_CRATES=ON` é passado ao CMake. Sem esse flag, o build é idêntico ao anterior.

## Layout

```
rust/
├── Cargo.toml                   # workspace
├── CMakeLists.txt               # integração CMake via corrosion (opt-in)
└── rpcs3-utilities/             # 1ª crate, folhas de Utilities/
    ├── Cargo.toml
    ├── cbindgen.toml            # gera header C para consumo do C++
    ├── include/
    │   └── rpcs3_utilities.h    # header C (gerado + commitado p/ IDE)
    └── src/
        └── lib.rs               # implementação + testes
```

## Testar só o lado Rust (não precisa CMake)

```bash
cd rust
cargo test --workspace
cargo clippy --workspace --all-targets -- -D warnings
```

## Integrar no build completo

```bash
cd ..
cmake -S . -B build -DBUILD_RPCS3_TESTS=ON -DUSE_RUST_CRATES=ON
cmake --build build --target rpcs3_utilities_rs
```

`rpcs3_utilities_rs` é o target CMake exportado por `corrosion_import_crate`. Ele ainda **não** está linkado em `rpcs3` — isso é deliberado; o primeiro patch que faz o binário de produção consumir código Rust é a próxima etapa da Fase 0.

## Estado de cada crate

| Crate | Status | Cobre | Testes |
|---|---|---|---:|
| `rpcs3-utilities` | phase-0 | `get_file_extension` (prova de toolchain, C ABI + parity table + 2 proptest fuzz contra oráculo C++) | 15 |
| `rpcs3-config` | wave-1 | `games.yml` parse/emit com round-trip byte-exato vs yaml-cpp | 21 |
| `rpcs3-crypto` | wave-1 | AES-128/192/256 ECB/CBC/CTR + SHA-1 + SHA-256 + MD5 + HMAC-SHA-1 + AES-CMAC (KAT FIPS-197, SP 800-38A F.2/F.5, FIPS-180, RFC 1321/2202/4493) | 30 |
| `rpcs3-loader-psf` | wave-2a | PARAM.SFO parse/emit, round-trip determinístico, validação de CATEGORY | 19 |
| `rpcs3-loader-elf-self` | wave-2b | ELF64 BE PPU + SCE/SELF header parsing, PT_LOAD address range validation | 14 |
| `rpcs3-loader-pkg` | wave-2c | PKG header parsing, entry type enumeration, content type detection | 16 |
| `rpcs3-loader-pup` | wave-2d | PUP firmware container parse + SHA-1 hash validation de cada entry | 10 |
| `rpcs3-loader-mself` | wave-2e | MSELF (multi-SELF) container parse + record enumeration | 9 |
| `rpcs3-emu-types` | wave-3 | Enums ABI congelados: `GameBootResult`, `SystemState`, `CpuFlag`, `CellError` (48 códigos) — compile-time assertions de ordinais | 11 |
| `rpcs3-lv2-syscall-table` | wave-3 | **1024 slots da tabela LV2 congelados** (extraídos automaticamente de `lv2.cpp:141-965` via `gen.py`). Cobertura 450 bound + 183 NullFunc + 117 Null + 274 Unused. Detecta 5 known-aliases do PS3 | 14 |
| `rpcs3-vfs-paths` | wave-3 | Canonicalização de paths PS3 (`lv2_fs_object::get_path_root_and_trail`) com `.`/`..` handling byte-a-byte idêntico ao C++ | 17 |
| `rpcs3-vfs-mount` | wave-3 | Mount table (`/dev_hdd0`, `/dev_bdvd`, `/dev_flash`, `/app_home`, `/dev_usb***`) + resolver PS3 path → host path com `$(EmulatorDir)` expansion | 13 |
| `rpcs3-memory` | wave-4a | PS3 memory layout types e constants congelados: PAGE_SIZE=4096, PAGE_COUNT=1048576, RESERVATION_BLOCK=128, PPU RAM [0, 0x30000000), overlay [0x30000000, 0x40000000), 8 page flags (readable/writable/executable/etc), 7 memory regions, block flags (4K/64K/1M page size + stack_guarded + preallocated) | 14 |
| `rpcs3-cpu-thread` | wave-4a | State machine `cpu_thread` sobre atomic bitset de `cpu_flag`: initial=stop+wait, set/clear/has atomic ops, `is_stopped`/`is_paused` matching `CPUThread.h:41-50`. Thread class from id high byte (PPU/SPU/RawSPU). | 15 |
| `rpcs3-memory-backing` | wave-4b | Page table (1M atomic page-flag bytes) + `SparseBackend` (HashMap de páginas 4KB) com `alloc_at`/`dealloc`/`page_protect`/`check_addr`/`read`/`write` typed LE + `ReservationTable` (512 rows × u64 timestamp com CAS `try_commit`). Safe APIs sem `unsafe` ainda — Wave 4c faz linear backing + JIT integration | 23 |
| `rpcs3-ppu-thread` | wave-4c | PPU register file: 32 × u64 GPR, 32 × f64 FPR, 32 × u128 VR, CR (pack/unpack via 32 bits), XER (SO/OV/CA/cnt), LR/CTR/CIA/VRSAVE, NJ mode, FPSCR. `id_base = 0x01000000`. Composes `CpuState` from `rpcs3-cpu-thread`. | 19 |
| `rpcs3-spu-thread` | wave-4c | SPU state: 128 × u128 GPR, 256 KB Local Store (`ls_read`/`ls_write` com wrap modulo), MFC command queue (16 slots + `mfc_enqueue`), spu_mfc_cmd (16 bytes), 35 MFC opcodes (Put/Get/Putllc/GetLlar/Barrier/Sync/...), MFC atomic status, tag update enum, reservation state (rtime/raddr/rdata). `id_base = 0x02000000`. | 22 |
| `rpcs3-ppu-opcodes` | wave-4c | PowerPC instruction format decoder: `PpuOpcode` com 40+ bitfield accessors (main/rd/ra/rb/simm16/ds/li/bt14/bt24/rc/oe/spr/etc), `ppu_decode` (rotate para lookup index 17-bit), `ppu_rotate_mask` (para rldicl/rldicr/rldic), 35+ primary opcode constants anchored against FIPS-equivalent bit positions of real PPC instructions (addi/add/sc/blr/b/lwz/stw/ld/vxor) | 25 |
| `rpcs3-ppu-interpreter` | wave-4d | **Execução real iter-9**: ALU + load/store + branches + sc + compare + shifts + SPR + unary/mul-high/divide/rotate-mask + FP double + FP single + **Altivec/VMX iter-2**. Integer D-form + XO-form. Branches completos. Shifts + SPR + Unary + Mul-high + Divide + Rotate-mask. FP primary 63 (fadd/fsub/fmul/fdiv A-form + fmr/fneg/fabs/fnabs X-form) e FP primary 59 (fadds/fsubs/fmuls/fdivs). FP loads/stores (lfs/lfd/stfs/stfd). **Altivec primary 4**: FP vaddfp/vsubfp/vmaddfp + **integer vadduwm (XO 128)/vsubuwm (XO 1152)/vand (1028)/vor (1156)/vxor (1220)/vnor (1284)** + **vperm byte permutation (VA-form 6-bit XO=43)** com sel & 0x1F. FPSCR.FPRF + VX/FX stickies. | 136 |
| `rpcs3-lv2-process` | wave-5 | Process-level syscalls: `sys_process_getpid`/`getppid`/`get_sdk_version`/`exit3`/`_sys_process_exit`/`_sys_process_exit2`/`get_number_of_object`. `ProcessState` trait plugável pelo emu core. `SyscallResult::Exit` sinaliza termination. 19 ObjectType constants frozen. | 14 |
| `rpcs3-lv2-ppu-thread` | wave-5 | PPU thread syscalls: `_sys_ppu_thread_exit`/`yield`/`join`/`detach`/`get_join_state`/`get_priority`/`set_priority`. `ThreadTable` trait + `JoinOutcome` (Terminated/NotYetTerminated) + `JoinState` (Joinable=0/Detached=1). Priority range 0..=3071 validated. Error codes: ESRCH/EINVAL/EDEADLK. | 16 |
| **`rpcs3-emu-core`** | **wave-6 MVP** | **Integration crate — o emulador vivo**. `EmuCore` wraps SparseBackend + PpuThread + ProcessState. `load_raw(base, bytes)` e `load_elf(elf_bytes)` (ELF64-BE PPU com PT_LOAD + flag mapping). `run()` loop step-by-step até `StepOutcome::Syscall`, dispatch via r11 para lv2-process/lv2-ppu-thread. Suporta syscalls 1/12/18/22/25/26/41/43. **ELF sintético ponta-a-ponta: parse ELF → map PT_LOAD → execute PPU → sc → dispatch → ExitStatus.** Step budget + UnsupportedSyscall error. **SPU integration**: `run_spu_group_single(image, source, attr)` deploy image em LS via lv2-spu-image + create/start/join via lv2-spu-group + execute via spu-interpreter até stop. Teste sintético SPU (il r3, 0xCAFE; stop 0x99) passa ponta-a-ponta. | 14 |
| `rpcs3-lv2-memory` | wave-5 | Memory allocation syscalls: `sys_memory_allocate/free/get_page_attribute/get_user_memory_size/container_create/container_destroy`. Page-size flags (4K/64K/1M), alignment validation, `MemoryContainer` trait plugável. PageAttr + MemoryInfo. Error codes: EALIGN/EINVAL/ENOMEM/ESRCH. | 17 |
| `rpcs3-lv2-sync` | wave-5 | Sync primitives syscalls: `sys_mutex_create/destroy/lock/unlock/trylock` + `sys_semaphore_create/destroy/post/wait/trywait/get_value`. `SyncTable` trait plugável + `BlockOutcome` (Acquired/MustBlock/Timeout). Protocol flags (FIFO/PRIORITY/PRIORITY_INHERIT), recursive mutex. Error codes: ESRCH/EBUSY/EPERM/EDEADLK/EAGAIN/EINVAL. Ref impl testa hand-off pra waiters, self-lock deadlock, sema overflow/underflow. | 24 |
| `rpcs3-hle-cellgame` | wave-6 | **Primeiro HLE module**: `cellGameBootCheck/ContentPermit/DataCheck/PatchCheck/GetParamInt/GetParamString/GetSizeKB`. `GameState` trait plugável. 29 `GameParamId` classificados string vs int. 5 `GameType` + 8 attribute flags frozen. Cell errors 0x8002CB__. | 20 |
| `rpcs3-lv2-fs` | wave-5 | Filesystem syscalls completos: `sys_fs_open/close/read/write/lseek/stat/mkdir/rmdir/unlink/opendir/readdir/closedir`. `FileSystem` trait + `FdTable` unificando files/dirs. Open flags (RDONLY/WRONLY/RDWR/CREAT/APPEND/TRUNC/EXCL), lseek whence (SET/CUR/END), mode bits (S_IFDIR/S_IFREG). Error codes: ENOENT/EACCES/EBADF/EISDIR/ENOTDIR/EEXIST/EINVAL. | 26 |
| `rpcs3-lv2-timer` | wave-5 | Timer + time syscalls: `sys_timer_usleep/create/destroy/start/stop/get_information` + `sys_time_get_current_time/system_time/timebase_frequency`. `TimeSource` + `TimerTable` traits plugáveis. `SleepOutcome` (Immediate/Sleep), `TimerState` (Stopped=0/Running=1), 79.8 MHz timebase frozen. Periodic ≥100us enforced. | 16 |
| `rpcs3-hle-cellsysutil` | wave-6 | System-utility HLE module: `cellSysutilCheckCallback/RegisterCallback/UnregisterCallback/GetSystemParamInt/GetSystemParamString/GetSystemMediaVer`. 8-slot `CallbackTable` + `CallbackQueue` com broadcast pra todas as slots. 11 callback events (RequestExitGame/DrawingBegin/etc) + 12 system params (Lang/Nickname/DateFormat/etc) classificados string vs int. Errors 0x8002B1__. | 21 |
| `rpcs3-lv2-event` | wave-5 | Event queue + event port IPC backbone: `sys_event_queue_create/destroy/receive/tryreceive/drain` + `sys_event_port_create/destroy/connect_local/disconnect/send`. `Event` 4-tuple (source/data1/data2/data3), `ReceiveOutcome` (Received/MustBlock), `EventRegistry` trait plugável. Queue types (PPU=1/SPU=2), protocols (FIFO=0x01/PRIORITY=0x02), port types (LOCAL=1/IPC=3). FIFO ring buffer queue, estado EISCONN/ENOTCONN em portas. | 17 |
| `rpcs3-lv2-cond` | wave-5 | Condition variables + integração com mutex: `sys_cond_create/destroy/wait/signal/signal_all/signal_to`. `CondRegistry` trait plugável, `WaitOutcome` (MustBlock/Woken/Timeout), `CondAttr` (pshared/flags/ipc_key/name). Valida ownership do mutex em wait (EPERM), destroy-com-waiters retorna EBUSY, FIFO waiter queue, `cond_resume_waiter` mostra re-lock do mutex pós-signal. `id_base = 0x86000000` matching C++. | 19 |
| `rpcs3-lv2-lwmutex` | wave-5 | Lightweight mutex com estado em user memory: `_sys_lwmutex_create/destroy/lock/trylock/unlock`. `LwMutexControl` (#[repr(C)] 32-byte BE, byte-exact vs `sys_lwmutex_t`) + `LwMutexTable` trait plugável. Sentinelas LWMUTEX_FREE/DEAD/RESERVED frozen, protocols FIFO/PRIORITY/PRIORITY_INHERIT/RETRY, recursive re-lock via rcount, EDEADLK em non-recursive re-lock, EPERM em unlock-por-outro-tid, EBUSY em destroy-com-waiters, ESRCH pós-mark_dead. `id_base = 0x95000000`. | 22 |
| `rpcs3-hle-cellsavedata` | wave-6 | HLE de save-data: `cellSaveDataAutoSave/AutoLoad/ListLoad/ListSave/ListAutoSave/ListAutoLoad/Delete`. `SaveDataState` trait plugável + `TestSaveData` in-memory reference (com quota_bytes check). `SaveDataDir` (nome 32B + title/subtitle), `FileOp` (READ/WRITE/DELETE/WRITE_NOTRUNC), `SaveDataResult` (bytes_written/read, files_touched, is_new). 12 error codes frozen (facility 0x8002B4__), 8 CBRESULT signed values, 4 FILEOPs + 6 FILETYPEs. Validação: empty filename/>63 chars/unknown op → PARAM, missing dir em load → NODATA, quota stuff → NOSPACE. | 23 |
| `rpcs3-lv2-rwlock` | wave-5 | Reader/writer lock com writer-priority: `sys_rwlock_create/destroy/rlock/tryrlock/runlock/wlock/trywlock/wunlock`. `RwlockRegistry` trait + `TestRwlockRegistry` reference (readers Vec + Option<writer>, FIFO read/write waiter queues). Invariantes: múltiplos readers concorrentes, writer-priority bloqueia novos readers quando writer queued, wlock re-entry = EDEADLK, runlock por não-holder = EPERM, destroy-com-holder/waiter = EBUSY. `drain_ready` entrega writer primeiro (se queued), senão todos readers de uma vez. `id_base = 0x88000000`. | 21 |
| `rpcs3-lv2-event-flag` | wave-5 | Event flags 64-bit bitmask: `sys_event_flag_create/destroy/wait/trywait/set/clear/get/cancel`. Mode bits frozen: WAIT_AND=0x01/WAIT_OR=0x02 (match) + WAIT_CLEAR=0x10/WAIT_CLEAR_ALL=0x20 (post-match side-effect). `EventFlagRegistry` trait + `TestEventFlagRegistry` com FIFO waiter queue. `set` faz OR + varre waiters em FIFO (CLEAR_* aplica per-waiter, então 1º waiter com CLEAR rouba bits do 2º → permanece parked), `clear` faz AND. WAITER_SINGLE=0x10000 rejeita 2º wait com EPERM. `cancel` acorda todos waiters de uma vez. `id_base = 0x98000000`. | 25 |
| `rpcs3-hle-cellpad` | wave-6 | Controller input HLE: `cellPadInit/End/GetInfo2/GetData/SetPortSetting/ClearBuf`. `PadBackend` trait plugável + `TestPadBackend` reference. `ButtonState` struct → `PadData` (64-entry u16 buffer com layout frozen: DIGITAL1/2 em bytes 2-3, analógicos 4-7, pressure 8-19). Constants frozen byte-exact vs pad_types.h: MAX_PORT_NUM=7/MAX_CODES=64/MAX_PADS=127, STATUS_CONNECTED=0x01, CTRL_* bitmasks (UP=0x10 / DOWN=0x40 / CROSS=0x40 / SQUARE=0x80). Errors 0x8012_11__. `ClearBuf` implementa latch one-shot (próximo read retorna len=0), init-twice = ALREADY_INITIALIZED, disconnect = NO_DEVICE, port out-of-range = INVALID_PARAMETER. | 20 |
| `rpcs3-hle-cellaudio` | wave-6 | Audio ports HLE: `cellAudioInit/Quit/PortOpen/PortClose/PortStart/PortStop/GetPortConfig/AddData`. `AudioSink` trait + `TestAudioSink` reference capturing all blocks. Port FSM (Close → Ready → Run), 8-port limit (PORT_FULL), ring buffer de `num_blocks` (8/16/32) x 256 samples x channels (2/8) f32. `add_data` valida tamanho exato e estado Run, passa bloco pro sink síncronamente. Status bits frozen (CLOSE=0x1010/READY=1/RUN=2), 15 error codes 0x8031_07__. PortConfig reporta port_size = 256 * ch * blocks * 4 bytes. | 24 |
| `rpcs3-lv2-spu-image` | wave-5 | SPU ELF image loader + `sys_spu_image_open/import/close/get_information/get_segments`. `SpuSegment` #[repr(C)] 24-byte (compile-time assert CHECK_SIZE 0x18) com 3 tipos frozen (COPY=1/FILL=2/INFO=4). `count_segments`/`fill_segments` replicam `sys_spu_image::get_nsegs/fill<WriteInfo=true>` byte-a-byte (PT_LOAD com bss vira 2 segs COPY+FILL, PT_NOTE vira INFO offset+0x14, unknown p_type → -1, capacity exceeded → -2). `deploy` escreve COPY/FILL em local store 256KB via callback fetch, EFAULT se fetch falha, EINVAL se segment fora dos limites. `id_base = 0x22000000`. | 21 |
| `rpcs3-hle-cellsync` | wave-6 | User-mode sync primitives em shared memory: `cellSyncMutexInitialize/Lock/TryLock/Unlock` (4-byte ticket lock BE rel+acq) + `cellSyncBarrierInitialize/Notify/TryNotify/Wait/TryWait` (4-byte value+count com bit 0x8000 como notified marker). `SyncMutex`/`SyncBarrier` `#[repr(C)]` com compile-time assert 4-byte. 17 error codes frozen (facility 0x8041_01__), 4 QUEUE_* directions. Mutex: blocking_lock retorna ticket FIFO, `mutex_poll_ready` para spin caller-side, BE layout validado via raw-slice (acq=1 → bytes `[00 00 00 01]`). Barrier: notify incrementa até count→0x8000 marker, wait decrementa até -0x8000→0 drain, reusável full cycle. | 19 |
| `rpcs3-spu-interpreter` | wave-4e | **SPU ISA interpreter iter-7**. Subset: imediatos, ALU 4-lane, ai + immediate logic, lqd/stqd, compares, multiply, shifts, FP fa/fs/fm, RRR (selb/shufb/fma/fnms/fms), sign-extend (xsbh/xshw/xswd), clz, cntb, convert cflts/cfltu/csflt/cuflt, **channel ops (rdch/wrch/rchcnt 11-bit primary 0x00D/0x10D/0x00F)** — ler/escrever canais SPU para mailbox, event, signals, decrementer. `StepOutcome::ChannelStall { channel, is_write }` sinaliza stall que o emu-core resolve parkando a thread. Channels via `SpuThread.channels: SpuChannels` (mailbox in/out/interrupt, event stat/mask, signal notify 1/2, decrementer, machine status). Control flow (br/bra + brnz/brz + stop/nop). Dispatch multi-stage (11-bit → 10-bit RI8 → 9-bit → 8-bit → 7-bit → 4-bit RRR). | 68 |
| `rpcs3-lv2-spu-group` | wave-5 | SPU thread group syscalls: `sys_spu_thread_group_create/destroy/start/suspend/resume/terminate/join/get_priority/set_priority`. `SpuGroupRegistry` trait + `TestSpuGroupRegistry` reference. FSM frozen: Initialized → Running → (Suspended → Running) → Stopped → Destroyed. 6 GROUP_TYPE_* constants byte-exact, 3 JOIN_* bitmask causes (GROUP_EXIT=1/ALL_THREADS_EXIT=2/TERMINATED=4). Validação: num_threads 1..=8, priority 16..=255, re-start/re-destroy retornam ESTAT/ESRCH/EBUSY corretos. `thread_exited` decrementa alive counter e transita p/ Stopped quando chega a zero com join_cause=ALL_THREADS_EXIT. `id_base=0x04000100` + step=0x100 matching C++. | 21 |
| `rpcs3-hle-cellspurs` | wave-6 | **SPURS framework** (SPU Runtime System — primeiro HLE "grande" sobre SPU thread groups, usado em massa por jogos AAA): `cellSpursInitialize/InitializeWithAttribute/AttributeInitialize/Finalize` + `AttributeSetNamePrefix/EnableSpuPrintfIfAvailable` + workloads `AddWorkload/RemoveWorkload/ShutdownWorkload/WaitForWorkloadShutdown/GetWorkloadInfo`. `SpursRegistry` trait + `TestSpursRegistry` reference. FSM de workload: Runnable → Shutdown → Removable. Invariantes: num_spus 1..=8, spu_priority 1..=16, ppu_priority 16..=255, name_prefix ≤15 chars, max_contention ≤ num_spus, MAX_WORKLOAD=16 (POLICY_AGAIN no 17º), finalize-com-workloads = CORE_BUSY. **8+ error codes byte-exact** (CORE_*/POLICY_*/TASK_* com facilidades 0x8041_07__ / 08__ / 09__). | 20 |
| `rpcs3-lv2-raw-spu` | wave-5 | Standalone SPU syscalls (bypass SPURS): `sys_raw_spu_create/destroy/create_interrupt_tag/set_int_mask/get_int_mask/set_int_stat/get_int_stat/set_spu_cfg/get_spu_cfg`. 5-slot allocator (MAX_RAW_SPU matching `g_raw_spu_id[5]`), `RAW_SPU_BASE_ADDR=0xE000_0000` + `RAW_SPU_OFFSET=0x10_0000` (1MB stride), 3 interrupt classes per SPU cada uma com mask/stat de 64 bits. `set_int_stat` implementa write-1-to-clear semântica de hardware. 9º create → EAGAIN, double tag no mesmo class → EAGAIN, class_id ≥ 3 → EINVAL. | 13 |
| `rpcs3-hle-cellgcm` | wave-7 | **Primeiro HLE gráfico**: GCM shadow-state: `cellGcmInitBody/GetConfiguration/SetDisplayBuffer/AddressToOffset/IoOffsetToAddress/SetTileInfo/BindTile/UnbindTile/SetZcull/SetFlipMode/GetCurrentField/GetReport/SetReport`. Estado shadow: 8 display buffers, 15 tile slots, 8 zcull slots, IO table (1MB pages) EA↔offset, flip mode HSYNC/VSYNC, current_field top/bottom toggle, report counters. DEFAULT_LOCAL_ADDR=0xC0000000 (VRAM), DEFAULT_IO_ADDR=0x0F900000 (main memory window). 6 error codes byte-exact (facility 0x8021_00__). Valida alinhamento (io_addr/io_size por 1MB, pitch por 64), LOCATION_LOCAL/MAIN enum, flip mode, id ranges. | 25 |
| `rpcs3-hle-cellsysmodule` | wave-7 | Dynamic HLE module loader: `cellSysmoduleInitialize/Finalize/LoadModule/UnloadModule/IsLoaded/SetMemcontainer`. 45+ module IDs byte-exact vs cellSysmodule.cpp:39+ (sys_net=0x0000, cellSpurs=0x000A, cellSync=0x000D, cellGcmSys=0x0010, cellAudio=0x0011, cellSysutil=0x0017, etc.). 5 error codes (DUPLICATED/UNKNOWN/UNLOADED/INVALID_MEMCONTAINER/FATAL, facility 0x8001_20__). Ref counting: load-twice = DUPLICATED, unload-sem-load = UNLOADED, id não reconhecido = UNKNOWN, finalize limpa tudo. | 16 |
| `rpcs3-hle-cellrtc` | wave-7 | Real-Time Clock HLE: `cellRtcGetCurrentTick/SetTick/GetTick/CheckValid/TickAdd{Ticks,Microseconds,Seconds,Minutes,Hours,Days}/CompareTick`. `RtcTick` (µs desde ano 1 AD) + `RtcDateTime` (year/month/day/h/m/s/µs). Conversão bidirecional com walk de dias incluindo leap year (1900 não/2000 sim), feb 29 só em leap. `WallClock` trait plugável + `FixedClock` reference para testes determinísticos. 14 error codes byte-exact (facility 0x8001_06__): NOT_INITIALIZED, INVALID_YEAR/MONTH/DAY/HOUR/MINUTE/SECOND/MICROSECOND, etc. | 21 |
| `rpcs3-hle-cellnetctl` | wave-7 | Network connection status HLE: `cellNetCtlInit/Term/GetState/AddHandler/DelHandler/GetInfo/NetStartDialogLoadAsync`. `NetCtlBackend` trait + `OfflineBackend`/`StubConnectedBackend` reference. 17 error codes (facility 0x8013_01__), 4 STATE_* (Disconnected=0/Connecting=1/IPObtaining=2/IPObtained=3), 6 EVENT_* codes, 16+ INFO_* codes. `get_info` retorna IP/netmask/default route/primary+secondary DNS/ether_addr/MTU/link status. NOT_CONNECTED quando backend offline, INVALID_CODE para info code desconhecido, HANDLER_MAX=4 concurrent. | 19 |
| `rpcs3-hle-sys-prx-for-user` | wave-7 | User-mode runtime helper library: `sys_get_random_number/process_is_stack/process_atexitspawn/process_at_Exitspawn/process_get_paramsfo/sys_get_console_id/sys_get_bd_media_id` + `console_getc/putc/write`. 4 plug-in traits (`RandomSource`, `ConsoleIO`, `ParamSfoSource`, `HardwareIds`) + references (`SeededRandom` xorshift64*, `TestConsole`, `StubHardwareIds`). `sys_process_is_stack` reconhece janela [0xD0000000, 0xE0000000) como stack. Random fills com limite de 4KB por call, console_write limitado a 64KB. | 15 |
| `rpcs3-hle-cellmsgdialog` | wave-7 | System modal dialog HLE: `cellMsgDialogOpen2/OpenErrorCode/Close/Abort/ProgressBarSetMsg/Reset/Inc`. `TypeFlags` struct decodifica flag word packed (SE_TYPE/MUTE/BG/BUTTON_TYPE/DISABLE_CANCEL/DEFAULT_CURSOR/PROGRESSBAR em bit-fields). FSM: Closed → Open → Closed. Button results: YES=1/OK=1/NO=2/ESCAPE=3/NONE=-1. Progress bars single/double com saturate-at-100. Validação: mix PROGRESSBAR_*+BUTTON_TYPE_YESNO = PARAM, reserved bits set = PARAM, abort com DISABLE_CANCEL_ON = PARAM, msg ≤256 chars, progress msg ≤64. | 22 |
| `rpcs3-hle-cellvideoout` | wave-7 | Display mode negotiation HLE (chamado por todo jogo após cellGcmInit): `cellVideoOutGetState/GetResolution/Configure/GetConfiguration/GetDeviceInfo/GetNumberOfDevice/GetResolutionAvailability`. 9 error codes byte-exact facility 0x8002_B22_. 9 RESOLUTION_* ids (1080=1/720=2/480=4/576=5 + 4 extended 16:9 modes 0x0A..0x0D), 3 aspectos (AUTO/4:3/16:9), 3 color formats (X8R8G8B8/X8B8G8R8/FP16). Validação rigorosa do pitch: ≥ width × bpp, múltiplo de 64. Primary port sempre 1 device, secondary sempre 0. VideoOutState {Enabled, Disabled, DeepSleep}. | 23 |
| `rpcs3-hle-cellfs` | wave-7 | Game-facing filesystem HLE wrapper sobre rpcs3-lv2-fs: `cellFsOpen/Close/Read/Write/Lseek/Stat/Mkdir/Rmdir/Unlink/Opendir/Readdir/Closedir` + stubs (Rename/Truncate/Chmod → ENOSYS). `translate_open_flags` converte flags octal cellFs (0o100=CREAT, 0o200=EXCL, 0o1000=TRUNC, 0o2000=APPEND) para bitfield lv2. `validate_path` enforcei path absoluto, não vazio, ≤1024 chars. Constants S_IFREG/S_IFDIR octal matching sys_fs.h:43-47. | 22 |
| `rpcs3-hle-cellsyscache` | wave-7 | HDD1 game-data cache HLE: `cellSysCacheMount/Clear`. `SysCacheManager` track current_id + title_prefix + retain_caches. Mount com mesmo id → RET_OK_RELAYED, mount com id diferente (sem retain) → RET_OK_CLEARED. `validate_cache_id` enforcei 1..=31 chars, printable ASCII, sem path separators (/ \ : * ? < > |). Path construction: `/dev_hdd1/caches/{cache_id}` ou `/dev_hdd1/caches/{title_prefix}_{cache_id}` quando prefix presente. 4 error codes frozen facility 0x8002_BC__. | 19 |
| `rpcs3-hle-cellusbd` | wave-7 | USB device driver subsystem HLE: `cellUsbdInit/End/RegisterLdd/UnregisterLdd/OpenPipe/ClosePipe/GetDeviceSpeed/GetDeviceDescriptor`. `UsbBackend` trait plugável + `FixedUsbBackend` reference para testes. `LddInfo` (vendor/product IDs + name), `Pipe` (device+endpoint+transfer type), `DeviceSpeed` enum (Low=1/Full=2/High=3) matching USB 1.1/2.0 speeds. 18 error codes frozen facility 0x8011_00__ + TCC constants (HC/EHCI completion codes). Invariantes: end-com-pipes-open=PIPE_NOT_RELEASED, end-com-ldds=LDD_NOT_RELEASED, duplicate VID/PID=LDD_ALREADY_REGISTERED, endpoint >0x0F=INVALID_PARAM. Full lifecycle teste valida init→register→open→close→unregister→end. | 23 |
| `rpcs3-hle-cellssl` | wave-7 | TLS/SSL cert management HLE: `cellSslInit/End/CertLoadFromBitmask/CertGetSerialNumber/GetIssuerName/GetSubjectName/GetPublicKey/GetNotBefore/GetNotAfter/GetMd5Fingerprint`. `Certificate` struct (serial + issuer + subject + public key + validity range + MD5 fingerprint) + `install_certificate` helper para emu-core plugar certs reais. `cert_load_from_bitmask` aceita OR de CERT_* constants (17 root CAs byte-exact vs cellSsl.h:30-80), rejeita bits não em CERT_KNOWN_MASK com UNKNOWN_LOAD_CERT. 15 error codes frozen facility 0x8074_00__ + time type constants. | 17 |
| `rpcs3-hle-cellcamera` | wave-7 | PlayStation Eye / EyeToy camera HLE: `cellCameraInit/End/Open/Close/Start/Stop/GetAttribute/SetAttribute/Read/GetType/IsAttached`. `CameraBackend` trait + `NullCameraBackend`/`TestCameraBackend` references. FSM: Closed → Open → Running → Open → Closed. Invariantes: init twice=ALREADY_INIT, open twice=ALREADY_OPEN, open com backend detached=DEVICE_NOT_FOUND, unknown format/resolution=FORMAT_UNKNOWN/RESOLUTION_UNKNOWN, framerate 0 ou >60=BAD_FRAMERATE, read sem start=NOT_STARTED, backend deactivated em read=DEVICE_DEACTIVATED. 15 error codes frozen facility 0x8014_08__ + 4 types (EYETOY=1/EYETOY2=2/USBVIDEOCLASS=3), 7 formats (JPG/RAW8/YUV422/RAW10/RGBA/YUV420/V_Y1_U_Y0), 4 resolutions (VGA=1/QVGA=2/WGA=3/SPECIFIED_WH=4), 28+ attributes (GAIN/EXPOSURE/BRIGHTNESS/AEC/AGC/AWB/etc). | 26 |
| `rpcs3-hle-cellfont` | wave-7 | Text rendering HLE — 3 object layers (Library/Font/Renderer): `cellFontInit/End/OpenFontset/OpenFontFile/CloseFont/CreateRenderer/DestroyRenderer/BindRenderer/UnbindRenderer/RenderCharGlyphImage/GetHorizontalLayout/SetScalePixel`. `FontBackend` trait + `StubFontBackend` reference (gera square glyphs determinísticos). 11 TYPE_* system fonts (RODIN_SANS_SERIF_LATIN/LIGHT/BOLD, NEWRODIN_GOTHIC_JAPANESE/LIGHT/BOLD, YD_GOTHIC_KOREAN, MATISSE_SERIF_LATIN, SEURAT_MARU_GOTHIC, VAGR_SANS_SERIF_ROUND). 25 error codes byte-exact facility 0x8054_00__ + MAX_FONTS=16. Invariantes: unknown fontset=NO_SUPPORT_FONTSET, 17º open=FONT_OPEN_MAX, bind-duplo=RENDERER_ALREADY_BIND, destroy de bound renderer=RENDERER_ALREADY_BIND, render sem bind=RENDERER_UNBIND. | 24 |
| `rpcs3-hle-cellatrac` | wave-7 | ATRAC3/ATRAC3+ audio decoder HLE: `cellAtracSetData/Decode/GetStreamDataInfo/AddStreamData/GetRemainFrame/GetSoundInfo/GetMaxSample/SetLoopNum/GetLoopInfo/ResetPlayPosition/GetBitrate` + decoder lifecycle (create/destroy). `AtracDecoder` trait + `StubAtracDecoder` reference. FSM: Idle → Ready → Exhausted (resumível via AddStreamData). 21 error codes byte-exact facility 0x8061_03__ + 3 remain sentinels (ALLDATA=-1/NONLOOP=-2/LOOP=-3) + HANDLE_SIZE=512. Invariantes: decoder twice=DECODER_WAS_CREATED, sem decoder=NO_DECODER, sem data=UNSET_DATA, decode após exaustão=ALLDATA_WAS_DECODED, min_write=0=READSIZE_IS_TOO_SMALL, min_write>data=READSIZE_OVER_BUFFER, loop_num<-1=UNSET_LOOP_NUM, sample>total=ILLEGAL_SAMPLE. | 25 |
| `rpcs3-hle-cellvpost` | wave-7 | Video post-processing HLE (YUV→RGBA scaling + deinterlace): `cellVpostQueryAttr/Open/OpenEx/Close/Exec`. `VideoPostBackend` trait + `StubVideoPostBackend` reference (byte copy). `CfgParam` (in/out max dims + depth + fmt), `CtrlParam` (exec/scaler/ipc types + windows), `PictureInfo` (processed_lines output). 22 error codes byte-exact facility 0x8061_04__ (4 facilities Query/Open/Close/Exec). 8 EXEC_TYPE_* (PFRM_PFRM/PTOP_ITOP/…), 4 SCALER_TYPE_* (BILINEAR/LINEAR_SHARP/2X4TAP/8X4TAP), 3 IPC_TYPE_* (DOUBLING/LINEAR/MAVG), 2 COLOR_MATRIX_* (BT601/BT709), 2 PIC_FMT_OUT_* (RGBA_ILV/YUV420_PLANAR). Validação de window dentro de in/out dims, exec/scaler/ipc enum check. | 20 |
| `rpcs3-hle-cellkb` | wave-7 | USB keyboard input HLE: `cellKbInit/End/GetInfo/Read/SetReadMode/GetConfiguration/ClearBuf/SetLEDStatus`. `KbBackend` trait + `NullKbBackend`/`TestKbBackend` references (queued scripted frames para testes). `KbData` com modifier + LED + keycode array (up to 62), auto-truncate. MAX_KEYBOARDS=127/MAX_KEYCODES=62/RMODE_INPUTCHAR=0/RMODE_PACKET=1/MAPPING_101/106/106_KANA. 5 LED flags (NUMLOCK=0x01/CAPSLOCK=0x02/SCROLLLOCK=0x04/COMPOSE=0x08/KANA=0x10). ClearBuf é latch one-shot. 8 error codes facility 0x8012_10__. Per-port config + led state isolation. | 23 |
| `rpcs3-hle-cellmouse` | wave-7 | USB mouse input HLE: `cellMouseInit/End/GetInfo/GetData/GetDataList/GetRawData/ClearBuf/SetTabletRotation`. `MouseBackend` trait + `NullMouseBackend`/`TestMouseBackend` references. `MouseData` com delta X/Y + buttons bitmap + wheel + tilt. `GetDataList` drena até 8 snapshots de uma vez. 5 button flags (LEFT/RIGHT/MIDDLE/BTN_4/BTN_5). 4 TABLET_ROTATION_* (0/90/180/270) com rotação matemática aplicada às deltas (90: x=-y y=x, 180: nega ambos, 270: x=y y=-x). MAX_MICE=127, MAX_DATA_LIST_NUM=8, MAX_CODES=64. 8 error codes facility 0x8012_12__. | 24 |
| `rpcs3-hle-cellkey2char` | wave-7 | USB HID scan code → Unicode char translator: `cellKey2CharOpen/Close/GetChar/SetMode/SetArrangement`. `translate(key, arrangement)` mapeia HID codes A-Z (0x04-0x1D), 1-9 e 0 (0x1E-0x27) com shift (!@#$%^&*()), punctuation (`-=[]\;'` etc) shifted e unshifted, control (`Enter/Tab/Space/Backspace`). 3 MODE_* (ENGLISH/NATIVE/NATIVE2), 3 ARRANGEMENT_* (101/106/106_KANA), 8 MKEY_* modifier bits. 6 error codes facility 0x8012_13__. End-to-end teste "Hello" via scan codes. | 29 |
| `rpcs3-hle-celladec` | wave-7 | Generic audio decoder framework (multiplexes MP3/AAC/AC3/DTS/ATRAC/LPCM/CELP/TrueHD): `cellAdecQueryAttr/Open/Close/StartSeq/EndSeq/DecodeAu/GetPcm/GetPcmItem`. `AdecDecoder` trait + `StubAdecDecoder` que emite frames silenciosos (1024 samples × channels por AU). 12 CODEC_* constants byte-exact vs cellAdec.h:193+ (LPCM_PAMF=1, AC3=2, ATRACX=3, MP3=4, ATRAC3=5, MPEG_L2=6, M2AAC=7, EAC3=8, TRUEHD=9, DTS=10, CELP=11, LPCM_BLURAY=12). FSM: Closed→Open→InSequence. Validação: codec válido, canais 1..=8, sample rate >0, AU size match bytes len. 10 error codes (generic 0x8061_000x + codec-specific M4AAC/CELP). `PcmFrame` queue por handle, `GetPcmItem` reporta remaining count. | 25 |
| `rpcs3-hle-cellvdec` | wave-7 | Generic video decoder framework (multiplexes MPEG2/AVC-H264/MPEG4/VC1/DIVX/JVT/MVC): `cellVdecQueryAttr/Open/Close/StartSeq/EndSeq/DecodeAu/GetPicture/GetPicItem/SetFrameRate`. `VdecDecoder` trait + `StubVdecDecoder` que emite YUV420 frames verdes (width × height luma + 2 × (width/2) × (height/2) chroma). 9 CODEC_* constants byte-exact vs cellVdec.h:15-26 (MPEG2=0, AVC=1, MPEG4=2, VC1=3, DIVX=5, JVT=7, DIVX3_11=9, MVC=11, MVC2=13). 8 FRAME_RATE_* (23976/24/25/2997/30/50/5994/60). FSM: Closed→Open→InSequence idêntico ao cellAdec. `Picture` struct traz YUV bytes + pts/dts/user_data. Output flui para cellVpost (YUV→RGBA). 7 error codes (ARG/SEQ/BUSY/EMPTY/AU/PIC/FATAL facility 0x8061_01__). | 26 |
| `rpcs3-hle-cellavconf` | wave-7 | Audio-out + audio-in AV configuration HLE (porta `cellAudioOut.cpp` + `cellAvconfExt.cpp`): `cellAudioOutGetNumberOfDevice/GetState/GetDeviceInfo/GetSoundAvailability/GetSoundAvailability2/Configure/GetConfiguration/SetCopyControl` + `cellAudioInGetNumberOfDevice/GetDeviceInfo`. `AvconfManager` com 2-port `audio_out_configuration` shadow (Primary HDMI com sound_modes array, Secondary peripheral) + Vec<AudioInDeviceInfo> para mics/BT. 16 out error codes 0x8002_b24_ + 8 in error codes 0x8002_b26_ byte-exact. 13 CODING_* (LPCM/AC3/MP3/AAC/DTS/ATRAC/DOLBY_TRUE_HD/DD_PLUS/DTS_HD_HIGHRES/DTS_HD_MASTER/BITSTREAM=0xFF), 4 CHNUM_* (2/4/6/8), 7 FS_* bitmasks (32/44/48/88/96/176/192 kHz), 6 PORT_* (HDMI/SPDIF/ANALOG/USB/BT/NETWORK), 4 SPEAKER_LAYOUT_* (default/2ch/6ch/8ch), 3 COPY_CONTROL_* (FREE/ONCE/NEVER), 3 DOWNMIXER_* (NONE/TYPE_A/TYPE_B). Invariantes: primary→1 device, secondary→0 devices (out-of-range retorna pseudo-state 0x10/layout 0xD00C1680 matching observed HW), configure ignora downMixer>TYPE_B silenciosamente (mantém anterior), configure-em-secondary=UNSUPPORTED_AUDIO_OUT, set_copy_control>NEVER=ILLEGAL_PARAMETER. | 32 |
| `rpcs3-hle-cellsail` | wave-7 | Streaming AV Interface Library (SAIL) HLE: framework high-level que orquestra dmux+adec+vdec+vpost+output adapters. `Player` FSM de 10 estados (Initialized=0/BootTransition=1/Closed=2/OpenTransition=3/Opened=4/StartTransition=5/Running=6/StopTransition=7/CloseTransition=8/Lost=9) matching cellSail.h:47-60. Cobertura: `cellSailPlayerInitialize/Finalize/Boot/CreateDescriptor/AddDescriptor/RemoveDescriptor/GetDescriptorCount/OpenStream/CloseStream/Start/Stop/Next/SetPaused/Cancel/SetParameter/GetParameter/SetPreset` + ES lifecycle (OpenEsAudio/Video/User + CloseEs + SetEsMuted) + events (Subscribe/Unsubscribe). 12 error codes byte-exact facility 0x8061_07__ (INVALID_ARG=01, INVALID_STATE=02, UNSUPPORTED_STREAM=03, INDEX_OUT_OF_RANGE=04, EMPTY=05, FULLED=06, USING=07, NOT_AVAILABLE=08, CANCEL=09, MEMORY=F0, INVALID_FD=F1, FATAL=FF). 4 STREAM_* (PAMF=0/MP4=1/AVI=2/UNSPECIFIED=-1), 7 PRESET_* (AV_SYNC/AS_IS/59_94Hz/29_97Hz/50Hz/25Hz/AUTO_DETECT), 16 CALL_* types, 12 EVENT_* types, 3 MEDIA_STATE_* (FINE/BAD/LOST). `Descriptor` (stream_type+uri+auto_selection+open+media_info) + `ElementaryStream` (Audio/Video/User kind). Invariantes: boot sem state=Initialized → INVALID_STATE, open_stream sem state=Closed → INVALID_STATE, remove current descriptor → USING, open_es fora de Opened/Running → INVALID_STATE, duplicate ES → USING, mark_media_state=LOST → FSM transita para Lost. Pipeline test end-to-end (boot → create_descriptor → open_stream → open_es → start → pause → next → stop → close_stream). | 42 |
| `rpcs3-hle-cellsearch` | wave-7 | XMB media search HLE (porta `cellSearch.cpp`): `cellSearchInitialize/Finalize/StartContentSearch/Cancel/End/NotificationOpen/NotificationClose/GetContentInfoByOffset/GetContentInfoByContentId/GetContentInfoGameComment`. `SearchManager` FSM 4 estados (Uninitialized/Initializing/Ready/Finalizing) + single-active-session enforcement. `ContentInfo` (id+content_type+status+path+title+tags+game_comment+duration+size+is_drm) + `ContentId` 16-byte + `SearchSession` (id+type+sort_key+sort_order+results). 19 error codes byte-exact facility 0x8002_C8__ (PARAM=01/BUSY=02/NO_MEMORY=03/UNKNOWN_MODE=04/ALREADY_INITIALIZED=05/NOT_INITIALIZED=06/FINALIZING=07/NOT_SUPPORTED_SEARCH=08/CONTENT_NOT_FOUND=0A/OUT_OF_RANGE=0C/INVALID_SEARCHID=0D/DRM=11/TAG=12/GENERIC=FF) + CANCELED=1 sentinel. 8 CONTENTTYPE_* (MUSIC/PHOTO/VIDEO + list variants + SCENE), 4 CONTENTSEARCHTYPE_*, 11 SORTKEY_* (TITLE/ARTIST/IMPORTEDDATE/etc), 3 SORTORDER_*, 8 EVENT_* types, 4 CONTENTSTATUS_* (NONE/AVAILABLE/NOT_SUPPORTED/BROKEN), 10 CONSTANT sizes (CONTENT_ID_SIZE=16/TITLE_LEN_MAX=384/TAG_NUM_MAX=6/GAMECOMMENT_SIZE_MAX=1024/etc). Sort by title/date asc/desc implementado. Invariantes: init-duplo=ALREADY_INITIALIZED, start-sem-init=NOT_INITIALIZED, start-com-active=BUSY, unknown search type=NOT_SUPPORTED_SEARCH, cancel wrong id=INVALID_SEARCHID, offset fora dos results=OUT_OF_RANGE, game_comment vazio=TAG, finalize-com-active=BUSY. | 34 |
| `rpcs3-hle-cellsubdisplay` | wave-7 | Second-display / PSP Remote Play HLE (porta `cellSubDisplay.cpp`): `cellSubDisplayInit/End/Start/Stop/GetRequiredMemory/GetPeerNum/GetPeerList/AudioOut/AudioOutBlocking/SetVideoMemory`. `SubDisplay` FSM 3 estados (Closed → Initialized → Running) + peer_list single-slot (MAX_PEERS=1 matching Remote Play limit). 9 error codes byte-exact facility 0x8002_98__ (OUT_OF_MEMORY=51/FATAL=52/NOT_FOUND=53/INVALID_VALUE=54/NOT_INITIALIZED=55/NOT_SUPPORTED=56/SET_SAMPLE=60/AUDIOOUT_IS_BUSY=61/ZERO_REGISTERED=13). 3 STATUS_* (JOIN=1/LEAVE=2/FATALERROR=3), 3 VERSION_* (0001=1/0002=2/0003=3), MODE_REMOTEPLAY=1, 3 VIDEO_FORMAT_* (A8R8G8B8/R8G8B8A8/YUV420), 2 ASPECT_RATIO_*, 2 VIDEO/AUDIO_MODE_* (SETDATA/CAPTURE), 3 MEMORY_CONTAINER_SIZE_* (8MB/10MB/10MB per version), V0003 geometry (WIDTH=864/PITCH=864/HEIGHT=480), 5 TOUCH_STATUS_*, NICKNAME_LEN=256/PSPID_LEN=16. `SubDisplayParam` validation: mode=REMOTEPLAY only (outros=NOT_SUPPORTED), pitch >= width, ch 1..=8, n_peer 1 (outros=INVALID_VALUE), n_group ≥1. Invariantes: init-duplo=FATAL, start-sem-init=NOT_INITIALIZED, peer_join fora de Running=NOT_INITIALIZED, 2º peer=OUT_OF_MEMORY, peer_leave id desconhecido=NOT_FOUND, audio_out vazio=SET_SAMPLE, audio_out não múltiplo de ch=SET_SAMPLE, audio_out com flag busy=AUDIOOUT_IS_BUSY, set_video_memory addr=0=INVALID_VALUE. | 39 |
| `rpcs3-hle-cellgem` | wave-7 | PlayStation Move tracking HLE (porta `cellGem.cpp`): `cellGemInit/End/GetInfo/GetMemorySize/PrepareCamera/PrepareVideoConvert/UpdateStart/UpdateFinish/GetState/GetInertialState/GetImageState/TrackHues/SetRumble/EnableMagnetometer/InvalidateCalibration/GetStatusFlags/ClearStatusFlags`. `GemManager` + 4-slot `GemController` array (MAX_NUM=4 matching hardware). 11 error codes byte-exact facility 0x8012_18__ (RESOURCE_ALLOCATION_FAILED=01/ALREADY_INITIALIZED=02/UNINITIALIZED=03/INVALID_PARAMETER=04/INVALID_ALIGNMENT=05/UPDATE_NOT_FINISHED=06/UPDATE_NOT_STARTED=07/CONVERT_NOT_FINISHED=08/CONVERT_NOT_STARTED=09/WRITE_NOT_FINISHED=0A/NOT_A_HUE=0B). 9 CellGemStatus (NOT_CONNECTED=1/SPHERE_NOT_CALIBRATED=2/CALIBRATING=3/COMPUTING_COLORS=4/HUE_NOT_SET=5/NO_VIDEO=6/TIME_OUT_OF_RANGE=7/NOT_CALIBRATED=8/NO_EXT_DEV=9), 8 CTRL_* bits (SELECT/T/MOVE/START/TRIANGLE/CIRCLE/CROSS/SQUARE), 3 EXT_* (CONNECTED/EXT0/EXT1), 9 VIDEO format codes (NO_OUTPUT=1..BAYER_RASTERIZED=9), 4 video conversion flags (AWB/GAMMA/COMBINE_PREV/FILTER_OUTLIER), 2 external device IDs (SHARP_SHOOTER=0x8081/RACING_WHEEL=0x8101), 13 status flags (CALIBRATION_OCCURRED/SUCCEEDED/FAILED_*/LIGHTING_CHANGED/etc), 3 hue sentinels (DONT_TRACK=0x02_00_0000/DONT_CARE=0x04_00_0000/DONT_CHANGE=0x08_00_0000), SPHERE_RADIUS_MM=22.5, LATENCY_OFFSET=-22000, VERSION=2. Invariantes: init wrong version=INVALID_PARAMETER, init_twice=ALREADY_INITIALIZED, prepare_camera exposure fora [40,511]=INVALID_PARAMETER, prepare_video_convert unknown format=INVALID_PARAMETER, buffer misaligned mod 16=INVALID_ALIGNMENT, prepare_video_convert duplo=CONVERT_NOT_FINISHED, update_start sem prepare_camera=UPDATE_NOT_STARTED, update_start duplo=UPDATE_NOT_FINISHED, track_hues len>max_connect=INVALID_PARAMETER, track_hues rgb>0xFFFFFF (não sentinel)=NOT_A_HUE, DONT_CHANGE_HUE preserva valor anterior, get_state de controller desconectado=INVALID_PARAMETER, status_flags aplica mask. | 42 |
| `rpcs3-hle-celloskdialog` | wave-7 | Onscreen-Keyboard / IME dialog HLE (porta `cellOskDialog.cpp`): `cellOskDialogLoadAsync/UnloadAsync/Abort/GetSize/GetInputText/SetLayoutMode/SetKeyLayoutOption/SetInitialInputDevice/DisableDimmer/SetScale/SetContinuousMode`. `OskDialog` FSM 5 estados (Idle → Loaded → Finished/Aborted → Unloaded). 4 error codes byte-exact facility 0x8002_b50_ (IME_ALREADY_IN_USE=01/GET_SIZE_ERROR=02/UNKNOWN=03/PARAM=04). 7 callback status codes (LOADED=0x0502 .. DISPLAY_CHANGED=0x0508), 4 INPUT_FIELD_RESULT_* (OK=0/CANCELED=1/ABORT=2/NO_INPUT_TEXT=3), 9 dialog types (SINGLELINE=0..SEPARATE_CANDIDATE_WINDOW=8), 3 INITIAL_PANEL_LAYOUT_* (SYSTEM/10KEY/FULLKEY), 2 INPUT_DEVICE_* (PAD/KEYBOARD), 4 CONTINUOUS_MODE_*, 2 DISPLAY_STATUS_*, 4 FinishReason (CONFIRM=0/CANCEL=1/ABORT=-1/TERMINATE=-2), 30 panelmode language bits (DEFAULT/ENGLISH=0x02/JAPANESE=0x0100/ALPHABET=0x01000000/PASSWORD=0x20000000/etc), 6 LAYOUTMODE_* X/Y align bits, 4 PROHIBIT_* flags (NO_SPACE/NO_RETURN/NO_INPUT_ANALOG/NO_STARTUP_EFFECT), 2 PANEL_* bits (10KEY/FULLKEY), SCALE_MIN=0.80/SCALE_MAX=1.05, STRING_SIZE=512 theoretical max. `InputFieldInfo` (message+init_text+limit_length) + `DialogParam` (allow_panel_flag+prohibit_flags+first_view_panel+initial_key_layout+initial_input_device+dialog_type) + `InputOutcome` (result+text). Invariantes: load_async duplo=IME_ALREADY_IN_USE, bad panel/layout/device/type/limit=PARAM, init_text>limit=PARAM, finish sem loaded=UNKNOWN, finish com bad result=PARAM, finish com text>limit=PARAM, unload_async em Loaded=UNKNOWN, unload após unload=UNKNOWN, abort de Idle=UNKNOWN, get_size bad type=GET_SIZE_ERROR, set_layout_mode sem exactly-one X e Y=PARAM, set_scale fora [0.80,1.05] ou NaN=PARAM, set_continuous_mode unknown=PARAM, get_input_text antes de finish=UNKNOWN. full_lifecycle_smoke (settings → load → finish → unload → re-load) passa. | 43 |
| `rpcs3-hle-cellmusic` | wave-7 | Background music playback HLE (porta `cellMusic.cpp`): `cellMusicInitialize/Finalize/SelectContents/SetSelectionContext/GetSelectionContext/SetPlaybackCommand/GetPlaybackStatus/SetVolume`. `MusicPlayer` singleton FSM 3 estados (Uninitialized → Initialized → ContentsSelected) + dialog_open flag para select_contents async. 11 error codes byte-exact facility 0x8002_C1__ (PLAYBACK_FINISHED=01/PARAM=02/BUSY=03/NO_ACTIVE_CONTENT=04/NO_MATCH_FOUND=05/INVALID_CONTEXT=06/PLAYBACK_FAILURE=07/NO_MORE_CONTENT=08/DIALOG_OPEN=09/DIALOG_CLOSE=0A/GENERIC=FF) + CANCELED=1 sentinel. 6 SYSUTIL events (INIT_FINISHED=1/SHUTDOWN=4/LOADING=5/UNLOADING=7/RELEASED=9/GRABBED=11), 8 EVENT_* types (STATUS/INIT/FINALIZE/SELECT_CONTENTS/SET_PLAYBACK_CMD/SET_VOLUME/SET_SELECTION_CONTEXT/UI), 7 PB_CMD_* (STOP=0/PLAY=1/PAUSE=2/NEXT=3/PREV=4/FASTFORWARD=5/FASTREVERSE=6), 5 PB_STATUS_*, PLAYBACK_MEMORY_CONTAINER_SIZE=11MB, SELECTION_CONTEXT_SIZE=2048, PLAYER_MODE_NORMAL=0, 4 REPEATMODE_*, 2 CONTEXTOPTION_* (NONE/SHUFFLE). `SelectionContext` (hash+repeat_mode+context_option+first_track+current_track+playlist) serialize→2048 byte blob (magic "SUS\\0" header + hash + BE current_track tail)/deserialize com INVALID_CONTEXT em magic errado. Music v1 e Music2 compartilham mesmo facility (0x8002_c1__) numerically byte-identical. Invariantes: init bad mode=PARAM, init twice=BUSY, finalize sem init=GENERIC, select_contents sem init=GENERIC, select duplo=DIALOG_OPEN, complete_selection sem dialog=DIALOG_CLOSE, set_playback_command antes de selection (!=STOP)=NO_ACTIVE_CONTENT, cmd unknown=PARAM, NEXT no último track=NO_MORE_CONTENT, PREV no primeiro=NO_MORE_CONTENT, set_selection_context com magic errado=INVALID_CONTEXT, get_selection_context sem content=NO_ACTIVE_CONTENT, set_volume fora [0,1] ou NaN=PARAM, STOP antes de selection=OK (documented exception). | 36 |
| `rpcs3-hle-cellvoice` | wave-7 | libvoice (voice-chat / codec router) HLE (porta `cellVoice.cpp`): `cellVoiceInit/End/StartSession/StopSession/CreatePort/DeletePort/StartPort/PausePort/ResetPort/ConnectIPortToOPort/DisconnectIPortFromOPort/GetPortInfo/GetPortAttr/SetPortAttr/SetVolume/SetMute`. `VoiceManager` FSM 3 estados (Uninitialized → Initialized → SessionRunning) + port graph (in_mic/in_pcm/in_voice → out_pcm/out_voice/out_secondary). 18 error codes byte-exact facility 0x8031_08__ (LIBVOICE_NOT_INIT=01/LIBVOICE_INITIALIZED=02/GENERAL=03/PORT_INVALID=04/ARGUMENT_INVALID=05/CONTAINER_INVALID=06/TOPOLOGY=07/RESOURCE_INSUFFICIENT=08/NOT_IMPLEMENTED=09/ADDRESS_INVALID=0a/SERVICE_DETACHED=0b/SERVICE_ATTACHED=0c/SERVICE_NOT_FOUND=0d/SHAREDMEMORY=0e/EVENT_QUEUE=0f/SERVICE_HANDLE=10/EVENT_DISPATCH=11/DEVICE_NOT_PRESENT=12). 7 BITRATE_* (3850/4650/5700/7300/14400/16000/22533), 7 EVENT_* mask bits + ALL_MASK=0x7F, 6 PCM_* data types (FLOAT/FLOAT_LE/SHORT/SHORT_LE/INTEGER/INTEGER_LE), 4 PORTSTATE_* (IDLE=0/READY=1/BUFFERING=2/RUNNING=3 + NULL=~0), 6 PORTTYPE_* (IN_MIC=0/IN_PCMAUDIO=1/IN_VOICE=2/OUT_PCMAUDIO=3/OUT_VOICE=4/OUT_SECONDARY=5), 6 ATTR_* (ENERGY_LEVEL/VAD/DTX/AUTO_RESAMPLE/LATENCY/SILENCE_THRESHOLD), SAMPLINGRATE_16000, VERSION_100, APPTYPE_GAME_1MB=0x20000000. Port limits: MAX_IN_VOICE=32, MAX_OUT_VOICE=4, GAME_1MB_MAX_IN=8, GAME_1MB_MAX_OUT=2, MAX_PORT=128, INVALID_PORT_ID=0xFF. Voice frame size=160 (10ms @ 16kHz). Invariantes: init bad version=ARGUMENT_INVALID, event_mask bits fora de ALL_MASK=ARGUMENT_INVALID, init twice=LIBVOICE_INITIALIZED, create_port bad type=PORT_INVALID, voice bitrate desconhecido=ARGUMENT_INVALID, PCM buf_size=0 ou bad data_type ou rate!=16kHz=ARGUMENT_INVALID, volume fora [0,2] ou NaN=ARGUMENT_INVALID, exceeds voice cap=RESOURCE_INSUFFICIENT, game_1mb app_type enforces 8/2 cap, connect output->input=TOPOLOGY, connect duplicate edge=SERVICE_ATTACHED, disconnect non-existent=SERVICE_DETACHED, start_port já running=TOPOLOGY, pause_port de Idle=TOPOLOGY, start_session twice=SERVICE_ATTACHED, stop_session sem start=SERVICE_DETACHED. full_graph_smoke (mic+voice_in→voice_out + start all + session cycle) passa. | 44 |
| `rpcs3-hle-celluserinfo` | wave-7 | User profile / account picker HLE (porta `cellUserInfo.cpp`): `cellUserInfoGetList/GetStat/GetCurrentUser/SelectUser_ListType/SelectUser_SetList/EnableOverlay` + test-only `complete_selection/cancel_selection` hooks. `UserInfoRegistry` mantém Vec<UserStat> + current_id + selection FSM (Idle → DialogOpen → Idle) + overlay flag. 4 error codes facility 0x8002_c30_ byte-exact (BUSY=01/INTERNAL=02/PARAM=03/NOUSER=04). Constants: USER_MAX=16, TITLE_SIZE=256, USERNAME_SIZE=64, LISTTYPE_ALL=0/NOCURRENT=1, RET_OK=0/RET_CANCEL=1, USERID_CURRENT=0/USERID_MAX=99999999, FOCUS_LISTHEAD=0xFFFFFFFF. `UserStat` (id+name), `UserList` (ids Vec), `ListSet` (title+focus+optional fixed_list whitelist), `TypeSet` (title+focus+list_type). new_single() factory cria registry com user 1 "User1", empty() factory testa NOUSER paths. Invariantes: add_user id=0 ou >USERID_MAX=PARAM, name >=USERNAME_SIZE=PARAM, id duplicado=PARAM, registry cheio (16º user)=INTERNAL, stat(USERID_CURRENT) resolve para current_id, stat unknown id=NOUSER, stat id>USERID_MAX=PARAM, current_user em empty=NOUSER, set_current unknown=NOUSER, select_user twice=BUSY, title>=TITLE_SIZE=PARAM, bad list_type=PARAM, focus não é FOCUS_LISTHEAD nem user existente=PARAM, empty registry select=NOUSER, fixed_list vazio ou >USER_MAX=PARAM, id=0 ou >MAX na fixed_list=PARAM, complete_selection sem dialog=INTERNAL, cancel_selection sem dialog=INTERNAL, complete com user desconhecido=NOUSER, enable_overlay twice=BUSY. full_selection_flow_smoke (select_set_list→complete→select_list_type→cancel) passa. | 35 |
| `rpcs3-hle-cellsysconf` | wave-7 | Bluetooth / system configuration HLE (porta `cellSysconf.cpp`): `cellSysconfBtGetDeviceList/AbortCb/Open/Close`. `SysconfManager` mantém Vec<BtDeviceInfo> (até BT_DEVICE_LIST_CAPACITY=16) + abort_cb_registered flag + open_handle flag. 1 error code facility 0x8002_bb__ (PARAM=01) — cellSysconf só exporta um único error code. 2 BT_DEVICE_TYPE_* (AUDIO=0x01/HID=0x02), 2 BT_DEVICE_STATE_* (UNAVAILABLE=0/AVAILABLE=1), BT_DEVICE_NAME_SIZE=64, BT_DEVICE_LIST_CAPACITY=16. BtDeviceInfo (device_id+device_type+state+name) + BtDeviceList wrapper. Admin-side hooks: register_device/unregister_device/set_device_state para simular ações do XMB (games não podem fazer isso via HLE — XMB-only). bt_device_list() retorna todos, bt_device_list_filtered(type) retorna subset por AUDIO ou HID. Invariantes: register_device id=0=PARAM, unknown device_type=PARAM, name>=64 chars=PARAM, id duplicado=PARAM, capacity cheio (17º)=PARAM, unregister unknown id=PARAM, set_device_state bad state=PARAM, set_device_state unknown id=PARAM, bt_device_list_filtered unknown type=PARAM, abort_cb já registered=PARAM, open duplo=PARAM, close sem open=PARAM. full_lifecycle_smoke (register 2 devices + set_state + open + abort_cb + list + filtered + unregister + close) passa. | 22 |
| `rpcs3-hle-cellphotoimport` | wave-7 | XMB photo library import HLE (porta `cellPhotoImport.cpp`): `cellPhotoImport` + `cellPhotoImport2`. `PhotoImportManager` FSM 2 estados (Idle → Busy → Idle) async picker dialog. 6 error codes facility 0x8002_c70_ byte-exact (BUSY=01/INTERNAL=02/PARAM=03/ACCESS_ERROR=04/COPY=05/INITIALIZE=06). 7 FT_* format types (UNKNOWN=0/JPEG=1/PNG=2/GIF=3/BMP=4/TIFF=5/MPO=6) + format_from_filename() helper que detecta extensão case-insensitive (.jpg/.jpeg/.png/.gif/.bmp/.tif/.tiff/.mpo). 4 TEX_ROT_* (0°/90°/180°/270°). Constants: VERSION_CURRENT=0, HDD_PATH_MAX=1055, PHOTO_TITLE_MAX_LENGTH=64 (×3 bytes after UTF-8), GAME_TITLE_MAX_SIZE=128, GAME_COMMENT_MAX_SIZE=1024. `SetParam` (file_size_max), `SourcePhoto` (path+size+dims+rotate+title) test-side fixture. `FileData` (dst_file_name+photo_title+game_title+game_comment+data_sub{width/height/format/rotate}). start/complete/cancel/fail_access com FSM enforcement. Invariantes: start version!=0=INITIALIZE, start duplo=BUSY, dst empty ou >=HDD_PATH_MAX=PARAM, dst fora de /dev_hdd0 ou /dev_hdd1=ACCESS_ERROR, game_title>=128=PARAM, file_size_max=0=PARAM, complete sem Busy=INTERNAL, complete path vazio=ACCESS_ERROR, complete size>file_size_max=COPY (+idles), complete comment>=1024=PARAM, complete title>=192 (64×3)=PARAM, complete rotate unknown=PARAM, cancel sem Busy=INTERNAL, fail_access sem Busy=INTERNAL. full_import_lifecycle_smoke (start /dev_hdd0/photo/game_xyz + complete IMG_0001.JPG com TEX_ROT_90 + reimport) passa. | 27 |
| `rpcs3-hle-cellvideoexport` | wave-7 | XMB video library export HLE (porta `cellVideoExport.cpp`): `cellVideoExportInitialize/Initialize2/FromFile/Progress/Finalize`. `VideoExportManager` FSM 3 estados (Uninitialized → Ready → Exporting → Ready) + progress counter 0..=0xFFFF + last_destination tracker. 10 error codes facility 0x8002_ca0_ byte-exact (BUSY=01/INTERNAL=02/PARAM=03/ACCESS_ERROR=04/DB_INTERNAL=05/DB_REGIST=06/SET_META=07/FLUSH_META=08/MOVE=09/INITIALIZE=0A). 2 RET_* (OK=0/CANCEL=1). Constants: VERSION_CURRENT=0, HDD_PATH_MAX=1055, VIDEO_TITLE_MAX_LENGTH=64, GAME_TITLE_MAX_LENGTH=64, GAME_COMMENT_MAX_SIZE=1024, PROGRESS_MAX=0xFFFF, CONTAINER_NONE=0xFFFFFFFE sentinel, MIN_CONTAINER_SIZE=5MB. `SetParam` (title+game_title+game_comment+editable flag). `check_path()` valida: comprimento<1055, apenas alphanumeric + `-`/`_`/`/`/`.`, root em /dev_hdd0/bdvd/hdd1, rejeita `..` traversal. `make_destination()` resolve colisão via suffix `_N` antes da extensão matching C++ `get_available_movie_path` loop. Invariantes: initialize bad version=PARAM, container<5MB (!=CONTAINER_NONE)=PARAM, initialize duplo=BUSY, finalize sem init=INITIALIZE, finalize em Exporting=BUSY, from_file sem init=INITIALIZE, from_file path inválido (caracteres proibidos ou `..`)=PARAM, from_file duplo=BUSY, title/game_title>=192 (64×3) ou comment>=1024=PARAM, tick_progress value>0xFFFF=PARAM, tick_progress sem Exporting=INTERNAL, progress sem init=INITIALIZE, complete_export sem Exporting=INTERNAL, complete filename vazio=PARAM, cancel sem Exporting=INTERNAL. full_export_lifecycle_smoke (init2→from_file recordings/clip1.mp4→tick_progress×2→complete→re-export→cancel→finalize) passa. | 36 |
| `rpcs3-hle-cellmic` | wave-7 | Microphone / MicIn HLE (porta `cellMic.cpp`): `cellMicInit/Init2/End/Open/Close/Start/Stop/Read/GetStatus/GetDeviceAttr/SetDeviceAttr`. `MicManager` com Vec<MicSlot> (até MAX_MICS_PERMISSABLE=4 por jogo; hw total MAX_MICS=8) + ring-buffer VecDeque por slot. 16 error codes facility 0x8014_01__ byte-exact (ALREADY_INIT=01..DEVICE_NOT_SUPPORT=10) + 16 DSP error codes facility 0x8014_02__ byte-exact (DSP=00..DSP_LIB_SAMPRATE=19). 4 SIGTYPE_* (NULL/DSP=1/AUX=2/RAW=4 bitmask, SIGTYPE_ALL_MASK=7), 7 MIC_TYPE_* (UNDEF=-1/UNKNOWN=0/EYETOY1=1/EYETOY2=2/USBAUDIO=3/BLUETOOTH=4/A2DP=5), 6 DEVATTR_* (LED=9/GAIN=10/VOLUME=201/AGC=202/CHANVOL=301/DSPTYPE=302), 5 SIGATTR_* (BKNGAIN=0/REVERB=9/AGCLEVEL=26/VOLUME=301/PITCHSHIFT=331), 6 SIGSTATE_* (LOCTALK=0/FARTALK=1/NSR=3/AGC=4/MICENG=5/SPKENG=6), 3 STARTFLAG_LATENCY_* (4=0x1/2=0x2/1=0x3), NULL_DEVICE_ID=-1. MicFormat (channel_num+subframe_size+bit_resolution+data_type+sample_rate) com validação (1-2 canais, bit_resolution ∈ {8,16,24,32}, sample_rate>0). Slot FSM Closed → Opened → Running. inject_pcm test hook simula captura de backend. Invariantes: init duplo=ALREADY_INIT, end sem init=NOT_INIT, open sem init=NOT_INIT, device_id<0=PARAM, mic_type desconhecido=DEVICE_NOT_SUPPORT, signal_types=0 ou bits fora de ALL_MASK=PARAM, channel_num fora [1,2] ou bit_resolution não em {8,16,24,32} ou sample_rate=0=PARAM, 5º open=PORT_FULL, open mesmo device_id=ALREADY_OPEN, close unknown=NOT_OPEN, start unknown=NOT_OPEN, start_flag não latency_1/2/4=PARAM, start twice=PARAM, stop sem start=NOT_RUN, read sem start=NOT_RUN, inject sem start=NOT_RUN, stop limpa pending_pcm, device_attr unknown attr=PARAM, set_device_attr clamps volume/gain em [0,127], AGC booleaniza. full_mic_lifecycle_smoke (init→open USBAUDIO RAW+AUX→set_volume 80→start LATENCY_2→inject 4B→read 4B→status→stop→close→end) passa. | 44 |
| `rpcs3-hle-cellscreenshot` | wave-7 | Screenshot capture HLE (porta `cellScreenshot.cpp`): `cellScreenShotEnable/Disable/SetParameter/SetOverlayImage`. `ScreenshotManager` com enabled flag + SetParam (photo_title+game_title+game_comment) + Option<Overlay> (dir_name+file_name+offset_x+offset_y). 5 error codes byte-exact facility 0x8002_d10_ (INTERNAL=01/PARAM=02/DECODE=03/NOSPACE=04/UNSUPPORTED_COLOR_FORMAT=05). Constants: PHOTO_TITLE_MAX_LENGTH=64, GAME_TITLE_MAX_LENGTH=64, GAME_COMMENT_MAX_SIZE=1024, ALLOWED_OVERLAY_ROOTS=[/dev_hdd0, /dev_hdd1, /dev_bdvd]. Enable/Disable idempotentes (matching C++ no-error behavior). SetParameter valida título ≤192 bytes (64×3) e comment ≤1024 bytes (PARAM). SetOverlayImage valida dir_name não vazio, file_name não vazio e .png (case-insensitive; outros formatos=DECODE), dir em ALLOWED_OVERLAY_ROOTS, offsets não negativos. overlay_path() helper retorna dir/file joined com slash handling. get_photo_title/get_game_title truncam para MAX_LENGTH chars; get_game_comment faz UTF-8-safe truncation para <GAME_COMMENT_MAX_SIZE bytes. Invariantes: set_parameter title>=192 ou comment>=1024=PARAM, set_overlay empty=PARAM, foreign root (usb/flash)=PARAM, negative offset=PARAM, non-PNG extension=DECODE, clear_overlay reseta Option. full_screenshot_lifecycle_smoke (enable→set_param→set_overlay→overlay_path→get_photo_title→clear→disable) passa. | 25 |
| `rpcs3-hle-cellrudp` | wave-7 | Reliable UDP (libRudp) HLE (porta `cellRudp.cpp`): `cellRudpInit/End/CreateContext/TerminateContext/SetOption/GetOption/Bind/Unbind/Write/Read/Flush/Poll`. `RudpManager` + Vec<Context> (até MAX_CONTEXTS=256) + HashMap<Vport→ContextId> para collision detection. 38 error codes byte-exact facility 0x8077_00__ (NOT_INITIALIZED=01..KEEP_ALIVE_FAILURE=26 — cellRudp tem o maior range de error codes do namespace). 17 OPTION_* (MAX_PAYLOAD=1, SNDBUF/RCVBUF, NODELAY, DELIVERY_CRITICAL, ORDER_CRITICAL, NONBLOCK, STREAM, CONNECTION/CLOSE_WAIT/AGGREGATION_TIMEOUT, LAST_ERROR=14, READ/WRITE/FLUSH_TIMEOUT, KEEP_ALIVE_INTERVAL/TIMEOUT), 4 POLL_EV_* bits (READ/WRITE/FLUSH/ERROR + ALL_MASK=0x0F), 3 MUXMODE_* (MUTED=0/SINGLE=1/MULTIPLE=2). MAX_VPORT=0xFFFF (port 0 reserved), PAYLOAD_LIMIT=64KB default. Context com options HashMap + VecDeque<Vec<u8>> rx_queue + last_error + bound_vport Option. Per-context defaults via `Context::default_option` (timeouts=30s em µs, boolean flags=0, SND/RCVBUF=64KB). inject_recv test hook simula arrival de mensagens. Invariantes: init duplo=ALREADY_INITIALIZED, create sem init=NOT_INITIALIZED, socket<0=INVALID_SOCKET, muxmode desconhecido=INVALID_MUXMODE, handler=None=NO_EVENT_HANDLER, >256 contexts=TOO_MANY_CONTEXTS, set_option unknown=INVALID_OPTION, LAST_ERROR write=INVALID_OPTION, timeout negativo=INVALID_ARGUMENT, MAX_PAYLOAD 0 ou >PAYLOAD_LIMIT=INVALID_ARGUMENT, boolean options clampados 0/1, vport 0 ou >0xFFFF=INVALID_VPORT, bind duplo=ALREADY_BOUND, vport conflict=VPORT_IN_USE, unbind sem bind=NOT_BOUND, write sem bind=NOT_BOUND, write empty=INVALID_ARGUMENT, write >MAX_PAYLOAD=PAYLOAD_TOO_LARGE (set last_error), read empty non-block=WOULDBLOCK, read empty blocking=END_OF_DATA, read buffer small=BUFFER_TOO_SMALL (re-queues msg), poll mask >ALL_MASK=INVALID_ARGUMENT, terminate_context libera vport. full_lifecycle_smoke (init→create×2 SINGLE+MULTIPLE→set NODELAY+NONBLOCK→bind 5000+5001→write+inject+read→flush→poll→terminate×2→end) passa. | 42 |
| `rpcs3-hle-cellhttp` | wave-7 | HTTP client library HLE (porta `cellHttp.cpp`): `cellHttpInit/End/CreateClient/DestroyClient/CreateTransaction/DestroyTransaction/AddRequestHeader/SendRequest/GetStatusCode/GetResponseHeader/GetContentLength/ReadResponseBody`. `HttpManager` + Vec<Client> + Vec<Transaction> com dependency guard (destroy_client com live transaction=BAD_CLIENT). ~36 error codes byte-exact facility 0x8071_00__ (ALREADY_INITIALIZED=01/NOT_INITIALIZED=02/NO_MEMORY=03/NO_BUFFER=04/INSUFFICIENT=06/INVALID_URI=07/INVALID_HEADER=08/BAD_METHOD=09/BAD_CLIENT=10/BAD_TRANS=11/NO_CONNECTION=12/NO_REQUEST_SENT=13/ALREADY_BUILT=14/ALREADY_SENT=15/NO_HEADER=16/NO_CONTENT_LENGTH=17/TOO_MANY_REDIRECTS=18/TOO_MANY_AUTHS=19/TRANS_ABORTED=24/UNAVAILABLE=26/INVALID_VALUE=27/CANNOT_AUTHENTICATE=28/COOKIE_NOT_FOUND=41/COOKIE_INVALID_DOMAIN=42/LINE_EXCEEDS_MAX=45/REQUIRES_BASIC_AUTH=46/UNKNOWN=51/INTERNAL=52/NET_CONNECT_TIMEOUT=92/NET_SELECT_TIMEOUT=93/NET_SEND_TIMEOUT=94). Net error categories (RESOLVER=0x100/ABORT=0x200/OPTION=0x300/SOCKET=0x400/CONNECT=0x500/SEND=0x600/RECV=0x700/SELECT=0x800 high bytes). Constants: MAX_USERNAME=256, MAX_PASSWORD=256, MAX_HEADER_LINE=16KB, MAX_REDIRECTS=5. **8 Method enum** (GET/HEAD/POST/PUT/DELETE/OPTIONS/CONNECT/TRACE) com parse (upper-case canonical). **Uri::parse** valida scheme (http/https only), authority non-empty, port numérico 0..=65535, constrói path com '/' default. Uri::build reemite URL preservando default-port elision. Transaction FSM Built → Sent → Received. inject_response test hook. Invariantes: init duplo=ALREADY_INITIALIZED, end sem init=NOT_INITIALIZED, create sem init=NOT_INITIALIZED, destroy client com live trans=BAD_CLIENT, uri não http/https=INVALID_URI, uri empty host=INVALID_URI, bad port (>65535 ou non-numeric)=INVALID_URI, add_header empty name ou com :/CR/LF=INVALID_HEADER, add_header line>16KB=LINE_EXCEEDS_MAX, add_header depois de send=ALREADY_SENT, send twice=ALREADY_SENT, inject sem send=NO_REQUEST_SENT, get_status_code sem receive=NO_REQUEST_SENT, get_response_header missing=NO_HEADER, Content-Length malformado=NO_CONTENT_LENGTH, cookie empty ou com '='=COOKIE_INVALID_DOMAIN, get_cookie missing=COOKIE_NOT_FOUND, set_basic_auth oversize=INVALID_VALUE. full_http_flow_smoke (init→client→user_agent→basic_auth→cookie→POST trans→headers→send→inject_response→status/content_length→read_body→destroy×2→end) passa. | 42 |
| `rpcs3-hle-cellmusicexport` | wave-7 | XMB music library export HLE (porta `cellMusicExport.cpp`): `cellMusicExportInitialize/FromFile/Progress/Finalize`. `MusicExportManager` FSM 3 estados idêntica ao cellVideoExport (Uninitialized → Ready → Exporting → Ready) + progress counter + last_destination tracker. 10 error codes facility 0x8002_c60_ byte-exact (BUSY=01/INTERNAL=02/PARAM=03/ACCESS_ERROR=04/DB_INTERNAL=05/DB_REGIST=06/SET_META=07/FLUSH_META=08/MOVE=09/INITIALIZE=0A). Constants: VERSION_CURRENT=0, HDD_PATH_MAX=1055, MUSIC_TITLE_MAX_LENGTH=64, GAME_TITLE_MAX_LENGTH=64, GAME_COMMENT_MAX_SIZE=1024, PROGRESS_MAX=0xFFFF, CONTAINER_NONE=0xFFFFFFFE, MIN_CONTAINER_SIZE=3MB (menor que vídeo porque ficheiro audio é menor). **`SetParam` com 5 campos de metadata** (title + game_title + artist + genre + game_comment — superset sobre cellVideoExport que tem só 3). check_path() idêntica ao cellVideoExport mas dst root é `/dev_hdd0/music/`. make_destination() com suffix `_N` resolve colisões. Invariantes: initialize bad version=PARAM, container<3MB (!=CONTAINER_NONE)=PARAM, initialize duplo=BUSY, finalize sem init=INITIALIZE, finalize em Exporting=BUSY, from_file sem init=INITIALIZE, from_file path inválido (caracteres proibidos ou `..`)=PARAM, from_file duplo=BUSY, title/artist/genre>=192 (64×3) ou game_title>=192 ou comment>=1024=PARAM, tick_progress value>0xFFFF=PARAM, tick sem Exporting=INTERNAL, progress sem init=INITIALIZE, complete sem Exporting=INTERNAL, complete empty filename=PARAM, cancel sem Exporting=INTERNAL. full_export_lifecycle_smoke (init→from_file recordings/theme.mp3→tick×2→complete→re-export com suffix collision→finalize) passa. | 36 |
| `rpcs3-hle-cellphotoexport` | wave-7 | XMB photo library export HLE (porta `cellPhotoExport.cpp`): `cellPhotoExportInitialize/FromFile/Progress/Finalize`. `PhotoExportManager` FSM 3 estados idêntica ao cellMusicExport/cellVideoExport. 10 error codes facility 0x8002_c20_ byte-exact (BUSY=01/INTERNAL=02/PARAM=03/ACCESS_ERROR=04/DB_INTERNAL=05/DB_REGIST=06/SET_META=07/FLUSH_META=08/MOVE=09/INITIALIZE=0A). Constants: VERSION_CURRENT=0, HDD_PATH_MAX=1055, PHOTO_TITLE_MAX_LENGTH=64, GAME_TITLE_MAX_LENGTH=64, GAME_COMMENT_MAX_SIZE=1024, PROGRESS_MAX=0xFFFF, CONTAINER_NONE=0xFFFFFFFE, MIN_CONTAINER_SIZE=2MB (menor ainda que music porque foto é o menor ficheiro). **`SetParam` com 3 campos** (photo_title + game_title + game_comment — mesmo shape que cellScreenshot). check_path() + make_destination() idênticas ao pattern Export, dst root `/dev_hdd0/photo/`. Invariantes: identical ao MusicExport modulo facility code, container floor (2MB vs 3MB), e 3 vs 5 campos metadata. full_export_lifecycle_smoke (init→from_file cap/shot1.png→tick×2→complete→re-export com suffix collision→finalize) passa. Completa a trinca de "export to XMB library" crates (music/photo/video). | 34 |
| `rpcs3-hle-cellhttputil` | wave-7 | HTTP utilities HLE (porta `cellHttpUtil.cpp`): stateless helpers `Uri::parse/build/copy_into` + `percent_encode/decode` + `base64_encode/decode` + `RequestLine/StatusLine::format/parse` + `parse_header`. 10 error codes facility 0x8071_10__ byte-exact (NO_MEMORY=01/NO_BUFFER=02/NO_STRING=03/INSUFFICIENT=04/INVALID_URI=05/INVALID_HEADER=06/INVALID_REQUEST=07/INVALID_RESPONSE=08/INVALID_LENGTH=09/INVALID_CHARACTER=0A). 5 URI_FLAG_* bits (FULL_URI=0/NO_SCHEME=1/NO_CREDENTIALS=2/NO_PASSWORD=4/NO_PATH=8). `Uri::parse` aceita `scheme://[user[:pass]@]host[:port][/path]` com default ports (http=80/https=443/ftp=21), valida scheme chars (alnum + `+-.`), host não vazio, port ≤0xFFFF. `Uri::build` com flags suprime selectivamente componentes (NO_CREDENTIALS remove user+pass, NO_PASSWORD mantém user só), elide porta default. `Uri::copy_into(pool_size)` calcula required = soma dos strings nul-terminated e reporta INSUFFICIENT se pool<required. **percent_encode** RFC 3986: preserva `A-Z/a-z/0-9/-/_/./~`, escapa tudo mais com `%HH` uppercase. **percent_decode** rejeita hex malformado (INVALID_CHARACTER), escape truncado (<3 bytes), control chars raw. **base64_encode/decode** RFC 4648 com `=` padding; decode ignora whitespace, rejeita length%4!=0 (INVALID_LENGTH) e chars fora do alphabet. **RequestLine** (METHOD PATH PROTO/MAJ.MIN) + **StatusLine** (PROTO/MAJ.MIN STATUS REASON) format/parse round-trip com CRLF, status_code validado em [100,599]. **parse_header**: rejeita empty name, space in name, control chars (<0x20 ou 0x7F). full_utilities_smoke encadeia parse→build(NO_CREDENTIALS)→percent encode/decode→base64 round-trip→request line format. | 42 |
| `rpcs3-hle-cellhttps` | wave-7 | HTTPS / TLS extension HLE (extrai subset de `cellHttp.h:121-139`): `cellHttpsInit/End/SetCaList/CreateContext/DestroyContext/ConnectionCreate/Destroy` + test-only `complete_handshake`. `HttpsManager` com Vec<Certificate> CA list + Vec<Context> + Vec<Connection> com dependency guard (destroy_context com live connection=CONTEXT_CREATION). 16 error codes byte-exact facility 0x8071_0a__ (CERTIFICATE_LOAD=01/BAD_MEMORY=02/CONTEXT_CREATION=03/CONNECTION_CREATION=04/SOCKET_ASSOCIATION=05/HANDSHAKE=06/LOOKUP_CERTIFICATE=07/NO_SSL=08/KEY_LOAD=09/CERT_KEY_MISMATCH=0A/KEY_NEEDS_CERT=0B/CERT_NEEDS_KEY=0C/RETRY_CONNECTION=0D) + 3 high-byte subcategories (NET_SSL_CONNECT=0x0b00/SEND=0x0c00/RECV=0x0d00). 4 TlsVersion enum (SSLv3=0x0300/TLS10=0x0301/TLS11=0x0302/TLS12=0x0303). Limits: MAX_CA_LIST=64, MAX_CONTEXTS=32, MAX_CONNECTIONS=64. `Certificate` validate (der non-empty, ≤16KB, subject/issuer non-empty, not_after>not_before) + `PrivateKey` validate (non-empty, ≤8KB). set_client_identity enforce 4-byte prefix match cert/key → CERT_KEY_MISMATCH. complete_handshake matches peer cert issuer contra CA list subjects → LOOKUP_CERTIFICATE se untrusted. require_handshake guard. Invariantes: init duplo=CONTEXT_CREATION, end sem init=NO_SSL, set_ca_list >64=BAD_MEMORY, cert inválido=CERTIFICATE_LOAD, destroy_context com live connection=CONTEXT_CREATION, create_connection socket<0=SOCKET_ASSOCIATION, bad context id=CONTEXT_CREATION, handshake twice=HANDSHAKE, require_handshake antes=HANDSHAKE, peer_cert antes=HANDSHAKE, client_cert_only=CERT_NEEDS_KEY, client_key_only=KEY_NEEDS_CERT. full_https_lifecycle_smoke (init→set_ca_list→create_context TLS1.2→set_client_identity mutual TLS→create_connection→complete_handshake matching trust chain→peer_cert→destroy×2→end) passa. | 36 |
| `rpcs3-hle-cellsync2` | wave-7 | 2nd-gen sync primitives HLE (porta `cellSync2.cpp`): Mutex/Cond/Semaphore/Queue em user-memory 128-byte-aligned que podem ser compartilhados entre PPU threads, PPU fibers, SPURS tasks e SPURS jobs. `Sync2` manager unified com Vec<Mutex>+Vec<Cond>+Vec<Semaphore>+Vec<Queue>. 12 error codes byte-exact facility 0x8041_0C__ (AGAIN=01/INVAL=02/NOMEM=04/DEADLK=08/PERM=09/BUSY=0A/STAT=0F/ALIGN=10/NULL_POINTER=11/NOT_SUPPORTED_THREAD=12/NO_NOTIFIER=13/NO_SPU_CONTEXT_STORAGE=14). 5 THREAD_TYPE_* bits (PPU_THREAD=1/PPU_FIBER=2/SPURS_TASK=4/SPURS_JOBQUEUE_JOB=8/SPURS_JOB=0x100) + ALL_MASK. NAME_MAX_LENGTH=31, OBJECT_ALIGNMENT=128, OBJECT_SIZE=128, ATTRIBUTE_SIZE=128. MutexAttribute (sdk+thread_types+max_waiters+recursive+name) + CondAttribute + SemaphoreAttribute + QueueAttribute (element_size+depth+max_push/pop_waiters). Mutex com recursive support e owner tracking. Cond bound a mutex — wait libera o mutex automaticamente. Semaphore com initial_count/max_count + acquire/release wake-up. Queue FIFO com fixed element_size VecDeque. LockOutcome enum (Acquired/WouldBlock). Invariantes: address misalignment=ALIGN, null addr=NULL_POINTER, thread_types=0 ou bits fora ALL_MASK=NOT_SUPPORTED_THREAD, name >31 ou control chars=INVAL, caller=0=NULL_POINTER, non-recursive self-relock=DEADLK, unlock sem owner=PERM, unlock por não-owner=PERM, max_waiters excedido=AGAIN, finalize held/with-waiters=BUSY, mutex_finalize com cond bound=BUSY (dependency guard), cond_init com mutex inválido=INVAL, cond_wait sem ownership=PERM, cond_wait libera mutex, semaphore_init bad counts=INVAL, release count≤0 ou over max=INVAL, queue element_size/depth=0=INVAL, queue element_size mismatch em push/pop=INVAL, queue push cheia/pop vazia=WouldBlock + max_waiters guard. full_sync2_lifecycle_smoke cobre mutex recursive + cond + semaphore + queue + finalize dependency ordering. | 38 |
| `rpcs3-hle-cellbgdl` | wave-7 | Background download HLE (porta `cellBgdl.cpp`): `cellBGDLSetMode/GetInfo/GetInfo2` para games polling do progresso de patch/DLC prefetch durante o gameplay. `BgdlManager` com modo global + Vec<BgdlTask> (até MAX_TASKS=64). 5 error codes byte-exact facility 0x8002_ce0_ (BUSY=01/INTERNAL=02/PARAM=03/ACCESS_ERROR=04/INITIALIZE=05). 5 STATE_* (ERROR=0/PAUSE=1/READY=2/RUN=3/COMPLETE=4), 2 MODE_* (AUTO=0/ALWAYS_ALLOW=1). `BgdlTask` (task_id+received_size+content_size+state+title) + `BgdlInfo` (received+content+state tuple exposto ao game). Shell-side hooks register_task/unregister_task/update_progress simulam ações do XMB (games não podem registrar, só observar). Invariantes: bad mode value=PARAM, register task_id=0=PARAM, content_size=0=PARAM, received>content=PARAM, unknown state=PARAM, duplicate task_id=PARAM, 65º task=INTERNAL, unregister unknown=PARAM, update_progress unknown task=PARAM, update com received>content=PARAM, update com bad state=PARAM, info unknown task=PARAM, info2 espelha info. full_lifecycle_smoke (set_mode ALWAYS_ALLOW→register→update progression READY→RUN→PAUSE→RUN→COMPLETE→info2 reports complete→unregister) passa. | 25 |
| `rpcs3-hle-cellpamf` | wave-7 | PAMF (PlayStation Application Media Format) reader HLE (porta `cellPamf.cpp`): `cellPamfReaderInitialize/GetNumberOfStreams/GetNumberOfSpecificStreams/SetStream/SetStreamWithIndex/GetCurrentStreamNumber/GetHeader/StreamTypeToEsFilterId`. `PamfReader` FSM 2 estados (Uninitialized → Open) + Vec<Stream> (até MAX_STREAMS=16) + PamfHeader + current_stream cursor. 8 error codes facility 0x8061_05__ byte-exact (STREAM_NOT_FOUND=01/INVALID_PAMF=02/INVALID_ARG=03/UNKNOWN_TYPE=04/UNSUPPORTED_VERSION=05/UNKNOWN_STREAM=06/EP_NOT_FOUND=07/NOT_AVAILABLE=08). 13 STREAM_TYPE_* (AVC=0/M2V=1/ATRAC3PLUS=2/PAMF_LPCM=3/AC3=4/USER_DATA=5/PSMF_AVC=6/PSMF_ATRAC3PLUS=7/PSMF_LPCM=8/PSMF_USER_DATA=9/VIDEO=20 virtual/AUDIO=21 virtual/UNK=22), 7 CODING_TYPE_* (M2V=0x02/AVC=0x1b/PAMF_LPCM=0x80/AC3=0x81/ATRAC3PLUS=0xdc/USER_DATA=0xdd/PSMF=0xff). 2 ATTRIBUTE_* flags (VERIFY_ON=1/MINIMUM_HEADER=2), AVC constants (profile MAIN=77/HIGH=100, levels 2P1..4P2), FRC tables (AVC + M2V shifted by 1), aspect ratios, colour primaries, transfer characteristics. MAGIC=b"PAMF", SUPPORTED_VERSION=0x0100. `Stream` com coding_type+stream_id+StreamInfo enum (Avc/M2v/Audio/UserData). `TimeStamp` with upper/lower u32 + as_u64 packer. coding_type_to_stream_type + stream_matches_type (virtual VIDEO matches AVC/M2V, AUDIO matches ATRAC/LPCM/AC3). `stream_type_to_es_filter_id` helper rejeita virtual types. Invariantes: bad attr bits fora ATTRIBUTE_ALL_MASK=INVALID_ARG, header<8 bytes=INVALID_PAMF, bad magic=INVALID_PAMF, version!=0x0100=UNSUPPORTED_VERSION, queries sem init=NOT_AVAILABLE, >16 streams=INVALID_PAMF, end_pts<start_pts=INVALID_PAMF, set_stream fora do range=STREAM_NOT_FOUND, unknown stream_type filter=UNKNOWN_TYPE, set_stream_with_index não encontrado=STREAM_NOT_FOUND. full_pamf_reader_lifecycle_smoke (init→set_streams AVC+ATRAC+UserData→set_time_stamps 30s→specific_count filter→set_stream_with_index AUDIO 0→current_stream) passa. | 29 |

| `rpcs3-hle-cellfiber` | wave-7 | PPU fiber (coroutine) HLE (porta `cellFiber.cpp`): `cellFiberPpuInitialize/InitializeScheduler/FinalizeScheduler/RunFibers/CheckFlags/CreateFiber/ExitFiber/Yield/JoinFiber/ContextInitialize/Finalize`. `Fiber` manager + Vec<Scheduler> (MAX_SCHEDULERS=16) + Vec<ContextEntry> (MAX_CONTEXTS=256). Each scheduler holds até MAX_FIBERS_PER_SCHEDULER=256 fibers. 11 error codes facility 0x8076_00__ byte-exact (AGAIN=01/INVAL=02/NOMEM=04/DEADLK=08/PERM=09/BUSY=0A/ABORT=0C/STAT=0F/ALIGN=10/NULL_POINTER=11/NOSYSINIT=20). Struct-size constants byte-exact com C++ CHECK_SIZE_ALIGN: Scheduler 512/128, SchedulerAttribute 256/8, Fiber 896/128, FiberAttribute 256/8, Context 640/16, ContextAttribute 128/8, WorkerControl 768/128, WorkerControlAttribute 384/8. NAME_MAX_LENGTH=31. SchedulerAttribute com auto_check_flags + debugger_support + interval_usec. FiberAttribute com name + on_exit_callback + arg. ContextAttribute com name + debugger_support. FiberState enum (Ready→Running→Yielded / Exited). run_fibers simula cooperative schedule — Ready/Yielded transitam para Running→Yielded (representing slice). join retorna exit_code ou BUSY se ainda running. on_exit_callback fire flag tracked. Invariantes: init alignment=ALIGN, null pool=NULL_POINTER, small pool<512=NOMEM, init duplo=BUSY, operations sem init=NOSYSINIT, init_scheduler auto_check_flags=true mas interval=0=INVAL, 17º scheduler=NOMEM, finalize com fiber viva=BUSY, create_fiber addr não 128-aligned=ALIGN, priority fora [0,1000]=INVAL, name>31 ou control chars=INVAL, fiber count>256=NOMEM, exit_fiber twice=STAT, yield em Yielded ou Exited=STAT, join running=BUSY, context alignment!=16=ALIGN. full_fiber_lifecycle_smoke (init→scheduler com debugger_support+auto_check+interval→2 fibers com diferentes priorities e on_exit cb→run_fibers rodada 1→exit+join→flag signal→cleanup→context init/finalize separado) passa. | 35 |

| `rpcs3-hle-cellovis` | wave-7 | SPU overlay table management HLE (porta `cellOvis.cpp`): `cellOvisGetOverlayTableSize/InitializeOverlayTable/FixSpuSegments/InvalidateOverlappedSegments`. `OverlayTable` com Vec<OverlayEntry> (até MAX_OVERLAYS=128) + initialized/aborted flags. Cada overlay é ls_addr+size+flags, segments SPU são seg_type+ls_addr+size+source+fill. 3 error codes facility 0x8041_04__ byte-exact (INVAL=02/ABORT=0C/ALIGN=10). Constants: LOCAL_STORE_SIZE=256KB, SEGMENT_ALIGN=16, TABLE_ENTRY_SIZE=16, TABLE_HEADER_SIZE=16. 3 SEG_TYPE_* (COPY=1/FILL=2/INFO=4) byte-exact com sys_spu_image.h. `overlay_table_size(count)` = header+count*16. `OverlayEntry.validate()` rejeita size=0, size>256KB, addr/size não 16-aligned, range excedendo LS. `OverlayEntry.overlaps()` detecta sobreposição via intervalos semi-abertos (touching boundaries don't overlap). `SpuSegment.validate()` permite INFO size=0, COPY/FILL enforce size>0 e ≤256KB. `fix_spu_segments()` filtra segmentos que sobrepõem overlays (INFO sempre passa). `invalidate_overlapped_segments()` in-place variant + retorna removed count. `find_at()` localiza overlay cobrindo endereço LS. Invariantes: init addr=0=INVAL, addr não 16-aligned=ALIGN, >128 overlays=INVAL, entry bad=propagate ALIGN/INVAL, fix/invalidate sem init=INVAL, após abort=ABORT, segment bad=propagate INVAL/ALIGN, seg_type unknown=INVAL. full_ovis_lifecycle_smoke (overlay_table_size 3→init 3 overlays→mix segments com overlaps→fix retains 2 (no-overlap + INFO)→find_at locate overlay→abort→subsequent ops=ABORT) passa. | 29 |

| `rpcs3-hle-cellrec` | wave-7 | Game video recording HLE (porta `cellRec.cpp`): `cellRecOpen/Close/Start/Stop/QueryMemSize/SetInfo/GetInfo`. `RecManager` FSM 4 estados (Unloaded → Opened → Started → Stopped → Opened/Closed) com loopback Started→Stopped→Started. 7 error codes byte-exact facility 0x8002_c50_ (OUT_OF_MEMORY=01/FATAL=02/INVALID_VALUE=03/FILE_OPEN=04/FILE_WRITE=05/INVALID_STATE=06/FILE_NO_DATA=07). 6 STATUS_* (UNLOAD=0/OPEN=1/START=2/STOP=3/CLOSE=4/ERR=10). Memory caps MAX=16MB (9MB legacy pre-SDK 0x300000), MAX_PATH_LEN=1023, AUDIO_BLOCK_SAMPLES=256. Thread defaults PPU=400/SPU=60. 3 CAPTURE_PRIORITY_*, 6 VIDEO_INPUT_* (DISABLE/ARGB_4_3/ARGB_16_9/RGBA_4_3/RGBA_16_9/YUV420PLANAR_16_9), 15 OPTION_* keys. ~30 video format codes byte-exact (MPEG4 0x0000-0x0240, AVC-MP 0x1000-0x1130, AVC-BL 0x2000-0x2130, MJPEG 0x3060-0x3690, M4HD 0x4010-0x4670, YouTube 0x0310 alias) + 8 audio formats (AAC_64K/96K/128K, ULAW_384K/768K, PCM_384K/768K/1536K). `RecParam` (video_format+audio_format+options HashMap). `MovieMetadata` (game_title/movie_title/description/userdata com limites 128/128/384/64). `SceneMetadata` (type+time+title/tags limits 128 + 6 tags × 64 bytes). `query_mem_size(param, legacy_sdk)` retorna full cap para HD720 formats, half para resto. Invariantes: open sem state=Unloaded=INVALID_STATE, bad video/audio fmt=INVALID_VALUE, bad option=INVALID_VALUE, empty/oversize path=INVALID_VALUE, close Started=INVALID_STATE, close sem open=INVALID_STATE, start sem Opened/Stopped=INVALID_STATE, stop sem Started=INVALID_STATE, set_info sem open=INVALID_STATE, end_time<start_time=INVALID_VALUE, oversize metadata=INVALID_VALUE, bad scene_type=INVALID_VALUE, >6 tags=INVALID_VALUE, unknown info key=INVALID_VALUE. full_rec_lifecycle_smoke (query_mem→open→set movie_meta→start_time=0→start→end_time=30s→scene_meta highlight→stop→get movie_time=30000→close) passa. | 40 |

| `rpcs3-hle-cellstorage` | wave-7 | USB storage data import/export HLE (porta `cellStorage.cpp`): `cellStorageDataImport/Export`. `StorageManager` FSM 2 estados (Idle → Busy → Idle). 5 error codes facility 0x8002_be0_ byte-exact (BUSY=01/INTERNAL=02/PARAM=03/ACCESS_ERROR=04/FAILURE=05). 2 VERSION_* (CURRENT=0/DST_FILENAME=1). Constants: HDD_PATH_MAX=1055, MEDIA_PATH_MAX=1024, FILENAME_MAX=64, FILESIZE_MAX=1GB, TITLE_MAX=256. IMPORT_FILENAME="IMPORT.BIN", EXPORT_FILENAME="EXPORT.BIN" (canonical). `check_hdd_path`: /dev_hdd0 ou /dev_hdd1 only, no `..`, ≤HDD_PATH_MAX. `check_media_path`: /dev_usb* ou /dev_ms ou /dev_cf, no `..`, ≤MEDIA_PATH_MAX. `check_filename`: ASCII alnum + `-_.` only, no `..`, ≤FILENAME_MAX. SetParam (file_size_max+title) com title 1..=256 bytes + size 1..=1GB. Direction enum (Import=USB→HDD, Export=HDD→USB). Invariantes: import duplo=BUSY, bad version=PARAM, media path fora whitelist ou traversal=ACCESS_ERROR, hdd path fora /dev_hdd0|1 ou traversal=ACCESS_ERROR, empty/oversize title=PARAM, zero file_size=PARAM, size>1GB=PARAM, complete/cancel sem Busy=INTERNAL, complete_with_failure retorna FAILURE (+ idles para re-use). full_lifecycle_smoke (import→complete→export→complete→import→failure→re-use) passa. | 25 |

| `rpcs3-hle-celljpgdec` | wave-7 | JPEG decoder HLE (porta `cellJpgDec.cpp`): `cellJpgDecCreate/Destroy/Open/Close/ReadHeader/SetParameter/DecodeData`. `JpgDec` manager + Vec<Handle> (MAX_HANDLES=1023, id_base=1 matching C++ CellJpgDecSubHandle). Handle FSM 4 estados (Opened → HeaderRead → Configured → Decoding → Configured loopback). 9 error codes byte-exact facility 0x8061_11__ (HEADER=01/STREAM_FORMAT=02/ARG=03/SEQ=04/BUSY=05/FATAL=06/OPEN_FILE=07/SPU_UNSUPPORT=08/CB_PARAM=09). 9 CS_* (UNKNOWN=0/GRAYSCALE=1/RGB=2/YCBCR=3/RGBA=10/UPSAMPLE_ONLY=11/ARGB=20/GRAYSCALE_TO_ALPHA_RGBA=40/ARGB=41) byte-exact. 2 SRC_* (FILE=0/BUFFER=1), 2 OUT_* (TOP_TO_BOTTOM=0/BOTTOM_TO_TOP=1), 2 DEC_STATUS_* (FINISH=0/STOP=1). Limits: MAX_WIDTH=MAX_HEIGHT=8192, MAGIC_SOI=[0xFF,0xD8]. `bytes_per_pixel` helper: GRAYSCALE=1, RGB/YCBCR=3, RGBA/ARGB/GRAYSCALE_TO_ALPHA=4. `Src` validate: FILE needs non-empty filename + size>0, BUFFER needs non-zero ptr+size. `InParam` validate: down_scale ∈ {1,2,4,8}, output_mode known, color_space known. `OutParam` derive output_width=width/down_scale etc. + use_memory_space total bytes. Invariantes: create zero space=ARG, duplo=BUSY, destroy sem create=SEQ, destroy com handles=BUSY, open sem create=SEQ, bad src=ARG, >1023 handles=FATAL, file sem name=OPEN_FILE, buffer null ptr/zero size=ARG, read_header SOI errado=HEADER, too short=HEADER, zero/oversize dims=STREAM_FORMAT, unknown color_space=STREAM_FORMAT, num_components not 1..=4=STREAM_FORMAT, read_header twice=SEQ, set_parameter antes de header=SEQ, bad down_scale=ARG, decode sem configure=SEQ, decode repeatable após success. full_jpg_lifecycle_smoke (create→open file→read_header→set_parameter RGBA down_scale 2→decode→close→destroy) passa. | 36 |

| `rpcs3-hle-cellpngdec` | wave-7 | PNG decoder HLE (porta `cellPngDec.cpp`): `cellPngDecCreate/Destroy/Open/Close/ReadHeader/SetParameter/DecodeData`. `PngDec` manager + Vec<Handle> (MAX_HANDLES=1023, id_base=1). Handle FSM 3 estados (Opened → HeaderRead → Configured). 10 error codes byte-exact facility 0x8061_12__ (HEADER=01/STREAM_FORMAT=02/ARG=03/SEQ=04/BUSY=05/FATAL=06/OPEN_FILE=07/SPU_UNSUPPORT=08/SPU_ERROR=09/CB_PARAM=0A). CODEC_VERSION=0x0042_0000. MAGIC_PNG=[0x89, 'P', 'N', 'G', 0x0D, 0x0A, 0x1A, 0x0A] — RFC 2083 §3.1 signature. 6 CS_* byte-exact (GRAYSCALE=1/RGB=2/PALETTE=4/GRAYSCALE_ALPHA=9/RGBA=10/ARGB=20). 2 SRC_*, 2 INTERLACE_* (NO=0/ADAM7=1), 2 OUT_MODE_*, 2 PACK_*, 2 ALPHA_SELECT_*, 2 COMMAND_*, 2 DEC_STATUS_*, BUFFER_MODE_LINE=1, 2 SPU_MODE_*. 6 CHUNK_* bit flags (IHDR=1/PLTE=2/IDAT=4/IEND=8/tRNS=16/gAMA=32) para chunk_information queries. `is_valid_bit_depth(cs, depth)` enforce RFC 2083 §11.2.2: GRAYSCALE ∈ {1,2,4,8,16}, PALETTE ∈ {1,2,4,8} (no 16), RGB/RGBA/ARGB/GRAYSCALE_ALPHA ∈ {8,16}. `bytes_per_pixel`: GRAYSCALE/PALETTE=1, GRAYSCALE_ALPHA=2, RGB=3, RGBA/ARGB=4. MAX_WIDTH=MAX_HEIGHT=8192. `InParam` extends JpgDec com bit_depth (8|16), packing mode, alpha_select. OutParam preserva output_bit_depth e dobra width_byte em 16-bit. Invariantes: create zero=ARG, duplo=BUSY, destroy sem create=SEQ, destroy com handles=BUSY, open sem create=SEQ, bad src_select=ARG, FILE sem name=OPEN_FILE, BUFFER ptr=0=ARG, spu_thread_enable fora {0,1}=ARG, read_header sem magic=HEADER, header<8=HEADER, dims zero/oversize=STREAM_FORMAT, color_space unknown=STREAM_FORMAT, bit_depth inválido para CS=STREAM_FORMAT, interlace fora {0,1}=STREAM_FORMAT, chunk_information sem IHDR=STREAM_FORMAT, read_header twice=SEQ, set_parameter bad bit_depth=ARG, bad packing/alpha_select=ARG, decode sem configure=SEQ. full_png_lifecycle_smoke (create 2MB→open file 512x384 RGBA 8bit→read_header valida magic+chunks→set_parameter RGBA→decode→close→destroy) passa. | 40 |

| `rpcs3-hle-celljpgenc` | wave-7 | JPEG encoder HLE (porta `cellJpgEnc.cpp`): `cellJpgEncQueryAttr/Open/Close/EncodePicture/EncodePictureWithAttr/WaitForOutput/Reset`. `JpgEnc` manager + Vec<Handle> (MAX_HANDLES=16). Handle FSM 3 estados (Idle → Encoding → HasOutput → Idle). 10 error codes facility 0x8061_11__ byte-exact (ARG=91/SEQ=92/BUSY=93/EMPTY=94/RESET=95/FATAL=96 + STREAM_ABORT=A1/STREAM_SKIP=A2/STREAM_OVERFLOW=A3/STREAM_FILE_OPEN=A4). 5 CS_* (GRAYSCALE=1/RGB=2/YCBCR=3/RGBA=10/ARGB=20), 5 SAMPLING_* (YCBCR_444=21/422=22/420=23/411=24/FULL=25 — auto-numbered C++ enum), 2 DCT_METHOD_* (QUALITY=0/FAST=5), 2 COMPR_MODE_* (CONSTANT_QUALITY=6/STREAM_SIZE_LIMIT=7), 2 LOCATION_* (FILE=8/BUFFER=9). Limits: MAX_WIDTH=MAX_HEIGHT=4096, QUALITY ∈ 1..=100. `Attr` (max_w/max_h/color_space/sampling/dct_method/compr_mode/quality). `query_attr` returns memory_size (raw + 50% Huffman headroom + 1KB) + enable_au flag (true only for SAMPLING_FULL). `EncodeParam` (input_dims+quality+location+dst_path/buffer). Invariantes: zero/oversize dims=ARG, unknown cs/sampling/dct/compr_mode=ARG, quality fora [1,100]=ARG, >16 handles=FATAL, close durante encoding=BUSY, encode sem Idle=BUSY, FILE sem dst_path=STREAM_FILE_OPEN, BUFFER sem ptr/size=ARG, input>max_w/h=ARG, wait_for_output Idle=EMPTY, wait durante Encoding=BUSY, complete sem Encoding=SEQ, reset em Idle=RESET. encode_with_attr permite swap de attrs entre encodes. full_jpg_enc_lifecycle_smoke (query_attr→open→encode FILE→complete→wait_for_output bytes→encode_with_attr BUFFER quality=90→reset→close) passa. | 38 |

| `rpcs3-hle-cellpngenc` | wave-7 | PNG encoder HLE (porta `cellPngEnc.cpp`): `cellPngEncQueryAttr/Open/OpenEx/Close/EncodePicture/WaitForOutput/Reset`. `PngEnc` manager + Vec<Handle> (MAX_HANDLES=16). Handle FSM 3 estados (Idle → Encoding → HasOutput → Idle). 10 error codes facility 0x8061_12__ byte-exact (ARG=91/SEQ=92/BUSY=93/EMPTY=94/RESET=95/FATAL=96 + STREAM_ABORT=A1/STREAM_SKIP=A2/STREAM_OVERFLOW=A3/STREAM_FILE_OPEN=A4). 6 CS_* byte-exact (GRAYSCALE=1/RGB=2/PALETTE=4/GRAYSCALE_ALPHA=9/RGBA=10/ARGB=20). 10 COMPR_LEVEL_* (0..=9 — zlib compression levels). 5 FILTER_TYPE_* bitmask (NONE=0x08/SUB=0x10/UP=0x20/AVG=0x40/PAETH=0x80 + ALL=0xF8). 17 CHUNK_TYPE_* (PLTE=0/TRNS/CHRM/GAMA/ICCP/SBIT/SRGB/TEXT/BKGD/HIST/PHYS/SPLT/TIME/OFFS/PCAL/SCAL/UNKNOWN=16). 2 LOCATION_* (FILE=0/BUFFER=1). Limits: MAX_WIDTH=MAX_HEIGHT=4096. Config (max_w/max_h/max_bit_depth/enable_spu/add_mem_size). `query_attr` retorna mem_size (raw × 1.5 + add_mem_size + 4KB) + version (0x0001/0x0000) + cmd_queue_depth=4. Picture validate: pitch≥width, bit_depth ∈ {1,2,4,8,16} e ≤config.max_bit_depth. EncodeParam valida color_space + compression_level 0..=9 + filter_type mask ⊆ ALL mas !=0 + ancillary_chunks conhecidos. OutputParam valida FILE needs stream_file_name, BUFFER needs non-zero stream_addr + limit_size (senão STREAM_OVERFLOW). Invariantes: config bad=ARG, >16 handles=FATAL, close durante encoding=BUSY, picture zero/oversize dims ou pitch<width ou bit_depth>max=ARG, null picture_addr=ARG, compression>9=ARG, filter_type=0 ou bits fora 0xF8=ARG, ancillary chunk >16=ARG, FILE sem name=STREAM_FILE_OPEN, BUFFER null/zero limit=ARG/STREAM_OVERFLOW, encode during Encoding=BUSY, wait_for_output Idle=EMPTY, wait durante Encoding=BUSY, complete sem Encoding=SEQ, reset em Idle=RESET. full_png_enc_lifecycle_smoke (query_attr→open→encode FILE→complete→wait_for_output→re-encode BUFFER→reset→close) passa. | 45 |

| `rpcs3-hle-celldmux` | wave-7 | Demuxer framework HLE (porta `cellDmux.cpp`): `cellDmuxQueryAttr/Open/Close/SetStream/ResetStream/EnableEs/DisableEs/ResetEs/ReleaseAu`. `Dmux` manager + Vec<Handle> (MAX_HANDLES=64) cada um com Vec<ElementaryStream> (MAX_ES_PER_DMUX=16). 5 error codes byte-exact facility 0x8061_02__ (ARG=01/SEQ=02/BUSY=03/EMPTY=04/FATAL=05). 5 STREAM_TYPE_* (UNDEF=0/PAMF=1/TERMINATOR=2/MP4=0x81 cellSail/AVI=0x82 cellSail). 3 MSG_TYPE_* (DEMUX_DONE=0/FATAL_ERR=1/PROG_END_CODE=2), 2 ES_MSG_TYPE_* (AU_FOUND=0/FLUSH_DONE=1). `query_attr(stream_type)` retorna mem_size (512KB para PAMF, 1MB para MP4/AVI) + versioning. Handle com stream_type + DmuxState (Idle/StreamSet) + StreamRange + next_es_id. ElementaryStream com id+filter+resource+EsState (Idle/Enabled/Flushing)+pending_aus. EsResource.validate enforce mem_addr!=0 + mem_size>0 + power-of-2 mem_alignment + addr %align == 0. `EsFilterId` (stream_id+private_stream_id+supplemental_info) identifica unique ES — duplicate filter=BUSY. Invariantes: open STREAM_TYPE_UNDEF/TERMINATOR=ARG, unknown=ARG, >64 handles=FATAL, close com active ES=BUSY, set_stream null addr/zero size=ARG, reset_stream volta todos ES para Idle, enable_es bad resource=ARG, duplicate filter=BUSY, 17º ES=FATAL, disable_es com pending_aus=BUSY, inject_au em Idle/Flushing ES=SEQ, release_au empty queue=EMPTY, reset_es seta Flushing + clears AUs. full_dmux_lifecycle_smoke (query_attr PAMF→open→set_stream→3 ES video+audio+user-data→inject 5 AUs cada→release all→reset_stream→disable×3→close) passa. | 34 |

| `rpcs3-hle-cellfs-sdata` | wave-7 | SDATA encrypted-file wrapper HLE (porta 3 entry points de `cellFs.cpp`): `cellFsSdataOpen` + `cellFsSdataOpenByFd` + `cellFsSdataOpenWithVersion` (stub). `cell_fs_sdata_open(flags)` rejeita flags≠0 com EINVAL e retorna `SdataOpenRequest { flags=0, arg1=0x180, arg2=0x10, arg_size=8 }` para o caller delegar a `cellFsOpen` com o header SDATA de 2 palavras big-endian. `cell_fs_sdata_open_by_fd(has_sdata_fd, mself_fd, flags, offset, arg_ptr, arg_size)` valida (!has_sdata_fd=EFAULT, mself_fd∉[3,255]=EBADF, flags≠0=EINVAL) e produz `SdataOpenByFdPlan` com o `lv2_file_op_09` ctrl block populado (vtable1=0xFA88_0000, op=0x8000_0009, vtable2=0xFA88_0020, arg1=0x180, arg2=0x10, arg_ptr, arg_size truncado via u32::try_from) e ctrl_size=0x40 — mirror byte-exato do fcntl(0x80000009, 0x40) do C++. `finish_sdata_open_by_fd(rc, ctrl)` traduz o out do fcntl em `SdataOpenByFdOutcome { FcntlError(rc) \| CtrlError(CellError) \| Opened{sdata_fd} }` preservando a prioridade de checks do C++ (fcntl rc → ctrl.out_code → out_fd). `SdataFdRegistry` helper com next_id=3 + checked_add wraparound + double-close detection (EBADF). Constantes byte-exatas: CELL_FS_O_RDONLY=0, SDATA_HEADER_ARG1=0x180/ARG2=0x10/SIZE=8, LV2_FILE_OP_SDATA_OPEN_BY_FD=0x8000_0009, LV2_FILE_OP_09_SIZE=0x40, MSELF_FD_MIN=3/MAX=255, SDATA_VTABLE1=0xFA88_0000/VTABLE2=0xFA88_0020. full_sdata_lifecycle_smoke (sdata_open→open_by_fd→finish com out_fd=33→registry.register→close) passa. | 36 |

| `rpcs3-hle-celltrophy` | wave-7 | NP Trophy system HLE (porta `sceNpTrophy.cpp`): `sceNpTrophyInit/Term/CreateHandle/DestroyHandle/AbortHandle/CreateContext/DestroyContext/RegisterContext/GetRequiredDiskSpace/SetSoundLevel/GetGameInfo/GetGameProgress/UnlockTrophy/GetTrophyUnlockState/GetTrophyInfo`. `Trophy` manager com is_initialized flag + Vec<Handle> + Vec<Context> (ambos ID_BASE=1, ID_COUNT=4 → range [1,5) byte-exato C++). 25 error codes facility 0x8002_29__ (ALREADY_INITIALIZED=01/NOT_INITIALIZED=02/NOT_SUPPORTED=03/CONTEXT_NOT_REGISTERED=04/OUT_OF_MEMORY=05/INVALID_ARGUMENT=06/EXCEEDS_MAX=07/INSUFFICIENT=09/UNKNOWN_CONTEXT=0A/INVALID_FORMAT=0B/INVALID_GRADE=0D/INVALID_CONTEXT=0E/ABORT=10/UNKNOWN_HANDLE=11/LOCKED=12/HIDDEN=13/CANNOT_UNLOCK_PLATINUM=14/ALREADY_UNLOCKED=15/INVALID_TYPE=16/INVALID_HANDLE=17/INVALID_NP_COMM_ID=18/UNKNOWN_NP_COMM_ID=19/INVALID_TROPHY_ID=20/UNKNOWN_TROPHY_ID=21/UNKNOWN=FF). 5 TrophyGrade (UNKNOWN=0/PLATINUM=1/GOLD=2/SILVER=3/BRONZE=4). 10 TrophyStatus (UNKNOWN=0..CHANGES_DETECTED=9). Constantes: TITLE_MAX_SIZE=128, GAME_DESCR_MAX_SIZE=1024, NAME_MAX_SIZE=128, FLAG_SETSIZE=128, FLAG_BITS_SHIFT=5, FLAG_WORDS=4, INVALID_TROPHY_ID=0xFFFFFFFF, NP_TROPHY_COMM_SIGN_MAGIC=0xB9DDE13B (be_u32 offset 0), NP_TROPHY_COMM_SIGN_VERSION=0x0100 (be_u16 offset 4), SOUND_LEVEL_MIN=20/MAX=100, OPTIONS_CREATE_CONTEXT_READ_ONLY=1. `CommunicationId` ({data:[u8;9], term, num:i32}), `CommunicationSignature` ({data:[u8;160]}) com helper `valid()` byte-exato. `TrophyFlagArray` bitmap 128 bits em 4 × u32 com set/get/popcount. `create_context` valida em ordem exata C++: null sign→INVALID_ARGUMENT, !init→NOT_INITIALIZED, null ctx/commId→INVALID_ARGUMENT, options>1→NOT_SUPPORTED, num>99→INVALID_NP_COMM_ID, magic≠→INVALID_NP_COMM_ID, padding[6..12]≠0→INVALID_NP_COMM_ID, version≠0x0100→INVALID_NP_COMM_ID, 5º handle/context→EXCEEDS_MAX. Context name = title[:9] + `_<num:02d>` (12 bytes). `unlock_trophy` enforce CONTEXT_NOT_REGISTERED→INVALID_TROPHY_ID range→CANNOT_UNLOCK_PLATINUM (gr=1)→ALREADY_UNLOCKED→grava timestamp. `game_progress` exclui platinum do denominador (comment C++ sceNpTrophy.cpp:1380). `get_game_info` count por grade + unlocked. full_trophy_lifecycle_smoke (init→create_handle→create_context READ_ONLY→register 4 trophies (bronze/silver/gold/platinum)→unlock_trophy 0+1→game_info→unlock_state popcount=2→reject_platinum→destroy_context→destroy_handle→term) passa. | 56 |

| `rpcs3-hle-cellspudll` | wave-7 | SPU DLL loader HLE (porta `cellSpudll.cpp` + `.h` — módulo minúsculo, 71 linhas C++). `cellSpudllGetImageSize(psize, so_elf, config)` null-check (NULL_POINTER) + stub CELL_OK matching C++ TODO. `cellSpudllHandleConfigSetDefaultValues(config)` popula firmware defaults: mode=0, dmaTag=0, numMaxReferred=16, numMaxDepend=16, unresolvedSymbolValueFor{Func,Object,Other}=vm::null, zero-fill reserved[9]. 7 error codes facility 0x8041_06__ byte-exact (INVAL=02/SRCH=05/STAT=0F/ALIGN=10/NULL_POINTER=11/UNDEF=12/FATAL=13). `HandleConfig` struct 64 bytes (7 u32 + 9 u32 reserved — size_of confirmado em teste). Constantes DEFAULT_NUM_MAX_REFERRED=16, DEFAULT_NUM_MAX_DEPEND=16, RESERVED_WORDS=9. full_spudll_lifecycle_smoke (set_default_values com junk → verify canonical defaults → get_image_size happy path → null fallbacks = NULL_POINTER) passa. | 19 |

| `rpcs3-hle-cellgifdec` | wave-7 | GIF decoder HLE (porta `cellGifDec.cpp` 676 linhas + `.h` 248). API: Create→Open→ReadHeader→SetParameter→Decode→Close→Destroy. `GifDec` manager com boolean `created` + Vec<GifStream> (MAX_SUBSTREAMS=1023, id_base=1). StreamState FSM 4 estados (Opened → HeaderRead → Configured → Decoded). 8 error codes facility 0x8061_13__ byte-exact (OPEN_FILE=00/STREAM_FORMAT=01/SEQ=02/ARG=03/FATAL=04/SPU_UNSUPPORT=05/SPU_ERROR=06/CB_PARAM=07). Enum ordinals byte-exatos: 2 SRC_* (FILE=0/BUFFER=1), 2 SPU_THREAD_*, 3 RECORD_TYPE_* (IMAGE_DESC=1/EXTENSION=2/TERMINATE=3), 2 COLORSPACE_* (RGBA=10/ARGB=20), 2 COMMAND_* (CONTINUE=0/STOP=1), 2 DEC_STATUS_* (FINISH=0/STOP=1), BUFFER_MODE_LINE=1, 2 SPU_MODE_*, 2 INTERLACE_*. **GIF signature byte-exact RFC-style**: magic=`GIF8` (0x47494638 BE u32 at offset 0..4), trailer `89a`=0x6139 ou `87a`=0x6137 (LE u16 at offset 4..6). `parse_gif_header(buffer[13])` parse completo do Logical Screen Descriptor: SWidth/SHeight em LE u16 (bytes 6-9), packed field byte 10 (bit 7=GCT flag, bits 6-4=(ColorRes-1), bit 3=sort, bits 2-0=(GCT size-1)), background color byte 11, pixel aspect ratio byte 12. `set_parameter` enforce color_space ∈ {RGBA=10, ARGB=20} (senão ARG), computa `outputWidthByte = (SWidth × SColorResolution × 3) / 8` byte-exato C++, `outputComponents = 4`, `outputBitDepth = 0` (unimplemented), `outputColorSpace` = input. `decode(command)` aceita CONTINUE→DEC_STATUS_FINISH, STOP→DEC_STATUS_STOP, outros→CB_PARAM. Invariantes: double create=SEQ, destroy sem create=SEQ, open sem create=SEQ, sub_handle desconhecido=ARG, open FILE name vazio=OPEN_FILE, open FILE size=0 ou BUFFER addr/size=0=ARG, 1024º substream=FATAL, read_header twice=SEQ, header<13 bytes ou magic errado ou trailer invalid=STREAM_FORMAT, set_parameter antes header=SEQ, decode antes Configured=SEQ, close unknown=ARG, destroy limpa todos streams + ids restart. full_gif_lifecycle_smoke (create→open Buffer→read_header 100x50 GIF89a→set_parameter RGBA→outputWidthByte=300→decode CONTINUE→FINISH→close→destroy) passa. | 39 |

| `rpcs3-hle-cellfont-ft` | wave-7 | FreeType font library HLE variant (porta `cellFontFT.cpp` 354 linhas + `.h`). 4 core entry points: cellFontInitLibraryFreeTypeWithRevision (lib_valid null check BEATS config_valid null check — ordem exata C++:12-22), cellFontInitLibraryFreeType (thin wrapper com revision=0), cellFontFTGetRevisionFlags (escreve magic 0x43 em *out, silent no-op em null), cellFontFTGetInitializedRevisionFlags (null check → INVALID_PARAMETER, senão CELL_OK com valor armazenado). 2 error codes byte-exact (`cellFont.h:9-10` INVALID_PARAMETER=0x80540002, UNINITIALIZED=0x80540003). REVISION_FLAGS_MAGIC=0x43 byte-exact cellFontFT.cpp:49. `FontFt` manager aloca LibraryHandle pseudo-address starting at 0x1000_0000 com stride 40 bytes (sizeof CellFontLibrary). STUB_ENTRY_POINTS array com exatamente 39 entry points matching REG_FUNC block em cellFontFT.cpp:306-354 (5 FTCacheStream_* + 27 FTFaceH_* + 7 FTManager_*) — todos retornam CELL_OK via `invoke_stub(name)`. Invoke case-sensitive (match C++ REG_FUNC). LibraryConfigFt + MemoryInterface structs byte-exatos. full_fontft_lifecycle_smoke (get_revision_flags=0x43 → init_library_with_revision(0x43) → get_initialized_revision_flags=0x43 → 7 stub invocations all CELL_OK → null out = INVALID_PARAMETER) passa. | 26 |

| `rpcs3-hle-sys-libc` | wave-7 | **🎉 MARCO 100!** PS3 libc-style HLE primitives (porta `sys_libc.cpp` 43 linhas + `sys_libc_.cpp` 502 linhas). Módulo especial — funções são IMPLEMENTAÇÕES REAIS (não stubs) espelhando libc bit-exato. Cobre 4 entry points sys_libc (`memcpy`, `memset`, `memmove`, `memcmp` com return u32 "C int" semantics) + 20+ entry points sysPrxForUser (`__sys_look_ctype_table`, `_sys_tolower`, `_sys_toupper`, `_sys_memchr`, `_sys_strlen`, `_sys_strcmp`, `_sys_strncmp`, `_sys_strcat`, `_sys_strchr`, `_sys_strncat`, `_sys_strcpy`, `_sys_strncpy`, `_sys_strncasecmp`, `_sys_strrchr`, `_sys_free`). **`CTYPE_TABLE: [i16; 129]` byte-exata** do sys_libc_.cpp:71-87 com sentinel no índice 0 + 128 entries ASCII. Bit layout: 0x01=upper, 0x02=lower, 0x04=digit, 0x08=ctrl, 0x10=punct, 0x20=space/ctrl, 0x40=hex-letter. tolower usa bit 0x01 (add 0x20), toupper usa bit 0x02 (sub 0x20) — ordem crítica herdada do PS3 firmware. `strlen`/`memchr` honram null-pointer shortcuts (retornam 0/None). `strncasecmp` usa o ctype table PS3, não locale C. `memcmp_u32` retorna `u32` byte-exato C int cast (−1 → 0xFFFF_FFFF). full_libc_lifecycle_smoke (memset 0xEE → strcpy "hello" → strlen=5 → strcat "!" → strchr '!'=5 → toupper loop → strcmp "HELLO!" → memcpy backup → memcmp) passa. | 49 |

| `rpcs3-hle-sys-heap` | wave-7 | PS3 user-mode heap + spinlock primitives (porta `sys_heap.cpp` 96 linhas + `sys_spinlock.cpp` 58 linhas). sys_heap: _sys_heap_create_heap/delete_heap/malloc/memalign/free + 4 stubs (alloc_heap_memory/get_mallinfo/get_total_free_size/stats). Registry de 1023 heaps (HEAP_ID_BASE=1, HEAP_ID_COUNT=1023, HEAP_ID_LIMIT=1024) matching HeapInfo::id_count do C++. HeapInfo {id, name, live_allocations} mirror. SysHeap manager com bump allocator (next_addr=0x2000_0000). `heap_malloc(heap, size)` retorna pseudo-address non-zero, incrementa live_allocations. `heap_memalign(heap, align, size)` clampa align ao mínimo 0x10000 (64 KiB — matching std::max<u32>(align, 0x10000)) e alinha next_addr antes de malloc. `heap_free` decrementa saturating_sub (sem underflow). Create full (1024º) retorna 0 sentinel matching idm::make failure. Delete unknown = CELL_OK (match C++ idm::remove void). sys_spinlock: initialize/lock/trylock/unlock com sentinels byte-exatos SPINLOCK_HELD_SENTINEL=0xABAD_CAFE (C++ exchange value), SPINLOCK_FREE_SENTINEL=0. `spinlock_initialize` só escreve se lock≠0 (conditional matching C++ `if(*lock) *lock=0`). `spinlock_trylock` retorna CELL_EBUSY=0x8001_000A quando held. `spinlock_lock_with_cap(max_iters)` busy-wait bounded para testes (C++ original loop infinito + ppu.test_stopped). full_sys_heap_lifecycle_smoke (create 2 heaps → 3 allocs via malloc+memalign 0x20000-aligned → free 1 → live_count drops → delete both → stubs OK). full_spinlock_lifecycle_smoke (initialize garbage → trylock → contend=EBUSY → unlock → lock_with_cap) ambos passam. | 30 |

| `rpcs3-hle-sys-mempool` | wave-7 | PS3 memory pool manager HLE (porta `sys_mempool.cpp` 226 linhas). 6 entry points: `sys_mempool_create/destroy/allocate_block/try_allocate_block/free_block/get_count`. `SysMempool` manager com Vec<MemoryPool>, cada pool com {chunk, chunk_size, block_size, ralignment, free_blocks:Vec<u32>, in_flight:u64}. Registry de 1023 pools (MEMPOOL_ID_BASE=1, MEMPOOL_ID_COUNT=1023 matching C++). `create` valida em ordem exata: block_size > chunk_size=EINVAL, ralignment ∈ {0, 2} → clampado para 4 (DEFAULT_ALIGNMENT byte-exato), alignment não power-of-2 = EINVAL, chunk não 8-aligned = EINVAL (CHUNK_PTR_ALIGN=8 do C++ `chunk.aligned(8)`), table full = EINVAL. Carves chunk em num_blocks=chunk_size/block_size blocos at addr+i*block_size (truncates to u32 matching C++ static_cast<u32>). `allocate_block` retorna `AllocateOutcome { Allocated(u32) \| WouldBlock \| UnknownPool }` enum — C++ original blocca no condvar; nossa versão expõe o estado observável sem bloquear thread. `try_allocate_block` retorna 0 (vm::null) em empty/unknown. `free_block` enforce block ≤ chunk+chunk_size (C++ uses `>` strict — exato boundary `==` é OK). `get_count` retorna u64 (free_blocks.len()), pool desconhecido retorna u64::from(CELL_EINVAL.0) matching C++ cast bug. in_flight counter saturates on underflow. Stack-like LIFO via Vec::pop — pool destroy via swap_remove. full_mempool_lifecycle_smoke (create 8×32 blocks → allocate todos 8 → verify in_flight=8 → try_allocate em empty=0 → allocate em empty=WouldBlock → free 1 → count back up → free past_end=EINVAL → destroy → count retorna EINVAL sentinel) passa. | 32 |

| `rpcs3-hle-libfs-utility` | wave-7 | PS3 filesystem utility init HLE (porta `libfs_utility_init.cpp` 86 linhas). 8 FNID-registered entry points (sem C names — firmware usa só IDs): fn_1F3CD9F1, fn_263172B8 (arg1 tipicamente 0x3), fn_4E949DA4, fn_665DF255, **fn_6B5896B0 (única com observable behavior — escreve *dest=2 partitions)**, fn_A9B04535 (arg1), fn_E7563CE6, fn_F691D443. CELL_EFAULT=0x8001_000D byte-exato. NUM_PARTITIONS=2 byte-exato libfs_utility_init.cpp:46 (`*dest = 2`). REGISTERED_FNIDS array na ordem exata do REG_FNID block cpp:77-84. `fn_6b5896b0(Option<&mut u64>)` modela vm::ptr<u64> — None=null=EFAULT, Some(slot)=escreve 2. `lookup_fnid(u32)` retorna Ok para FNIDs conhecidos, Err(EFAULT) para desconhecidos — helper para cross-check byte-exact. Todos os 8 FNIDs têm teste individual de byte-exactness + no-duplicates check. full_libfs_utility_lifecycle_smoke (todos 8 FNIDs → fn_6b5896b0 com partitions=2 verified → unknown FNID=EFAULT → null dest=EFAULT) passa. | 21 |

| `rpcs3-hle-sys-prx-user` | wave-7 | **🏆 ITER #100!** PS3 PRX loader user-mode helpers (porta `sys_prx_.cpp` 282 linhas). 17 entry points REGISTERED_ENTRY_POINTS (byte-exato ordem REG_FUNC block cpp:264-280): sys_prx_load_module/by_fd/on_memcontainer/on_memcontainer_by_fd/list/list_on_memcontainer/start_module/stop_module/unload_module/register_library/unregister_library/get_module_list/get_module_info/get_module_id_by_name/by_address/exitspawn_with_level/get_my_module_id. CELL_EINVAL=0x8001_0002. OPTION_CMD_PREPARE=1, OPTION_CMD_FINALIZE=2 (FSM do start/stop module). OPTION_ENTRY2_NONE=0xFFFF_FFFF (vm::ptr::set(-1) sign-extended). GET_MODULE_LIST_OPT_INDEX=2 (hardcoded em cpp:201). `StartStopOption` mirror do sys_prx_start_stop_module_option_t com {size, cmd, entry, entry2, res}; `prepare(size)` builder matching C++:101-104 (cmd=1, entry2=sentinel); `finalize(result)` updates cmd=2 + res matching C++:115-116. `EntryDecision` tri-state enum (Entry2/Entry/None) modela cascade de entryx em cpp:19-34 com priority: entry2!=sentinel → Entry2, entry!=0 → Entry, else → None. `GetModuleList` + `GetModuleListOption` mirror pair com build_option/apply_option matching cpp:193-204. 4 validators: start_stop_module (null result=EINVAL), get_module_list (null info ou null idlist=EINVAL), get_module_info (null info=EINVAL), get_module_id_by_name (flags!=0 OR pOpt!=0=EINVAL). convert_path_list widens u32→u64 matching C++ convert_path_list helper. full_sys_prx_lifecycle_smoke (start_module null→EINVAL → valid → opt.prepare(64) → entryx com entry=0x1234_0000 → Entry decision → finalize(42) → get_module_list null idlist=EINVAL → valid→apply_option → id_by_name flags=1=EINVAL → exitspawn OK) passa. | 33 |

| `rpcs3-hle-sys-lwmutex-user` | wave-7 | PS3 lightweight mutex user-mode HLE (porta `sys_lwmutex_.cpp` 420 linhas). 5 entry points: sys_lwmutex_create/destroy/lock/trylock/unlock. LwMutexAttr.validate enforce exatamente sys_lwmutex_.cpp:18-34: recursive ∈ {SYS_SYNC_RECURSIVE=0x10, SYS_SYNC_NOT_RECURSIVE=0x20} senão EINVAL, protocol ∈ {FIFO=0x01, RETRY=0x04, PRIORITY=0x02} senão EINVAL. internal_protocol helper mapeia protocol != FIFO → PRIORITY byte-exato cpp:38. `LwMutex` mirror do sys_lwmutex_t com {owner, attribute = recursive|protocol, recursive_count, sleep_queue, all_info_waiters}. Sentinels byte-exatos sys_lwmutex.h:21-23: LWMUTEX_FREE=0xFFFFFFFF, LWMUTEX_DEAD=0xFFFFFFFE, LWMUTEX_RESERVED=0xFFFFFFFD. `lock` retorna LockOutcome { Acquired \| WouldSleep \| Error(CellError) } tri-state — fast CAS free→tid, recursive branch com EDEADLK/EKRESOURCE, dead=EINVAL, contention=WouldSleep (incrementa waiters). finish_sleep_acquire pós-syscall valida `old_owner == LWMUTEX_RESERVED` matching fmt::throw_exception do C++:199-202. `trylock` espelha lock sem waiters bump — contention retorna EBUSY. `unlock` retorna UnlockOutcome { Released \| NeedsSyscall{reserved:bool} } — non-owner=EPERM, recursive_count>0 decrements, sem waiters=free, SYS_SYNC_RETRY path libera owner direto, normal path escreve LWMUTEX_RESERVED. `destroy` EBUSY se tid==owner matching cpp:68-71 (recursive self-destroy prevention), senão marca LWMUTEX_DEAD. 7 CellErrors byte-exatos: EINVAL/EPERM/ESRCH/EBUSY/ETIMEDOUT/EDEADLK/EKRESOURCE. full_lwmutex_lifecycle_smoke (create recursive → lock tid=42 → lock 42 again (recursive_count=1) → lock 99=WouldSleep (waiters=1) → unlock 42 recursion → unlock 42 (reserved path) → finish_sleep_acquire 99 (waiters=0) → unlock 99 free → destroy → dead → lock=EINVAL) passa. | 37 |

| `rpcs3-hle-sys-lwcond-user` | wave-7 | PS3 lightweight condition variable user-mode HLE (porta `sys_lwcond_.cpp` 392 linhas). Depende de `rpcs3-hle-sys-lwmutex-user` (primeira inter-crate dependência fora emu-types). 6 entry points: sys_lwcond_create/destroy/signal/signal_all/signal_to/wait. SYS_SYNC_ATTR_PROTOCOL_MASK=0xF byte-exato sys_sync.h:23. SignalOutcome tri-state enum { SyscallDirect{mode=2} \| SyscallWithOwner{mode=1} \| SyscallAfterLock{mode=3} } modela decisão cpp:57-96: SYS_SYNC_RETRY path (mode 2 direct), caller owns mutex (mode 1 + all_info++), trylock success (reserva owner + mode 3), trylock EBUSY (mode 2 direct), outros trylock errors (ESRCH). signal_all usa mesma shape mas mode 1 para caller-owns e trylock-success — sem bump pre-syscall (count vem do syscall return). signal_to adiciona target_tid no outcome struct SignalToOutcome. wait_prepare valida `owner == tid` senão EPERM (cpp:287-290), salva recursive_count em WaitState, marca owner=LWMUTEX_RESERVED + recursive=0. wait_finish split em 4 paths observáveis matching cpp:312-370: Ok → restore + all_info-- (Woken), ESRCH → restore + bubble ESRCH, EBUSY/ETIMEDOUT → NeedsRelock outcome (caller driva sys_lwmutex_lock + finish_relock), EDEADLK → swap + restore → maps to ETIMEDOUT (cpp:356-368 documented recovery). finish_relock aplica saved recursive_count + retorna OK se EBUSY mapped, senão ETIMEDOUT. Invariant check: old_owner == FREE ou DEAD em restore = EINVAL (throw_exception no C++:323-326). full_lwcond_lifecycle_smoke (create → thread 42 lock+prepare_wait(recursive=2 saved, owner=RESERVED) → signal path → wait_finish Ok → owner=42 restored, recursive=2 restored, waiters=0 → destroy=DEAD) + full_lwcond_timeout_flow_smoke (prepare → Err(ETIMEDOUT) → NeedsRelock{mapped_ok=false, saved=5} → sim relock → finish_relock → ETIMEDOUT bubbled, recursive=5 restored) ambos passam. | 28 |

| `rpcs3-hle-sys-mmapper-user` | wave-7 | PS3 memory mapper user-mode HLE (porta `sys_mmapper_.cpp` 50 linhas — módulo mais compacto). 5 entry points: allocate_memory/allocate_memory_from_container/map_memory/unmap_memory/free_memory. **SYS_MMAPPER_NO_SHM_KEY=0xFFFF_0000_0000_0000** byte-exato sys_mmapper.h:49 — sentinel injetado para allocate_memory anônimo (sem SHM key explícita). 3 page-size constants byte-exatos sys_memory.h:29-31: PAGE_SIZE_4K=0x100, PAGE_SIZE_64K=0x200, PAGE_SIZE_1M=0x400, PAGE_SIZE_MASK=0xF00. `flags_to_page_size` helper mapeia flags & MASK → {1M → 0x100000, 64K → 0x10000, outros → None} matching sys_memory.cpp:124-127. SysMmapperUser manager com Vec<SharedMem> + bump id allocator start 1. AllocateSharedRequest/AllocateSharedFromContainerRequest structs capturam args forwarded para underlying syscall (expõe SHM-key injection para testes). allocate_memory valida null mem_id ptr=EFAULT, bad page flags=EINVAL, zero size=EINVAL, non-page-aligned size=EINVAL. allocate_memory_from_container adiciona CELL_ESRCH para container_id=0. map_memory enforce mem_id unknown=ESRCH + conflict (outro mem_id já mapeado no addr)=EINVAL. unmap_memory retorna original mem_id matching C++ signature (vm::ptr<u32> mem_id out param). free_memory enforce unknown=ESRCH + still-mapped=EINVAL (caller leaked mapping). 4 CellErrors byte-exatos: EINVAL=0x8001_0002, ESRCH=0x8001_0005, EFAULT=0x8001_000D, ENOMEM=0x8001_0004. full_mmapper_lifecycle_smoke (alloc 64K anon + alloc 1M from container 5 → verify NO_SHM_KEY injected em ambos → map both → unmap anon → free anon OK → free cont=EINVAL (still mapped) → unmap cont → free cont OK → empty) passa. | 30 |

| `rpcs3-hle-sys-ppu-thread-user` | wave-7 | **🎉 MARCO 3000 TESTS!** PS3 PPU thread user-mode HLE (porta `sys_ppu_thread_.cpp` 288 linhas). 8 entry points: sys_initialize_tls/sys_ppu_thread_create/get_id/exit/once/register_atexit/unregister_atexit/sys_interrupt_thread_disestablish. **TlsPool** byte-exato modela TLS state global cpp:17-23: image_addr/size/zero_size/slot_size=tls_mem_size+0x30/area_start=pool+0x30/max_slots=(0x40000-0x30)/slot_size + slot_used bitmap + alt_live fallback. Constants byte-exatos: TLS_SYSTEM_AREA_SIZE=0x30 (cpp:81), TLS_POOL_BYTES=0x40000 (cpp:82), TLS_GPR13_OFFSET=0x7030 main thread (cpp:87), CHILD_TLS_GPR13_OFFSET=0x7030 child (cpp:140), MAX_ATEXIT_HANDLERS=8 (cpp:12). alloc_slot prefer unused small slot → fallback alt bump allocator (models vm::alloc). free_slot handle ambos small-slot (offset check % slot_size) e alt-slot paths matching cpp:56-61 alternative TLS detection. **AtexitRegistry** fixed 8-slot array mirror cpp:192-234: register EPERM se duplicate, ENOMEM se cheio; unregister ESRCH se desconhecido; handlers preserve registration order; re-register após unregister fills gap. **once_control** (OnceOutcome::Run/AlreadyDone enum) enforce: if *ctrl==ONCE_INIT(0) → write DONE_INIT(1)+Run, else AlreadyDone sem overwrite. SYS_PPU_THREAD_CREATE_INTERRUPT=0x2 flag determina needs_start=false (caller skip sys_ppu_thread_start) via CreateThreadPlan struct. ExitPlan retorna atexit_list in order + tls_freed bool. 4 CellErrors byte-exatos (EINVAL/EPERM/ENOMEM/ESRCH). full_ppu_thread_lifecycle_smoke (initialize TLS → spawn 3 workers → register 2 atexit → dup register EPERM → once first Run + second AlreadyDone → w2.exit emit atexit list order [5001,5002] + TLS freed → unregister → interrupt_disestablish w3) passa. 2 fixes mid-iter: testes tls_alloc_exhaust + tls_free_alt assumiam max_slots reservas mas initialize já consome slot 0 — ajustei para `remaining = max_slots - live_small_count()`. | 34 |

| `rpcs3-hle-sys-spu-user` | wave-7 | PS3 SPU user-mode HLE (porta `sys_spu_.cpp` 502 linhas — um dos maiores user-mode wrappers). 12 entry points: sys_spu_elf_get_information/get_segments, sys_spu_image_import, sys_spu_image_close, sys_raw_spu_load, sys_raw_spu_image_load, _sys_spu_printf_{initialize,finalize,attach_group,detach_group,attach_thread,detach_thread}. 5 CellErrors byte-exatos (EINVAL/ENOMEM/ENOENT/ENOEXEC=0x8001_0008/ESTAT=0x8001_0009). Magic bytes byte-exatos: **SCE_MAGIC=0x53434500** ("SCE\\0"), **ELF_MAGIC=0x7F454C46** ("\\x7FELF"), SCE_HVER_EXPECTED=2 (cpp:142), SCE_TYPE_SELF=1, SELF_HTYPE_EXPECTED=3 (cpp:151), ELF_DATA_BE=2 (cpp:160), ELF_MACHINE_SPU=0x17. **Raw SPU constants byte-exatos** SPUThread.h:167-177: RAW_SPU_BASE_ADDR=0xE0000000, RAW_SPU_OFFSET=0x100000, RAW_SPU_PROB_OFFSET=0x40000, SPU_NPC_OFFS=0x4034. 4 image type constants (USER=0/KERNEL=1/PROTECT=0/DIRECT=1). SpuElfInfo::init reproduz cascade EXATO cpp:127-179 — null src=EINVAL, SCE present → valida se_magic OR hver OR type OR meta OR htype OR elfoff OR phdroff senão ENOEXEC, ELF header → magic + data_BE + class ∈ {1,2} senão ENOEXEC. sys_spu_elf_get_information rejeita SCE wrapper (cpp:194-198) + valida e_machine==SPU + e_phnum>0. SpuImage::import 2-path: PROTECT path força todos phdrs serem p_type ∈ {1, 4} (LOAD/INFO), retorna KERNEL image sem segs; DIRECT path popula USER image com segment table via fill_segments. get_nsegs + fill_segments counters +1 para p_type==1 ou 4 matching C++ sys_spu_image::{get_nsegs,fill}. close USER clears segs, KERNEL noop, outro=EINVAL. raw_spu_address(id) = BASE + OFFSET*id computation. raw_spu_npc_address(id) = raw_spu + PROB + NPC. SpuPrintfCallbacks com 4 handlers (attach_group/detach_group/attach_thread/detach_thread) — initialize popula, finalize clears; qualquer chamada sem callback registrado = CELL_ESTAT. full_spu_lifecycle_smoke (parse SPU ELF → info(entry=0x1000, nseg=2) → extract segments → DIRECT import → raw_spu_address(2)=0xE020_0000 → npc(2)=0xE024_4034 → printf callbacks initialize+attach_group OK → finalize → attach_group=ESTAT → close USER image) passa. | 45 |

| `rpcs3-hle-sys-net-user` | wave-7 | PS3 BSD-sockets user-mode HLE (porta `sys_net_.cpp` 716 linhas — maior stub registry). Módulo enorme (~95 entry points) mas quase tudo stub. **REGISTERED_ENTRY_POINTS array com exatamente 95 entradas** ordem cpp:610-714: 30 BSD FNIDs (accept/bind/connect/listen/recv/send/socket + inet_*), 28 sys_net/_sys_net utility funcs, 22 _sys_net_lib_* LV2 callbacks (malloc/calloc/realloc/free/rand/ioctl/thread_*/sync_*/sysctl/usleep), 8 sys_netset_*, 7 _sce_net_*. is_registered case-sensitive + no-duplicates invariant. INET_ADDR_NONE=0xFFFFFFFF byte-exato cpp:65 (inet_addr stub always returns). TLS offsets byte-exatos: TLS_BASE_OFFSET=0x7030 (subtração comum), ERRNO_TLS_OFFSET=0x2C (cpp:296 errno_loc), H_ERRNO_TLS_OFFSET=0x28 (cpp:373 h_errno_loc) — diferem exatamente 4 bytes. errno_loc + h_errno_loc formulas byte-exatas `gpr[13] - 0x7030 + offset`. inet_addr_stub preserva firmware behavior (CELL_OK com 0xFFFFFFFF); inet_addr_parse é real parser dotted-quad rejeitando overflow/short/long/non-decimal e convertendo para network byte order via to_be(). SocketState FSM 5 estados (Created → Bound → Listening → Connected → Closed). NetUser manager com Vec<Socket> + fd bump start em 3 (preserva stdin/stdout/stderr). Socket struct {fd, family, sock_type, protocol, state, local, peer, listen_backlog}. SockAddr mirror {family, port, addr}. bind transiciona Created→Bound, listen requer Bound, connect rejeita Listening/Closed com EINVAL, close force-transition. full_net_lifecycle_smoke (socket AF_INET server+client fds 3/4 → bind+listen server → connect client → inet_addr_parse "192.168.1.1" válido → inet_addr_stub=0xFFFFFFFF → errno_loc 0x1000_7030 → close both → connect closed=EINVAL) passa. | 36 |

| `rpcs3-hle-sys-io-user` | wave-7 | PS3 I/O subsystem user-mode HLE (porta `sys_io_.cpp` 253 linhas). 8 entry points `sys_config_*`: start/stop/add_service_listener/remove_service_listener/register_io_error_handler/unregister_io_error_handler/register_service/unregister_service. **LibIoSysConfig** mirror do fxo singleton cpp:16-31 com reference counter init_ctr + queue_id + ppu_id + 3 registries (service_listeners, services (name,handler), error_handlers). Constants byte-exatos: CONFIG_EVENT_QUEUE_DEPTH=0x20 (cpp:164), CONFIG_THREAD_PRIORITY=512 (cpp:168 prio=512), CONFIG_THREAD_STACK_SIZE=0x2000 (cpp:168 stacksize=0x2000), CONFIG_THREAD_NAME="_cfg_evt_hndlr" (cpp:157), EVENT_KIND_PAD_NOTIFY=1 (cpp:77 arg1==1 branch). **StartOutcome** (FirstStart{queue_id, ppu_id} / AlreadyStarted{init_ctr}) + **StopOutcome** (LastStop{queue_id, ppu_id} / StillActive{init_ctr} / NotStarted) enums modelam refcount lifecycle EXATO matching cpp:152 `if (cfg.init_ctr++ == 0)` + cpp:185 `if (cfg.init_ctr && cfg.init_ctr-- == 1)`. send_connect_event drop se init_ctr=0 senão enqueue ConfigEvent {source, arg1=1, arg2=index, arg3=state} matching cpp:139 port->send(0, 1, index, state). should_dispatch_pad helper dispatch EVENT_KIND_PAD_NOTIFY matching cpp:77-89. 3 registries com duplicate EINVAL + unknown unregister EINVAL. start_stop_roundtrip verified: 3x start → stop→StillActive(2) → stop→StillActive(1) → stop→LastStop. full_sys_io_lifecycle_smoke (start 0x4000/0x5000 → register "pad"+"mouse" services → send_connect_event enqueues → dispatch_pad=true → register+unregister handler → start 2nd=AlreadyStarted → stop=StillActive → stop=LastStop → send_connect_event drop → stop=NotStarted) passa. | 30 |

| `rpcs3-hle-sys-rsxaudio-user` | wave-7 | PS3 RSX audio subsystem user-mode HLE (porta `sys_rsxaudio_.cpp` 72 linhas). 9 entry points todos UNIMPLEMENTED_FUNC no C++ — Rust port adiciona observable lifecycle FSM. **RsxAudioState** enum 5 estados: Uninitialized → Initialized → ConnectionOpen → Prepared → Running. RsxAudio manager com state + shm_imported flag. initialize requires Uninitialized, finalize requires !Running && !Prepared, create_connection requires Initialized, close_connection requires ConnectionOpen/Prepared (auto-clears shm), import_shared_memory requires ConnectionOpen + !imported, unimport requires imported + !Running/Prepared, prepare_process requires ConnectionOpen + imported, start_process requires Prepared, stop_process requires Running (volta para Prepared). 9 REGISTERED_ENTRY_POINTS em ordem alfabética byte-exato REG_FUNC block cpp:62-70. full_rsxaudio_lifecycle_smoke (init → create → import → prepare → start (Running) → stop (Prepared) → close (Initialized + shm cleared) → finalize (Uninitialized)) passa. | 28 |

| `rpcs3-hle-sys-game-user` | wave-7 | PS3 sys_game user-mode HLE (porta `sys_game_.cpp` 216 linhas). 10 entry points: sys_game_process_exitspawn/exitspawn2 (complex size calc + flag transform) + 8 stubs (board_storage_read/write + get_rtc_status + get_system_sw_version + get_temperature + watchdog_start/stop/clear). Constants byte-exatos: EXIT2_PARAM_X0=0x85 (cpp:117 arg->x0=0x85), EXIT2_HEADER_SIZE=0x30 (cpp:118 this_size=0x30), STRING_ALIGN=0x10 (per-string alignment `(len + 0x10) & -0x10`), EXITSPAWN_ARG_BUDGET=0x1000 (cpp:85 drop-args threshold), EXIT2_DATA_RESERVE=0x1030 (cpp:98 data_size>0 extra), EXIT2_MAGIC=0x1000_0000 (cpp:137 4th arg de _sys_process_exit2). **string_array_size** byte-exato cpp:14-31 — inicial 8, cada string `((len + 0x10) & -0x10) + 8`. **exitspawn_size** byte-exato cpp:33-48 — arg_count starts at 1 (path), env_count starts at 0, path: `((strlen + 0x10) & -0x10) + 8`, +argv/envp sizes, +8 pad se (arg+env) % 2 != 0. **ExitspawnPlan** retorna alloc_size + next_size + arg_count + env_count + transformed_flags + args_dropped + calls_atexitspawn (flags>>62==0) + calls_at_exitspawn (flags>>62==1). **exitspawn flag transform** cpp:144: `(flags & 0xf0) | (1ull << 63)` — top-2=2 → no at* callback. **exitspawn2 flag transform** cpp:151: if (flags>>62)>=2 → flags & 0xf0, else → flags & 0xc0000000000000f0 — preserve high 2 bits quando não atingem threshold. Budget overflow path: alloc > 0x1000 → drop argv+envp + recompute. data_size > 0 adiciona 0x1030 ao final. SysGame manager com watchdog_running/expired state — watchdog_start sets running=true + clears expired, watchdog_clear só clears expired. 10 REGISTERED_ENTRY_POINTS em ordem C++ cpp:205-214 (exitspawn2 vem ANTES de exitspawn matching REG_FUNC order). full_sys_game_lifecycle_smoke (exitspawn /eboot.bin --help LANG=C data=0x200 → plan.arg=2, env=1, !dropped → exitspawn2 com 0x4000...f7 keeps 0xc0 bits → at_exitspawn=true → watchdog start→expired→clear→stop → stubs OK) passa. | 32 |

| `rpcs3-hle-sys-crashdump` | wave-7 | PS3 crash-dump user-log-area HLE (porta `sys_crashdump.cpp` 36 linhas + `.h` 16 linhas — módulo minúsculo). 2 entry points: sys_crash_dump_get_user_log_area, sys_crash_dump_set_user_log_area. Constants byte-exatos: SYS_CRASH_DUMP_MAX_LABEL_SIZE=16, SYS_CRASH_DUMP_MAX_LOG_AREA=127, NUM_LOG_AREAS=128 (indices válidos 0..=127). LogAreaInfo struct mirror do sys_crash_dump_log_area_info_t com label:[u8;16] (15 chars + NUL) + addr + size. new("label", addr, size) truncate para 15 bytes + sempre NUL-terminate. label_str() strip NUL para read-back. CrashDump manager com fixed-size [LogAreaInfo; 128] array. get valida index > 127 (index is u8, so 128..=255 trigger) OR !entry_valid = EINVAL. set mesma validação. Overwrite permitido no mesmo slot. populated_count helper retorna non-empty slots (size != 0). 2 REGISTERED_ENTRY_POINTS em ordem alfabética byte-exato REG_FUNC block. full_crashdump_lifecycle_smoke (3 areas populated "audio"/"graphics"/"netlog" → roundtrip readback com label_str matches → populated_count=3 → index 200=EINVAL → null entry=EINVAL → overwrite slot 0 "AUDIO2") passa. | 27 |

| `rpcs3-hle-sys-lv2dbg` | wave-7 | PS3 LV2 debugger-interface HLE (porta `sys_lv2dbg.cpp` 316 linhas). 35 entry points registered + **47 error codes byte-exatos** facility 0x8001_04__ (range contíguo 0x80010401..=0x8001042F): INVALIDPROCESSID=01/INVALIDTHREADID=02/ILLEGALREGISTERTYPE=03/…/ILLEGALCOREDUMPPARAMETER=2F. REGISTERED_ENTRY_POINTS array 35 entradas em ordem exata REG_FUNC block cpp:278-314. **Lv2Dbg** manager com observable state: DABR (Data Address Breakpoint Register) = dabr_addr + dabr_ctrl_flag (set/get_address_to/from_dabr); MAT (Memory Access Trace) conditions Vec<(addr, cond)> com upsert semantics (set overwrites existing addr); PPU exception handler lifecycle com ppu_exc_initialized + ppu_exc_handler + ppu_exc_priority/stack_size/mask/mask_flags. initialize_ppu_exception_handler enforce !initialized senão DEHANDLERALREADYINITIALIZED=0x80010429, finalize requires initialized senão DEHANDLERNOTINITIALIZED=0x8001042D. register/unregister handler enforce complementary — DEHANDLERALREADYREGISTERED vs DEHANDLENOTREGISTERED=0x8001042B. mat_set_condition upsert matching addr. All 47 error codes tested for distinct via sort+windows + contiguous range check (0x2F - 0x01 + 1 = 47). full_lv2dbg_lifecycle_smoke (set_stacksize → init_ppu_handler prio=512 → register handler 0x4000_0000 → arm DABR → set 2 MAT conditions → set_mask → double-register=EALREADYREGISTERED → unregister + re-register → finalize → double-finalize=ENOTINITIALIZED) passa. | 28 |

| `rpcs3-hle-static-hle` | wave-7 | PS3 pattern-matching static HLE (porta `StaticHLE.cpp` 179 linhas). Sistema que reconhece libc PS3 memcpy/memset/memmove/memcmp via assinatura binária dos primeiros 32 bytes + CRC16 dos bytes seguintes, então patches com 4 instruções PPU (LIS+ORI+MTCTR+BCTR) que saltam para HLE helper. **9 patterns byte-exatos** do shle_patterns_list cpp:14-25 em RAW_PATTERNS array — 2 memcpy + 3 memset + 1 memmove + 3 memcmp. POLY=0x8408 byte-exato cpp:100 #define POLY. START_PATTERN_BYTES=32 (hex_len=64). WILDCARD_NIBBLE=0xFFFF (cpp:67-68 retorna FFFF se `..`). Hex parser com `hex_byte(c1, c2)`: ambos `.` → WILDCARD, um só → None (erro), outros → valor. parse_start_pattern 64-char hex → [u16; 32] com wildcards. **gen_crc16 byte-exata** cpp:102-126: seed 0xFFFF, poly 0x8408, loop 8 bits/byte, invert + byte-swap halves no final. compile_patterns compila 9 rows para Vec<ShlePattern>. check_against_patterns reproduz cpp:128-178: skip short data < total_length, start_pattern 32-byte match (wildcards pass), CRC16 check dos bytes [32..32+crc16_length], return match. **PPU instruction builders** const fn: ppu_lis (0x3C00_0000 | imm), ppu_ori (0x6000_0000 | …), ppu_mtctr (0x7C0903A6 | rs<<21), ppu_bctr=0x4E800420 byte-exatos. emit_stub(target) produz [u32;4] que patches guest memory: LIS r0, target>>16 + ORI r0, r0, target&0xFFFF + MTCTR r0 + BCTR. full_static_hle_lifecycle_smoke (compile_patterns 9 rows → build synthetic buf com pattern[0] start bytes + zero pad até total_length=0x5C4 → CRC16 check rejects all-zero middle → emit_stub(0x0001_0000) produz [0x3C00_0001, 0x6000_0000, 0x7C09_03A6, 0x4E80_0420]) passa. | 35 |

| `rpcs3-hle-hle-patches` | wave-7 | RPCS3 per-game HLE fixups (porta `HLE_PATCHES.cpp` 57 linhas). Único entry point: WaitForSPUsToEmptySNRs — workaround para race condition em Sonic the Hedgehog onde PPU→SPU signalling races causam missing graphics quando SNR (Signal Notification Register) é sobrescrito com valor não-vazio. MODULE_NAME="RPCS3_HLE_LIBRARY" byte-exato cpp:54. SPU_ID_ALL=0xFFFFFFFF (umax matching C++). Masks: SNR_MASK_SNR1=0b01 (cpp:29), SNR_MASK_SNR2=0b10 (cpp:35), SNR_MASK_RELEVANT=0b11. Early return EXATO cpp:17-20: (1) spu_id != umax && SPU não achado → SpuNotFound, (2) snr_mask % 4 == 0 (ambos bits 0 e 1 zero) → MaskIsEmpty, (3) senão WouldSpin{all_spus=spu_id==umax}. SpuSnrState.has_busy preserves cpp:29-38 — checa SNR1 if mask&1 && count!=0, SNR2 if mask&2 && count!=0. SpuGroup com find + any_busy para SPU_ID_ALL path. wait_for_spus_to_empty_snrs aceita max_iters cap (C++ spins indefinitely) + drain_per_iter flag para testes determinísticos — C++ yield para scheduler, nosso port simula drain. WaitOutcome enum (SpuNotFound/MaskIsEmpty/WouldSpin{all_spus:bool}) + WaitResult enum (ReturnedEarly/Drained). 1 fix mid-iter: smoke test assumia que drain por SPU paralelo esvaziaria SPU 0 junto com SPU 1 — na verdade, como spu_id=1 é específico, loop para assim que SPU 1 draina (max(5,3)=5 iters), SPU 0 fica com SNR1=0 (saturated) e SNR2=1 remaining. Ajustei asserção. full_hle_patches_lifecycle_smoke (sonic-style wait para SPU 1 com mask RELEVANT → decision=WouldSpin{all_spus=false} → drain 5 iters → SPU 1 drained → SPU 0 partial → mask=0 early return preserva counts) passa. | 25 |

| `rpcs3-hle-libmedi` | wave-7 | PS3 Mediator telemetry library HLE (porta `libmedi.cpp` 79 linhas). 10 entry points: cellMediator{CreateContext/CloseContext/GetStatus/GetProviderUrl/GetUserInfo/GetSignatureLength/Sign/PostReports/ReliablePostReports/FlushCache}. MediatorState FSM 3 estados (Uninitialized → Open → Closed — sem re-open) matching library-singleton lifecycle. Mediator manager com state + 3 counters (reports_posted/reliable_reports_posted/cache_flushed_count). create_context enforce !Uninitialized=EINVAL, close_context requires Open, cannot reopen after close. Ops gate via require_open: sign/get_provider_url/get_user_info/get_signature_length/post_reports/reliable_post_reports/flush_cache todos EINVAL se não-Open. get_status exception: retorna state sem guard (matching C++ stub sempre CELL_OK). MEDIATOR_SIGNATURE_LENGTH=256 (PS3 RSA signature size). get_provider_url retorna "https://mediator.ps3.sony.net/" const. REGISTERED_ENTRY_POINTS array 10 entradas em ordem alfabética byte-exato REG_FUNC cpp:69-78. full_libmedi_lifecycle_smoke (pre-open sign=EINVAL, get_status OK → create_context → provider_url/user_info/sig_len OK → sign/post x1/reliable_post x2/flush x1 → close → post_reports=EINVAL → counters preserved 1/2/1) passa. | 25 |

| `rpcs3-hle-libmixer` | wave-7 | PS3 surround mixer + SSPlayer HLE (porta `libmixer.cpp` 719 linhas). 27 entry points (3 AAN + 14 SurMixer + 7 SSPlayer + 3 Util). 8 error codes facility 0x8031_00__ byte-exatos (NOT_INITIALIZED=02/INVALID_PARAMATER=03/NO_MEMORY=05/ALREADY_EXIST=06/FULL=07/NOT_EXIST=08/TYPE_MISMATCH=09/NOT_FOUND=0A — note typo "PARAMATER" preserved). 9 SSPlayer constants byte-exatos (ONESHOT=0/ONESHOT_CONT=2/LOOP_ON=16/STATE_ERROR=0xFFFFFFFF/STATE_NOTREADY=0x88888888/STATE_OFF=0x00/STATE_PAUSE=0x01/STATE_CLOSING=0x08/STATE_ON=0x20). MIX_SAMPLES_PER_BLOCK=256, SURROUND_CHANNELS=8. **SsPlayer** struct com 13 fields incluindo position/speed/level runtime. **LibMixer** manager com Vec<SsPlayer> + SurMixerState FSM (Uninit/Created/Running/Paused) + Vec<(u32,u32,u32,u32)> AAN edges + mix_count + notify_callbacks. ss_player_create valida outputMode=0 + channels-1 < 2 (rejeita 0 via wrapping sub). Offsets loop_start e position decremented by 1 matching cpp:254/256. set_wave sem common_present=fallback ONESHOT. aan_connect marca source.connected=true + adiciona edge; disconnect só desmarca se não há outras edges ao mesmo source. SurMixer lifecycle Create→Start→{Pause↔Running}→Finalize. pause type 0=pause Running→Paused, outros=unpause Paused→Running. notify_callbacks set/remove com ALREADY_EXIST/NOT_EXIST. **Util math helpers REAIS** (C++ original retorna 0 fatal, aqui implementamos): level_from_db via 10^(db/20) = exp(db/20 × ln(10)), note_to_ratio via 2^((note-ref)/12) = exp(diff/12 × ln(2)) — libm_exp Taylor series local com scaling /8 +^8 para precisão em [-20, +20] dB range. level_from_db(0)=1.0, level_from_db(20)=10.0, level_from_db(-20)=0.1 verified < 0.01 tolerance. note_to_ratio(60, 72)=2.0 (octava up), (60, 48)=0.5 (octava down). 27 REGISTERED_ENTRY_POINTS em ordem REG_FUNC cpp:687-718 (AAN×3 → SurMixer×14 → SSPlayer×7 → Util×3). full_libmixer_lifecycle_smoke (sur_mixer Create+set_notify+Start → 2 SSPlayers set_wave+connect+Play → state=ON → pause+unpause → stop+disconnect+remove player a → state=ERROR → util math verified → Finalize) passa. | 41 |

| `rpcs3-hle-libsnd3` | wave-7 | PS3 Sound Player 3 + MIDI/SMF HLE (porta `libsnd3.cpp` 390 linhas). 47 entry points (init/exit, setOutputMode, synthesis, 4 bind/unbind/noteOn variants, 10 Voice ops, 16 SMF playback). 16 error codes facility 0x8031_03__ byte-exatos range contíguo (PARAM=01/CREATE_MUTEX=02/SYNTH=03/ALREADY=04/NOTINIT=05/SMFFULL=06/HD3ID=07/SMF=08/SMFCTX=09/FORMAT=0A/SMFID=0B/SOUNDDATAFULL=0C/VOICENUM=0D/RESERVEDVOICE=0E/REQUESTQUEFULL=0F/OUTPUTMODE=10). Snd3 manager com state (Uninit/Initialized) + Vec<Voice> (max_voice alocado up-front) + Vec<SoundData> (HD3 registry) + Vec<Smf> (SMF registry) + next_key_on_id auto-incrementing + effect state + output_mode. VoiceState FSM 4 estados (Idle → Playing → SustainHold → Releasing). Voice com 12 fields (pitch i32, velocity, panpot, panpot_ex, pitch_bend, envelope, reserve_mode, sustain_hold, key_on_id, midi_channel, state). SoundData mirror HD3 com {hd3_id, synth_mem_offset}. Smf com {smf_id, hd3_id, play_status:SmfStatus (Stopped/Playing/Paused), tempo, play_velocity, play_panpot, play_panpot_ex, play_channel_bit=0xFFFF default (all 16 channels), key_on_ids:[u32;16]}. MAX_VOICES=128, MAX_HD3=64, MAX_SMF=16, MIDI_CHANNELS=16. init valida max_voice ∈ (0, 128]=PARAM, double init=ALREADY, pre-allocates voices array. set_output_mode enforce mode ≤ 1 else OUTPUTMODE. bind_sound_data full=SOUNDDATAFULL, unbind unknown=HD3ID. note_on_by_tone requer HD3 bound, auto-incrementa next_key_on_id. voice_set_* 7 helpers validam voice_num < max_voice via get_voice_mut (VOICENUM em OOB). voice_note_on_by_tone transiciona state=Playing + armazena key_on_id. voice_key_off → Releasing. voice_all_key_off releases todas !Idle. SMF transport: play Stopped/Paused/Playing→Playing, pause requires Playing senão SMFCTX, resume requires Paused senão SMFCTX, stop → Stopped (any state). smf_add_tempo wrapping_add. require_init guard em todas init-requiring ops (NOTINIT). full_libsnd3_lifecycle_smoke (init 16 voices → set_output_mode 1 → set_effect → bind HD3 + SMF → play 127/64/1 → pause/resume/add_tempo 120 → 2 voices note_on + velocity/panpot/pitch → voice_key_off 0 + all_key_off → stop+unbind SMF → unbind HD3 → exit) passa. | 40 |

| `rpcs3-hle-libsynth2` | wave-7 | PS3 Sound Synth 2 (SPU-2 style) HLE (porta `libsynth2.cpp` 145 linhas). 17 entry points: Config/Init/Exit lifecycle, Set/Get {Param:u16, Switch:u32, Addr:u32}, SetCoreAttr, SetEffect{Attr, Mode}, Generate, VoiceTrans/VoiceTransStatus, Note2Pitch/Pitch2Note. 3 error codes byte-exatos facility 0x8031_02__ (FATAL=01/INVALID_PARAMETER=02/ALREADY_INITIALIZED=03). SynthState FSM 2-state. SoundSynth2 manager com init_flag, config_slots Vec<(i16,i32)>, core0+core1 RegisterBank (params/switches/addrs/core_attrs Vec<(reg, value)> com upsert semantics), effect_attrs/modes Vec<(bus, EffectAttr)>, voice_transfers Vec<VoiceTransfer>. VOICES_PER_CORE=24, NUM_CORES=2, TOTAL_VOICES=48 (SPU-2 standard). VOICE_TRANS_SYNC=0/ASYNC=1. voice_trans valida channel ∈ [0, 48) else INVALID_PARAMETER. Sync mode → VoiceTransState::Done imediato, Async → InProgress → first poll transiciona para Done. generate requires Initialized (FATAL senão), retorna samples count. **Note2Pitch byte-exato formula** (C++ stub retorna 0 mas port implementa real PS2 SPU-2): pitch = 4096 × 2^((note + fine/128 - center_note - center_fine/128) / 12) clamped [0, 0xFFFF]. Pitch2Note inverse: note = center + 12 × log2(pitch / 4096). Fine-tuning em unidades de cents × 128 (128 ticks = 1 semitone). libm_pow2 + libm_log2 local helpers via Taylor series. note2pitch(60,0,60,0)=0x1000 (1:1), (60,0,72,0)=0x2000 (octava up), (60,0,48,0)=0x800 (octava down), pitch2note roundtrips. 1 compile fix mid-iter: `0x1000_f32` não é válido Rust literal (hex-float não supported), trocado para `4096.0` decimal. full_libsynth2_lifecycle_smoke (config+init → set_param/switch/addr/core_attr/effect → 2 voice_trans (async+sync) → voice_trans_status both Done → generate 256 → verify all register reads → note2pitch(60,0,72,0)=0x2000 → exit) passa. | 34 |

| `rpcs3-hle-cellpesmutility` | wave-7 | PS3 PESM movie-recording encryption HLE (porta `cellPesmUtility.cpp` 113 linhas). 15 entry points todos UNIMPLEMENTED no C++ — Rust port adiciona FSM + sample counters. **PesmState** 6-state com total ordering via Ord impl: Uninitialized < Initialized < DeviceOpen < Loaded < Prepared < Recording. Pesm manager com init_version flag (0=V1, 1=V2 via init_entry2, u32::MAX=finalized2), samples_encrypted/samples2_encrypted counters (V1 vs V2 paths distintos), sinf_available boolean (só true após LoadAsync), load_tokens refcount (LoadAsync bumps, UnloadAsync decrements — at 0 volta para DeviceOpen). finalize2 deixa sentinel init_version=u32::MAX para tests distinguirem V1/V2 paths. open_device enforce Initialized only. close_device aceita DeviceOpen/Loaded/Prepared mas rejeita Recording (=EINVAL). load_async requer ≥ DeviceOpen. encrypt_sample requires Recording state; encrypt_sample2 separate counter para V2 path. get_sinf requires sinf_available (LoadAsync-gated). prepare_rec: Loaded → Prepared. start_movie_rec: Prepared → Recording. end_movie_rec: Recording → Prepared (pode re-iniciar). REGISTERED_ENTRY_POINTS array 15 entradas em ordem REG_FUNC block cpp:98-112. full_pesm_lifecycle_smoke (init → init_entry2 (V2) → open_device → 2x load_async → sinf OK → prepare_rec → start_movie_rec → 3x encrypt_sample + 2x encrypt_sample2 → end_movie_rec → 2x unload_async → DeviceOpen → close_device → finalize) passa. | 30 |

| `rpcs3-hle-celldaisy` | wave-7 | PS3 Daisy lock-free queue + SPU interlock HLE (porta `cellDaisy.cpp` 223 linhas). 23 logical entry points (43 FNID entries total — cada nome registered twice com prefixes `_ZN` e `_QN` no C++ ABI mangling). 8 error codes byte-exatos facility 0x8041_05__ NON-CONTIGUOUS (NO_BEGIN=01/INVALID_PORT_ATTACH=02/NOT_IMPLEMENTED=03/PERM=09/STAT=0F/AGAIN=11/INVAL=12/BUSY=1A — com gaps). 3 families: (1) **LFQueue2** lock-free producer/consumer: push_open/close + pop_open/close com ref counting, get_pop_pointer + complete_pop_pointer, has_unfinished_consumer. push_close/pop_close underflow=PERM. get_pop_pointer NO_BEGIN se never opened, AGAIN non-blocking empty, BUSY blocking empty. complete_pop_pointer valida pointer == head senão INVAL. (2) **Lock** MPMC ring: initialize(depth) com INVAL(0)/BUSY(double-init), push/pop_open/close com require_init=NO_BEGIN guard, get_next_head_pointer AGAIN empty, get_next_tail_pointer AGAIN full (via wrapping_sub), complete_produce/consume valida pointer match. (3) **ScatterGatherInterlock** 4-state FSM (Uninit→Armed→Probed→Released): ctor_variant1/variant2 (2 C++ overloads distintos), destruct reset to default, probe non-blocking Armed=AGAIN + blocking→Probed + already-Probed idempotent + Uninit/Released=STAT, release requires Probed→Released, proceed_sequence_number no-error counter bump. CPP_FNID_COUNT=43 reflects each logical name registered twice. full_celldaisy_lifecycle_smoke (LFQueue2 push/pop open+inject_push+complete+close → Lock initialize+push_open+get_next_tail+complete_produce+get_next_head+complete_consume → ScatterGatherInterlock ctor_variant2+probe(1)+proceed_seq+release+destruct) passa. | 35 |

| `rpcs3-hle-cellauthdialog` | wave-7 | **🏆 MARCO 120 ITERS!** PS3 authentication dialog utility HLE (porta `cellAuthDialog.cpp` 66 linhas — um dos menores módulos). 3 entry points: cellAuthDialogOpen/Abort/Close. 3 error codes byte-exatos contíguos facility 0x8002_D2__ (UNKNOWN_201=01/ARG1_IS_ZERO=02/UNKNOWN_203=03 — nomes preservados do C++ que tem comentário "All error codes are unknown at this point in implementation"). MODULE_NAME="cellAuthDialogUtility" byte-exato cpp:61. AuthDialogState FSM 4-state (Idle → Open → Aborted/Closed — terminal states). AuthDialog manager com state + last_arg1 captured + rejected_opens counter (conta tentativas de open com arg1=0). open(arg1) enforce `arg1 != 0` senão ARG1_IS_ZERO (byte-exato cpp:35 check). C++ source documenta que "arg1 is s64 but the check is for == 0 instead of >= 0" — port preserva isso: valores com sign bit aceitos (e.g., 0x8000_0000_0000_0000 válido). abort/close rejeitam Idle com UNKNOWN_203 (firmware commented-out guard em cpp:46/56 mirrored). Terminal states não permitem re-abort/re-close. reset() permite reopen após Aborted/Closed. 3 REGISTERED_ENTRY_POINTS em ordem exata REG_FUNC cpp:63-65. full_authdialog_lifecycle_smoke (open 0=ARG1_IS_ZERO rejected_counter=1 → open 0xDEAD_BEEF OK → abort → Aborted state → subsequent abort/close=UNKNOWN_203 → reset → reopen 0xCAFE → close → Closed) passa. | 21 |

| `rpcs3-hle-cellprint` | wave-7 | PS3 printer utility HLE (porta `cellPrint.cpp` 165 linhas). 14 entry points: cellSysutilPrint{Init,Shutdown} + cellPrint{LoadAsync,LoadAsync2,UnloadAsync,GetStatus,OpenConfig,GetPrintableArea,StartJob,EndJob,CancelJob,StartPage,EndPage,SendBand}. 8 error codes byte-exatos contíguos facility 0x8002_C4__ (INTERNAL=01/NO_MEMORY=02/PRINTER_NOT_FOUND=03/INVALID_PARAM=04/INVALID_FUNCTION=05/NOT_SUPPORT=06/OCCURRED=07/CANCELED_BY_PRINTER=08). 3 color format constants (RGB=0, GRAYSCALE=1, RGBA=2). MODULE_NAME="cellPrintUtility" byte-exato cpp:148. PrintState FSM 6-state (Uninitialized → Idle → Loaded → JobActive → PageActive → Cancelled). Print manager com state + load_param Option<LoadParam {mode}> + current_job_total_pages + current_color_format + counters (pages_started/finished, bands_sent, total_bytes_sent) + async_callbacks_pending Vec<AsyncCallback>. AsyncCallback captures function+userdata+result+CallbackKind enum (LoadAsync/LoadAsync2/UnloadAsync/OpenConfig — kinds distintos para tests). drain_callbacks FIFO consumption via core::mem::take. start_job valida total_page > 0 + color_format ∈ {0,1,2} (INVALID_PARAM) + requires Loaded. start_page rejeita após pages_started >= total_pages. send_band accumulates bytes, validates size >= 0. cancel_job aceita JobActive ou PageActive. end_job aceita JobActive OR Cancelled. DEFAULT_PRINTABLE_WIDTH=5100, DEFAULT_PRINTABLE_HEIGHT=6600 (8.5"×11" @ 600 DPI). get_status retorna PrintStatus mirror com state-encoded status value (-1..4) + continue_enabled=0 se Cancelled. full_cellprint_lifecycle_smoke (init → load_async queue cb → open_config queue cb → start_job 2-page RGB → page 1 com 3 bands (1024+2048+4096=7168 bytes) → end_page → page 2 com 1 band → end_job → Loaded → unload_async → shutdown) passa. | 33 |

| `rpcs3-hle-cellresc` | wave-7 | PS3 video rescaling HLE (porta `cellResc.cpp` 410 linhas). 19 entry points: cellResc{Init,Exit,SetSrc,SetDsts,SetDisplayMode,AdjustAspectRatio,SetPalInterpolateDropFlexRatio,ConvertAndFlip,SetRegisterCount,GetRegisterCount,SetWaitFlip,ResetFlipStatus,GetFlipStatus,SetFlipHandler,SetVBlankHandler,GetNumColorBuffers,CreateInterlaceTable,SetBufferAddress,VideoOutResolutionIdToBufferMode}. 8 error codes byte-exatos contíguos facility 0x8021_03__ (NOT_INITIALIZED=01/INVALID_ARG_VAL=02/NOT_ENOUGH_MEMORY=03/BAD_ALIGN=04/BAD_ARGUMENT=05/BAD_COMBINATION=06/REINITIALIZED=07/CELL_OK_UNUSED=08). MAX_INIT_CONFIG_SIZE=28. 4 buffer modes (480i/576i=0x1, 720p=0x2, 1080i=0x4, 1920x1080=0x8). PAL temporal modes 1..=5 (DEFAULT, INTERPOLATE, INTERPOLATE_30, DROP, FOR_HSYNC). Flip modes VSYNC=0/HSYNC=1. PAL + flip mode compatibility matrix byte-exata cpp:125-139: PAL_INTERPOLATE modes+HSYNC=BAD_COMBINATION, PAL_DROP+HSYNC=BAD_COMBINATION, PAL_FOR_HSYNC+VSYNC=BAD_COMBINATION. get_num_color_buffers formula: 6 (INTERPOLATE modes), 3 (DROP), 2 (FOR_HSYNC/non-PAL). 8 src slots [0..=7]. pal_flex_ratio clamped 0.0..=1.0. flip counter + status (0=pending, 1=done). flip/vblank handlers registered (0 clears to None). interlace table validation (null ea + length<=0 + src_h<=0 = BAD_ARGUMENT). full_cellresc_lifecycle_smoke (init → resolution_id→buffer_mode lookup → set_dsts 720p → set_display_mode → adjust_aspect_ratio → set_src slot 0 → convert_and_flip → flip_status pending → set_wait_flip → reset_flip_status → register_count roundtrip → interlace_table → set_flip_handler → set_vblank_handler → exit) passa. | 41 |

| `rpcs3-hle-libad-core` | wave-7 | PS3 sceAd core middleware HLE (porta `libad_core.cpp` 57 linhas — módulo stub no C++: todas as 7 entries retornam CELL_OK após UNIMPLEMENTED_FUNC). 7 entry points em ordem exata REG_FUNC cpp:50-56: sceAdOpenContext, sceAdFlushReports, sceAdGetAssetInfo, sceAdCloseContext, sceAdGetSpaceInfo, sceAdGetConnectionInfo, sceAdConnectContext. MODULE_NAME="libad_core" byte-exato cpp:4/48. 6 error codes internos placeholder facility `0x8002_E1__` (NOT_INITIALIZED=01, ALREADY_OPEN=02, NOT_CONNECTED=03, ALREADY_CONNECTED=04, CONTEXT_CLOSED=05, INVALID_ASSET=06) — preservam happy-path=CELL_OK do C++ mas fazem enforcement FSM do lado Rust. AdContextState FSM 4-state (Uninitialized → Open → Connected → Closed — Closed terminal). AdCore manager: state + assets Vec<AdAssetInfo {id/w/h/duration}> + spaces Vec<AdSpaceInfo {id/slot_count}> + connection {bytes_sent/received/round_trips} + reports Vec<AdReport {asset_id/event/timestamp}> + counters (open/close/connect/flush). open_context rejeita double-open (ALREADY_OPEN), post-close (CONTEXT_CLOSED). connect_context requer Open (NOT_INITIALIZED se Uninit, ALREADY_CONNECTED se já conectado). close_context aceita Open/Connected, double-close=CONTEXT_CLOSED. flush_reports requer Connected, drena Vec via core::mem::take. get_asset_info/get_space_info requer ≥Open, lookup por id (INVALID_ASSET se missing). get_connection_info requer Connected, snapshot de traffic counters. inject_asset/space/enqueue_report/record_traffic harness helpers (não expostos como FNIDs). full_libad_core_lifecycle_smoke (open → inject 2 assets/1 space → asset lookup Open-only → connect → record_traffic 1024/256 → 2 reports → flush drena ambos → close → todos rejected CONTEXT_CLOSED) passa. | 28 |

| `rpcs3-hle-libad-async` | wave-7 | PS3 sceAd async middleware HLE (porta `libad_async.cpp` 51 linhas — módulo stub no C++: 6 entries retornam CELL_OK após UNIMPLEMENTED_FUNC). Contraparte assíncrona do libad_core. 6 entry points em ordem exata REG_FUNC cpp:44-49: sceAdAsyncOpenContext, sceAdAsyncConnectContext, sceAdAsyncSpaceOpen, sceAdAsyncFlushReports, sceAdAsyncSpaceClose, sceAdAsyncCloseContext. MODULE_NAME="libad_async" byte-exato cpp:4/42. 8 error codes internos placeholder facility 0x8002_E2__ (NOT_INITIALIZED=01, ALREADY_OPEN=02, NOT_CONNECTED=03, ALREADY_CONNECTED=04, CONTEXT_CLOSED=05, SPACE_NOT_FOUND=06, SPACE_ALREADY_OPEN=07, SPACE_NOT_OPEN=08). AdAsyncContextState FSM 4-state (Uninit → Open → Connected → Closed). Cada call aloca request_id monotônico e enfileira AdAsyncCallback {kind, request_id, result, userdata}. AdAsyncCallbackKind enum: OpenContext/ConnectContext/SpaceOpen/FlushReports/SpaceClose/CloseContext. AdAsync manager: state + open_spaces Vec<u32> + pending_callbacks Vec + reports Vec + next_request_id (saturating_add) + 6 counters per-entry. space_open valida connected + duplicate (SPACE_ALREADY_OPEN). space_close valida initialized + presença (SPACE_NOT_OPEN). close_context clears all open_spaces implicitly (matches SDK: apenas 1 callback CloseContext em vez de N SpaceClose callbacks). drain_callbacks via core::mem::take preserva FIFO. flush_reports drena reports queue + enfileira callback + retorna (request_id, reports) como tupla. full_libad_async_lifecycle_smoke (open 0x1001 → connect 0x1002 → space_open 1000+2000 → 2 reports → flush 0x3000 → space_close 1000 → close_context drops remaining → drain yields 7 cbs em ordem monotônica de request_id → all results=CELL_OK → post-close rejeita 6 surfaces com CONTEXT_CLOSED) passa. | 26 |

| `rpcs3-hle-cellmusicdecode` | wave-7 | PS3 music decode utility HLE (porta `cellMusicDecode.cpp` 678 linhas — maior módulo desta wave). 20 entry points em ordem exata REG_FUNC cpp:656-676 (10 legacy cellMusicDecode* + 10 mirror cellMusicDecode*2). MODULE_NAME="cellMusicDecodeUtility" byte-exato cpp:654. 14 error codes byte-exatos facility 0x8002_C1__ (CANCELED=0x0001, DECODE_FINISHED=C101..NO_MORE_CONTENT=C108, DIALOG_OPEN/CLOSE=C109/A, NO_LPCM_DATA=C10B, NEXT_CONTENTS_READY=C10C, ERROR_GENERIC=C1FF). 8 event constants (STATUS_NOTIFICATION=0..NEXT_CONTENTS_READY_RESULT=7). 4 commands (STOP=0..PREV=3). 2 status (DORMANT=0, DECODING=1). 5 positions (NONE=0..END_LIST_END=4). 2 decode2 speeds (MAX=0, SPEED_2=2). MIN_BUFFER_SIZE=458752 byte-exato. is_valid_spu_priority [0x10..=0xFF] preserva u32-underflow `(spuPriority - 0x10U > 0xef)` de cpp:291. is_valid_spu_usage_rate [1..=100] preserva `(spuUsageRate - 1U > 99)` cpp:314. MusicSelectionContext com playlist/current_track/hash + advance_next/prev. DeferredCallback {event, result, variant} mirror do sysutil_register_cb. MusicDecode manager v1/v2 com full state (status/command/read_pos/decoder_size/speed/buf_size/data/context/has_decode_error/track_fully_decoded/timestamps VecDeque/pending_callbacks). initialize rejects mode!=0, priority fora range, !func. initialize_system_workload substitui priority por usage_rate + spurs + priority_ptr. initialize2 adiciona bufSize>=MIN + speed∈{MAX,2}. set_decode_command preserve cpp inner FSM: STOP→DORMANT+read_pos=0, START→DECODING+read_pos=0, NEXT/PREV validate playlist end (NO_MORE_CONTENT → DORMANT). read implementa posição cpp:203-225 (START read_pos==0, MID !track_fully_decoded||size_left>req, END advance OK, END_LIST_END advance umax). Timestamp VecDeque avança quando read_pos >= timestamps_ms[1].0. POSITION_END_LIST_END reseta state. POSITION_END só reseta read_pos. full_cellmusicdecode_lifecycle_smoke (initialize2 → select_contents playlist → START → inject LPCM + timestamp → read POSITION_START → get_contents_id → finalize → drain 4 callbacks em ordem INITIALIZE/SELECT/COMMAND/FINALIZE) passa. | 39 |

| `rpcs3-hle-cellcrosscontroller` | wave-7 | PS3 cross-controller / PS Vita companion HLE (porta `cellCrossController.cpp` 192 linhas). 2 entry points: cellCrossControllerInitialize (visible REG_FUNC) + finish_callback (REG_HIDDEN_FUNC — msg-dialog cancel hook). 14 error codes byte-exatos **NON-CONTIGUOUS** facility 0x8002_CD__: CANCEL/NETWORK (80/81) + 11 cluster 90..9A (OUT_OF_MEMORY/FATAL/INVALID_PKG/SIG/ICON_FILENAME/VALUE/PKG/SIG/ICON_FILE_OPEN/INVALID_STATE/INVALID_PKG_FILE) + INTERNAL=A0. Gap structure byte-exato preserved (0x82..0x8F + 0x9B..0x9F). MODULE_NAME="cellCrossController" byte-exato cpp:185. Status byte-exato: INITIALIZED=1, FINALIZED=2. String caps cellCrossController.h:27-34: APP_VER_LEN=6, TITLE_ID_LEN=10, TITLE_LEN=52, PARAM_FILE_NAME_LEN=255. 6 CellMsgDialog button sentinels. CrossControllerPhase FSM 3-state (Uninitialized → Initialized → Finalized — Finalized é terminal, no re-init). CellCrossControllerParam (package/signature/icon file names Option<String>) + CellCrossControllerPackageInfo (title/title_id/app_ver). fits_in_cap reproduz `memchr(ptr, '\0', LEN+1)` semantics. Validation cascade byte-exato cpp:142-181: (1) state==INITIALIZED=INVALID_STATE, (2) null param OR pkg_info=INVALID_VALUE, (3) package variants produzem INVALID_PKG/SIG/ICON_FILENAME em ordem, (4) pkg_info fields null/over cap OR missing cb=INVALID_VALUE. finish_callback (cpp:115-124) preserva `ensure()` as Result: non-ESCAPE button=INVALID_STATE, null cb OR state!=Initialized=INVALID_STATE, happy path queues FINALIZED+CANCEL + transita Finalized. deliver_connection_result test hook modela cpp:49-60 connection-established path com CellError customizável. CrossControllerCallback {status, error_code, userdata}. drain_callbacks via core::mem::take preserva FIFO. full_crosscontroller_lifecycle_smoke (init 0x8000_0000 callback + 0xFEEDFACE userdata → queues INITIALIZED+OK → deliver_connection_result OK → FINALIZED+OK → drain 2 cbs ambos userdata=0xFEEDFACE → re-init+re-cancel=INVALID_STATE) passa. | 27 |

| `rpcs3-hle-celldtcpiputility` | wave-7 | PS3 DTCP-IP copy-protection HLE (porta `cellDtcpIpUtility.cpp` 100 linhas — módulo stub: todas 13 entries retornam CELL_OK após UNIMPLEMENTED_FUNC). 13 entry points em ordem exata REG_FUNC cpp:86-98: Read, Finalize, Activate, Open, CheckActivation, Initialize, GetDecryptedData, StopSequence, Seek, StartSequence, SetEncryptedData, Close, SuspendActivationForDebug. MODULE_NAME="cellDtcpIpUtility" byte-exato cpp:4/84. 12 error codes internos placeholder facility 0x8002_D0__ (NOT_INITIALIZED=01, REINITIALIZED=02, NOT_ACTIVATED=03, ALREADY_ACTIVATED=04, NOT_OPEN=05, ALREADY_OPEN=06, NOT_IN_SEQUENCE=07, ALREADY_IN_SEQUENCE=08, INVALID_PARAMETER=09, NO_DATA=0A, SUSPENDED=0B, FINALIZED=0C) — preservam happy-path=CELL_OK do C++ mas fazem enforcement FSM 3-dimensional do lado Rust. **FSM 3-eixo**: ModuleState (Uninit/Initialized/Finalized), ActivationState (NotActivated/Activated/Suspended), SessionState (Closed/Open/InSequence). DtcpIp manager com 13 counters per-entry + encrypted/decrypted Vec<u8> + read_pos. Lifecycle: Initialize → Activate → Open → StartSequence → SetEncryptedData → Read/Seek/GetDecrypted → StopSequence → Close → Finalize. Finalize é terminal (no re-init, matching sceAd precedent). SuspendActivationForDebug tears down sessions + marks Suspended. resume_activation Rust helper (firmware drops back via license round-trip). require_initialized/require_activated/require_open/require_in_sequence guards encadeadas preservam ordem cascade. SetEncryptedData com stub-level identity transform (encrypted==decrypted) permite testar read/seek cursor arithmetic sem real decryptor. read retorna ReadOutcome {bytes_read, remaining}, EOF=NO_DATA. seek past end=INVALID_PARAMETER. full_dtcpip_lifecycle_smoke (Init→Activate→CheckActivation=true→Open→StartSequence→2 SetEncryptedData total 8 bytes→read 3→Seek 5→read 3 remaining=0→StopSequence→Close→Finalize→11 counters matching dispatch trace) passa. | 33 |

| `rpcs3-hle-cellmusicselectioncontext` | wave-7 | PS3 music selection context helper (porta `cellMusicSelectionContext.cpp` 372 linhas — C++ documentado cpp:8 como "just a helper and not a real cell entity", sem PRX entries). Consumido por cellMusic/cellMusicDecode para representar a playlist ativa. Constants byte-exatos: CELL_MUSIC_SELECTION_CONTEXT_SIZE=2048, MAGIC=[b'S',b'U',b'S',0] (cellMusic.h:145 char magic[4]="SUS" auto-nul-terminado), MAX_DEPTH=2 (cellMusic.h:154), TARGET_FILE_TYPE="Music Playlist" (cpp:155), TARGET_VERSION="1.0" (cpp:156), UMAX=u32::MAX sentinel. 3 mirror enums com from_u32 parsers: CellSearchContentType (None=0..Scene=7, 8 values), CellSearchRepeatMode (None=0/Repeat1=1/All=2/NoRepeat1=3), CellSearchContextOption (None=0/Shuffle=1). CellMusicSelectionContext wire struct [u8; 2048] com repr(C) + custom Debug/PartialEq. MusicSelectionContext mirror com 9 campos (valid/magic/hash/content_type/repeat_mode/context_option/first_track/current_track/playlist) + shuffle_triggered counter. set(wire) valida MAGIC prefix cpp:14-17 + extrai hash até NUL terminator + UTF-8 validation. get() serializa back cpp:25-41 (oversize hash=None em vez do C++ fmt::throw_exception). set_playlist com content_type auto: 1 entry=Music, >1=MusicList (matches cpp:95/111). set_track cpp:258-279 com ends_with matching (firmware host-path termina em stored /dev_hdd0/music/xxx). **step_track byte-exato cpp:281-371 para 4 repeat modes**: NONE advance OR umax-at-boundary, REPEAT1 stay, ALL modular wrap, NOREPEAT1 terminal umax. **Shuffle+All+>1 tracks** triggers reshuffle quando wrap (next && current==0 OR !next && current==len-1) cpp:359-367 — Rust expõe pluggable shuffler callback para determinism em tests + shuffle_triggered counter. get_next_hash AtomicU64 threadsafe (C++ usa local static + counter). context_to_hex " %.2x" format byte-exato cpp:68. to_string_header mirror cpp:43-46. impl From<&ctx> for bool (operator bool cellMusic.h:172-175). full_music_selection_context_lifecycle_smoke (3 tracks Shuffle+All → set_track ends_with → get wire + peer.set roundtrip → step forward 1→2→0 triggers shuffle 1x → NoRepeat1 mode termina umax) passa. | 34 |

| `rpcs3-hle-cellgameexec` | wave-7 | PS3 game exec + PlayStation Home launch HLE (porta `cellGameExec.cpp` 148 linhas). 10 entry points em ordem exata REG_FUNC cpp:138-147: cellGameSetExitParam, cellGameGetHomeDataExportPath, cellGameGetHomePath, cellGameGetHomeDataImportPath, cellGameGetHomeLaunchOptionPath, cellGameExecGame, cellGameDeleteGame, cellGameGetBootGameInfo, cellGameGetExitGameInfo, cellGameGetList. MODULE_NAME="cellGameExec" byte-exato cpp:136. 3 error codes byte-exatos imported from cellGame.h: ERROR_PARAM=0x8002_CB07, ERROR_NOAPP=0x8002_CB08, CELL_HDDGAME_ERROR_INTERNAL=0x8002_BA03. Constants: CELL_GAME_DIRNAME_SIZE=32, GAMETYPE_DISC=1, GAMETYPE_HDD=2. CellGameGameType enum com as_u32/from_u32 roundtrip. BootGameInfo output struct {game_type, dir_name:Option<String>, exec_data:Option<u32>}. **2 real-work entries** + 8 stubs. set_exit_param stores u32 execdata para next boot-chained game. get_boot_game_info valida null type/dir_name (PARAM), resolve source type (default=HDD quando no injection), on HDD copia boot_dir se < CELL_GAME_DIRNAME_SIZE (>=32=HDDGAME_ERROR_INTERNAL speculative cpp:113-115). PlayStation Home paths: get_home_data_export_path e import_path sempre NOAPP (Home defunct), get_home_path sempre OK (TODO), get_home_launch_option_path ambos required ou PARAM depois NOAPP. GameExec manager com 6 counters + execdata + boot_source Option + boot_dir. set_boot_source Rust-helper para inject Emu state. full_gameexec_lifecycle_smoke (HDD boot "NPJA00001" + execdata 0x0BAD_F00D → boot_info retorna HDD+dir+exec_data → Home hooks NOAPP/OK variants → exec_game+delete_game+get_exit_game_info+get_list stubs OK) passa. | 24 |

| `rpcs3-hle-cellsheap` | wave-7 | PS3 shared heap + key-addressed sync primitives HLE (porta `cellSheap.cpp` 162 linhas — stub: todas 18 entries retornam CELL_OK). 18 entry points em ordem REG_FUNC cpp:142-161 = 5 core heap (Initialize/Allocate/Free/QueryMax/QueryFree) + 13 KeySheap (Initialize + 6 pair New/Delete para Buffer/Mutex/Barrier/Semaphore/Rwm/Queue). MODULE_NAME="cellSheap" byte-exato cpp:4/140. 4 error codes byte-exatos facility 0x8041_03__ cpp:7-13: INVAL=02, BUSY=0A, ALIGN=10, SHORTAGE=12. KeySheapKind enum (Buffer/Mutex/Barrier/Semaphore/Rwm/Queue). KeySheapObject {key, kind, size, alignment}. SheapAllocation {addr u32, size, alignment}. SheapState FSM 2-state Uninit → Initialized (separate para core + key). is_power_of_two helper `n != 0 && (n & (n-1)) == 0`. Bump allocator mirror 0x4000_0000 base: allocate alinha bump + checked_add overflow prevention → SHORTAGE se end>capacity. free via swap_remove (non-stack order tolerated). Core FSM: initialize(0)=INVAL, double-init=BUSY. Key FSM: init preservada + 6 kind-tagged New/Delete com key-uniqueness check (duplicate=BUSY) + kind-match check (key+wrong_kind=INVAL). Buffer/Queue requer size>0 (outros kinds accept size=0). Alignment validation: 0 → default 1, non-power-of-2 → ALIGN, power-of-2 aligns bump cursor. full_sheap_lifecycle_smoke (core init 0x10000 → 3 allocs varying alignment 16/64/128 verifica alignment preservado → free b non-stack order → key_init + mutex+barrier+buffer new → query_max/free → delete all + free remaining → counters: 1 init + 3 alloc + 3 free + 1 query_max + 1 query_free + 1 key_init) passa. | 25 |

| `rpcs3-hle-cellrtcalarm` | wave-7 | PS3 RTC alarm helper HLE (porta `cellRtcAlarm.cpp` 43 linhas — stub: 5 entries CELL_OK). 5 entry points em ordem REG_FUNC cpp:38-42: Register, Unregister, Notification, StopRunning, GetStatus. MODULE_NAME="cellRtcAlarm" byte-exato cpp:4/36. 5 error codes internos placeholder facility 0x8001_07__ (NOT_REGISTERED=01, ALREADY_REGISTERED=02, NOT_RUNNING=03, ALREADY_RUNNING=04, INVALID_PARAMETER=05) — preservam happy-path=CELL_OK do C++ + enforcement FSM Rust-side. AlarmState FSM 3-state (Unregistered → Registered → Running → Registered → Unregistered). AlarmState::as_status_code retorna 0/1/2 para GetStatus. RtcAlarm manager com handler_addr + fire_time_utc_us + last_notification_us + notification_count + 5 per-entry counters. register: handler=0=INVALID_PARAMETER, state!=Unregistered=ALREADY_REGISTERED. unregister: aceita qualquer state live (firmware tolerates emergency teardown from Running), Unregistered=NOT_REGISTERED. notification: Unregistered=NOT_REGISTERED, Running=ALREADY_RUNNING, Registered→Running + bump counter + record timestamp. stop_running: Unregistered=NOT_REGISTERED, Registered=NOT_RUNNING, Running→Registered. get_status retorna u32 code. full_rtcalarm_lifecycle_smoke (register 0x8000_0000 T+60s → notification@60M Registered→Running → stop_running → re-fire@120M notification_count=2 → unregister from Running tolerated → teardown → counters) passa. | 21 |

| `rpcs3-hle-cellremoteplay` | wave-7 | PS3 Remote Play / PSP companion HLE (porta `cellRemotePlay.cpp` 86 linhas — stubs `todo()` com 1 real-work em GetComparativeVolume). 8 entry points em ordem REG_FUNC cpp:78-85: GetStatus, SetComparativeVolume, GetPeerInfo, GetSharedMemory, EncryptAllData, StopPeerVideoOut, GetComparativeVolume, Break. MODULE_NAME="cellRemotePlay" byte-exato cpp:5/76. 1 error code byte-exato `cellRemotePlay.h`: INTERNAL=0x8002_9830 (único code commitado). 6 status constants byte-exatos: LOADING=0/WAIT=1/RUNNING=2/UNLOADING=3/FATALERROR=4/PREMOEND=5. DEFAULT_COMPARATIVE_VOLUME=1.0 matching cpp:63. RemotePlayStatus enum com as_u32/from_u32 roundtrip. PeerInfo mirror {nickname/psp_ver/link_state}. RemotePlay manager com status + Option<comparative_volume> + Option<PeerInfo> + shared_memory {addr, size} + encrypt_enabled + break_requested + 8 per-entry counters. 3 test hooks (inject_peer_info, inject_shared_memory, set_status) para state injection. get_status: FatalError=INTERNAL, outros retornam stored. set_comparative_volume: NaN=INTERNAL, outros stored. get_comparative_volume null ptr tolerado (cpp:61 firmware branch). encrypt_all_data + stop_peer_video_out + request_break todos idempotentes. full_remoteplay_lifecycle_smoke (status Loading→Wait→Running → inject_peer vita1+psp_ver→inject_SHM 0x5000_0000/1MiB → encrypt → volume default 1.0 → set 0.75 → stop_peer_video_out + request_break → status Unloading) passa. | 22 |

| `rpcs3-hle-cellnetaoi` | wave-7 | PS3 Area-of-Interest network peer tracking HLE (porta `cellNetAoi.cpp` 71 linhas — stub: 9 entries CELL_OK). 9 entry points em ordem REG_FUNC cpp:62-70: DeletePeer, Init, GetPspTitleId, Term, Stop, GetRemotePeerInfo, Start, GetLocalInfo, AddPeer. MODULE_NAME="cellNetAoi" byte-exato cpp:4/60. 7 error codes placeholder facility 0x8002_D3__ (NOT_INITIALIZED=01, ALREADY_INITIALIZED=02, NOT_STARTED=03, ALREADY_STARTED=04, PEER_NOT_FOUND=05, PEER_ALREADY_EXISTS=06, INVALID_PARAMETER=07). CELL_NET_AOI_MAX_PEERS=32. ModuleState FSM 4-state (Uninit → Initialized ↔ Started/Stopped → Uninit). NetAoiPeer {peer_id, nickname, psp_title_id}. NetAoiLocalInfo {local_id, nickname}. NetAoi manager com state + Vec<peers> + 9 counters + test hooks (set_local_info, set_psp_title_id). require_initialized aceita Initialized/Started/Stopped (só Uninit=NOT_INITIALIZED). require_started requer exatamente Started. init/term/start/stop/add_peer/delete_peer/get_remote_info/get_local_info/get_psp_title_id todos validam FSM. add_peer enforce peer_id!=0, duplicate=ALREADY_EXISTS, cap 32=INVALID_PARAMETER. term clears peers + state=Uninit (rebootable). stop/start pode transitar entre Started↔Stopped preservando peers. full_netaoi_lifecycle_smoke (stage local_id 0xABCD+psp title UCUS98674 → init+start → 3 peers added → get_remote(2)→peer2 → delete peer 2 → stop → add_peer bloqueado NOT_STARTED → start → add peer 9 → query local/psp persistent → stop+term → counters) passa. | 24 |

| `rpcs3-hle-celllibprof` | wave-7 | PS3 user-trace profiler HLE (porta `cellLibprof.cpp` 36 linhas — menor módulo desta wave, 4 stub entries CELL_OK). 4 entry points em ordem REG_FUNC cpp:32-35: cellUserTraceInit, Register, Unregister, Terminate. MODULE_NAME="cellLibprof" byte-exato cpp:4/30. 6 error codes placeholder facility 0x8002_D4__ (NOT_INITIALIZED=01, ALREADY_INITIALIZED=02, PROBE_NOT_FOUND=03, PROBE_ALREADY_EXISTS=04, INVALID_PARAMETER=05, TERMINATED=06). CELL_LIBPROF_MAX_PROBES=256, CELL_LIBPROF_MAX_NAME_LEN=64. ModuleState FSM 3-state (Uninit → Initialized → Terminated — Terminated é terminal, no re-init). TraceProbe {probe_id u32, name String}. Libprof manager com state + Vec<probes> + 4 counters. register: probe_id=0=INVALID_PARAMETER, empty/>64-char name=INVALID_PARAMETER, duplicate=ALREADY_EXISTS, >256=INVALID_PARAMETER. unregister: unknown=PROBE_NOT_FOUND. terminate: clears probes + state=Terminated (no re-init). find_probe helper para tests. full_libprof_lifecycle_smoke (init → 3 probes enter_world/player_dies/frame_begin → unregister player_dies → survey → terminate drops all → re-init=TERMINATED sealed) passa. | 21 |

| `rpcs3-hle-cellsysutilap` | wave-7 | PS3 Wi-Fi AP / PSP tethering HLE (porta `cellSysutilAp.cpp` 99 linhas — 1 real-work + 2 stubs). 3 entry points em ordem REG_FUNC cpp:96-98: GetRequiredMemSize (real-work returns 1MiB), ApOn (stub), ApOff (stub). MODULE_NAME="cellSysutilAp" byte-exato cpp:6/94. 8 error codes byte-exatos cpp:9-19 facility 0x8002_CD__ (OUT_OF_MEMORY=00, FATAL=01, INVALID_VALUE=02, NOT_INITIALIZED=03, ZERO_REGISTERED=13, NETIF_DISABLED=14, NETIF_NO_CABLE=15, NETIF_CANNOT_CONNECT=16) — sub-range 0x00..0x16 disjoint de cellCrossController 0x80..0xA0 no mesmo facility. String length constants byte-exatos: TITLE_ID_LEN=9, SSID_LEN=32, WPA_KEY_LEN=64. REQUIRED_MEM_SIZE=1MiB=0x100000 byte-exato cpp:79. **3 wire structs #[repr(C)]** mirror byte-exato: CellSysutilApTitleId (12 bytes com padding[3]), CellSysutilApSsid (36 bytes), CellSysutilApWpaKey (68 bytes), CellSysutilApParam (type i32, wlan_flag i32 + 3 string structs) — compile-time size assertions via core::mem::size_of. copy_nul_terminated helper reproduz memcpy+NUL+zero-fill semantics. ApState FSM 2-state Off ↔ On. ApNetifBackend trait plugável (is_enabled/has_cable/can_connect) para testar NETIF_* error paths + HealthyNetifBackend reference. **on() validation cascade**: re-on=FATAL, null param=INVALID_VALUE, oversize strings=INVALID_VALUE, empty SSID=ZERO_REGISTERED, depois 3 netif checks em ordem. off() em Off=NOT_INITIALIZED (firmware stub silently accepts mas port enforces). SysutilApInputs ergonomic helper struct com string refs. full_sysutilap_lifecycle_smoke (get_required_mem_size=1MiB → on BLES1234+PS3-AP+secret123 + container 0xFACE → verify stored struct bytes → re-on=FATAL → off → counters) passa. | 23 |

| `rpcs3-hle-cellcelpenc` | wave-7 | PS3 CELP speech encoder HLE (porta `cellCelpEnc.cpp` 99 linhas — stub: 10 entries `todo()` com 1 real real-return CELL_OK each). 10 entry points em ordem REG_FUNC cpp:89-98: QueryAttr, Open, OpenEx, OpenExt, Close, Start, End, EncodeFrame, WaitForOutput, GetAu. MODULE_NAME="cellCelpEnc" byte-exato cpp:6/87. 6 error codes byte-exatos cellCelpEnc.h:10-18 facility 0x8061_40__ dois tiers (user 01-03 + core 81-83): FAILED/SEQ/ARG/CORE_FAILED/CORE_SEQ/CORE_ARG. Configuration enums byte-exatos cellCelpEnc.h:20-43: RPE_CONFIG_0..3 (0-3), FS_16KHZ=2, EXCITATION_MODE_RPE=1, WORD_SZ_INT16_LE=0/FLOAT=1. VERSION_UPPER=0x0001_0000/LOWER=0x0000_0000 committed. WORK_MEM_SIZE=2MiB convention. **6 wire structs #[repr(C)]** mirror cellCelpEnc.h:45-90: CellCelpEncAttr (work_mem_size + 2 version words), CellCelpEncResource (total_mem+start_addr+3 thread fields), CellCelpEncParam (excitation/sample/config/word + out_buff/out_size), CellCelpEncAuInfo, CellCelpEncPcmInfo, CellCelpEncResourceEx (com SPURS + priority [u8;8] + max_contention). EncoderState FSM per-handle (Opened → Started → Ended). OpenVariant enum (Open/OpenEx/OpenExt). EncoderHandle com id + state + variant + param + pending_aus VecDeque + per-handle counters. CelpEnc manager com handles Vec (MAX=8) + next_id + 10 per-entry counters + inject_au test hook. 4 validators const fn (is_valid_rpe_config/excitation_mode/sample_rate/word_size). open_inner valida start_addr!=0 + total_mem_size>=WORK_MEM_SIZE + MAX=FAILED. start valida FSM Opened + all 4 enum fields + out_buff!=0 + out_size!=0. end requires Started. encode_frame requires Started + frame.size>0 + start_addr!=0. wait_for_output requires Started + non-empty queue. get_au preserves queue on buffer-too-small (retry-safe pattern). full_celpenc_lifecycle_smoke (QueryAttr verify version → Open+Start com EXCITATION_RPE+FS_16KHZ+CONFIG_2+INT16_LE → 3 encode_frames + 3 inject_aus → 3 wait+get_au drain → End → Close → counter trace) passa. | 27 |

| `rpcs3-hle-cellcelp8enc` | wave-7 | PS3 CELP8 8kHz MPE speech encoder HLE (porta `cellCelp8Enc.cpp` 92 linhas — companion do cellCelpEnc). 9 entry points em ordem REG_FUNC cpp:83-91 (sem OpenExt comparado a cellCelpEnc). MODULE_NAME="cellCelp8Enc" byte-exato cpp:6/81. 6 error codes byte-exatos cellCelp8Enc.h:10-18 facility 0x8061_40A1..A3 + B1..B3 — **sub-range distinct/disjoint de cellCelpEnc** 0x8061_4001..4083 (verified via boundary test). **MPE_CONFIGS non-contiguous** [0,2,6,9,12,15,18,21,24,26] byte-exato cellCelp8Enc.h:21-33 (vs cellCelpEnc's RPE_CONFIG_0..3 contiguous 0-3). Single sample rate FS_8KHZ=1 (vs 16KHz). Single excitation mode MPE=0 (vs RPE=1). Single word size FLOAT=0 (vs INT16_LE+FLOAT). WORK_MEM_SIZE=1MiB (vs 2MiB no cellCelpEnc — 8kHz tem frame menor). 6 wire structs #[repr(C)] mirror byte-exato cellCelp8Enc.h:50-95. is_valid_mpe_config usa whitelist lookup (não range check). EncoderState FSM per-handle 3-state + OpenVariant 2-variant (Open/OpenEx). Celp8Enc manager MAX=8 handles + 9 per-entry counters + inject_au test hook. start valida 4 enum fields + out_buff!=0 + out_size!=0. get_au preserve queue on buffer-too-small (retry-safe). test completo whitelisted MPE config: itera 10 valid values + rejeita 9 invalid. test distinct_facility_range_from_celpenc verifica que 0x8061_40A1 > 0x8061_4083 + < 0x8061_40B1. full_celp8enc_lifecycle_smoke (QueryAttr → Open → Start MPE+FS_8KHZ+config_9+FLOAT → 2 encode_frames + 2 inject_aus → 2 wait+get_au → End → Close) passa. | 26 |

| `rpcs3-hle-cellsysutilmisc` | wave-7 | PS3 license-area query HLE (porta `cellSysutilMisc.cpp` 20 linhas — tiniest do batch, 1 entry point). 1 entry point: cellSysutilGetLicenseArea (cpp:8-15 retorna g_cfg.sys.license_area). MODULE_NAME="cellSysutilMisc" byte-exato cpp:6/17. 7 CellSysutilLicenseArea constants byte-exatos cellSysutil.h: J=0 (SCEJ Japan), A=1 (SCEA Americas), E=2 (SCEE Europe), H=3 (SCEH Southeast Asia), K=4 (SCEK Korea), C=5 (SCH China), OTHER=100 sentinel. CellSysutilLicenseArea enum #[repr(i32)] com as_i32/from_i32 roundtrip + sce_tag helper retornando exact 3-letter region code from header comments. Default=A (SCEA). LicenseAreaSource trait plugável mirror de g_cfg.sys.license_area. FixedLicenseArea reference impl. cell_sysutil_get_license_area<S: LicenseAreaSource> entry point + get_license_area_typed convenience. full_license_area_lifecycle_smoke itera todas 7 regions, valida raw i32 matches as_i32 + typed enum roundtrip. unique sce_tag assertion via BTreeSet. Custom DynamicBackend impl in tests demonstrates trait override. | 13 |

| `rpcs3-hle-cellvideoupload` | wave-7 | PS3 YouTube upload HLE (porta `cellVideoUpload.cpp` 54 linhas — service decommissioned ~2012, 1 entry point cellVideoUploadInitialize). 1 entry point em REG_FUNC cpp:53. MODULE_NAME="cellVideoUpload" byte-exato cpp:7/51. 12 error codes byte-exatos cellVideoUpload.h:49-60 facility 0x8002_D0__ NON-CONTIGUOUS: CANCEL=00, NETWORK=01, SERVICE_STOP/BUSY/UNAVAILABLE/QUOTA=02-05, ACCOUNT_STOP=06 (gap 0x07..0x1F), OUT_OF_MEMORY/FATAL/INVALID_VALUE/FILE_OPEN/INVALID_STATE=20-24. Gap structure byte-exato preserved (test error_code_gap_structure_preserved). 2 status codes: INITIALIZED=1, FINALIZED=2. 6 length caps byte-exatos: FILE_PATH=1023, YOUTUBE CLIENT_ID=64/DEVELOPER_KEY=128/TITLE=61/DESCRIPTION=1024/KEYWORD=25. RESULT_URL_LEN=128 matching firmware vm::var<char[]>(128) cpp:40. YoutubeUploadFields struct (client_id, developer_key, title_utf8, description_utf8, keyword_1/2/3_utf8, is_private, rating). CellVideoUploadParam com site_id + file_path + youtube + options. CellVideoUploadOption {type, value}. UploadCallback {status, error_code, result_url, userdata}. validate_param valida file_path non-empty/<=1023 + all YouTube field caps + rating<=5 + is_private<=1. initialize queues FIFO [INITIALIZED+OK, FINALIZED+OK] byte-exato cpp:42-43 (service stub sempre succeeds + terminal). init_calls counter. double-init=INVALID_STATE. null param=INVALID_VALUE, null cb=INVALID_VALUE, empty file_path=FILE_OPEN, oversize strings=INVALID_VALUE. **NOTA de facility overlap**: cellVideoUpload commit real 0x8002_D000..D006/D020..D024; cellDtcpIpUtility (iter #127) usa placeholder 0x8002_D001..D00C no mesmo facility sub-range — documented no-conflict since crates don't cross-depend. full_videoupload_lifecycle_smoke (initialize → 2 deferred callbacks INITIALIZED+FINALIZED both OK → re-init=INVALID_STATE) passa. | 23 |

| `rpcs3-hle-cellsysutilnpeula` | wave-7 | PS3 NP EULA confirmation dialog HLE (porta `cellSysutilNpEula.cpp` 103 linhas — 3 entries, real-work stubbed para always-accepted). 3 entry points em REG_FUNC cpp:100-102: sceNpEulaCheckEulaStatus, sceNpEulaAbort, sceNpEulaShowCurrentEula. MODULE_NAME="cellSysutilNpEula" byte-exato cpp:7/98. 15 error codes byte-exatos sceNp.h facility 0x8002_E5__ **3 sub-ranges**: base 00-05 (UNKNOWN/INVALID_ARGUMENT/NOT_INITIALIZED/ALREADY_INITIALIZED/OUT_OF_MEMORY/BUSY), EULA A0-A1 (EULA_NOT_FOUND/NET_OUT_OF_MEMORY), CONF B0-B6 (CONF_FORMAT/INVALID_FILENAME/TOO_MANY/INVALID_LANGUAGE/INVALID_COUNTRY/INVALID_NPCOMMID/INVALID_EULA_VERSION). Gap structure byte-exato preserved (test error_code_sub_range_gaps). 6 SceNpEulaStatus enum values byte-exatos cpp:9-17 (Unknown=0..Error=5) com as_u32/from_u32 roundtrip. STUB_EULA_VERSION=1 matching cpp:51. SceNpCommunicationId #[repr(C)] 14-byte mirror. EulaCallbackKind enum (CheckEulaStatus/ShowCurrentEula). EulaDeferredCallback {kind, status, error_code, version, userdata}. SysutilNpEula manager com status + 2 boolean registration flags (mutual exclusion) + pending_callbacks + 3 counters. check_eula_status valida comm_id+cb_addr + rejects if either callback live (ALREADY_INITIALIZED) + sets status=AlreadyAccepted cpp:46 + queues deferred callback. abort: NOT_INITIALIZED sem callback registered, senão status=Aborted (preserving registration per cpp:70 comment). show_current_eula valida + rejects concurrent + registers flag (NO callback queued — cpp:93 "Unknown parameters" comment). deliver_pending helper simulates sysutil scheduler + clears registration flags. either_callback_registered helper reproduz cpp:40/88 combined check. full_npeula_lifecycle_smoke (check → deliver AlreadyAccepted → show → abort → status=Aborted, counters) passa. | 23 |

| `rpcs3-hle-cellvideoplayerutility` | wave-7 | PS3 video player utility HLE (porta `cellVideoPlayerUtility.cpp` 127 linhas — largest all-stub module, 17 entries CELL_OK). 17 entry points em ordem REG_FUNC cpp:110-126. MODULE_NAME="cellVideoPlayerUtility" byte-exato cpp:4/108. 8 placeholder error codes facility 0x8002_D5__ (NOT_INITIALIZED=01, ALREADY_INITIALIZED=02, NOT_OPEN=03, ALREADY_OPEN=04, FINALIZED=05, INVALID_PARAMETER=06, THUMBNAIL_NOT_ACTIVE=07, THUMBNAIL_ALREADY_ACTIVE=08). VOLUME_MIN=0.0/MAX=1.0/DEFAULT=1.0. **6 PlaybackCommand** (Play/Pause/Resume/Stop/FastForward/FastReverse). **5 PlaybackStatus** (Stopped/Playing/Paused/FastForward/FastReverse). **FSM 3-axis**: ModuleState (Uninit→Initialized→Finalized terminal), SessionState (Closed↔Open), thumbnail_active bool. TransferPictureInfo {frame_count, bytes_available, transfer_complete}. OutputPicture {picture_id, width, height, is_stereo}. VideoPlayer manager com 3 state axis + volume + start/stop/download positions + transfer info + 17 per-entry counters. playback_control valida comando vs current status: Pause requires Playing, Resume requires Paused, outros sempre-OK. set_start_position valida<stop, set_stop_position valida>start. set_volume clamp to [0,1], NaN=INVALID_PARAMETER. thumbnail_lifecycle enforces start/end pair. get_output_picture/stereo requires Playing state, retorna descriptor com picture_id incremental. full_video_player_lifecycle_smoke (init→open→positions 0..120s→volume 0.75→thumbnail cycle→Play→2 frames→Pause→Resume→Stop→transfer_complete→close→finalize + counter trace) passa. | 25 |
| `rpcs3-hle-cellphotodecode` | wave-7 | PS3 photo decoder HLE (porta `cellPhotoDecode.cpp` 181 linhas, 4 entries). 4 entry points em ordem REG_FUNC cpp:177-180 (Initialize/Initialize2/Finalize/FromFile). MODULE_NAME="cellPhotoDecodeUtil" byte-exato cpp:175. **6 error codes byte-exato** 0x8002_C901..C906 (BUSY/INTERNAL/PARAM/ACCESS_ERROR/INITIALIZE/DECODE) cpp:10-18. VERSION_CURRENT=0 cpp:41. SYS_MEMORY_CONTAINER_ID_INVALID=0xFFFF_FFFF. Wire structs: **CellPhotoDecodeSetParam** {dst_buffer, width:u16, height:u16, reserved1, reserved2} 16 bytes; **CellPhotoDecodeReturnParam** {width:u16, height:u16, reserved1, reserved2} 12 bytes — ambos com size_of assert. **InitVariant enum** V1 {container1, container2} vs V2 {container2} registra qual Initialize foi chamada. **Deferred callback queue**: cada Initialize/Initialize2/Finalize enfileira PendingCallback com CallbackCause — mirrors `sysutil_register_cb`. drain_callbacks via core::mem::take. **VfsRegistry** stub com register_file/unregister_file/is_file + dedup. **PhotoDecodeBackend trait** (decode_scaled) + MockBackend capturando calls + injetando next_result. from_file cascata: null-check→`*return=0`→prefix whitelist (/dev_hdd0, /dev_hdd1, /dev_bdvd)→VFS is_file→backend decode→fills width/height. Backend Err()→ERROR_DECODE; prefix/file fail→ERROR_ACCESS_ERROR. 4 per-entry counters + 4 outcome counters (param_error, access_error, decode_error, decode_success). join_vpath sem normalização (preserva behaviour upstream). full_photodecode_lifecycle_smoke (init V1→register file→from_file 320x240 com backend down-scale→finalize→drain 2 callbacks→counter trace) passa. | 25 |
| `rpcs3-hle-cellusbpspcm` | wave-7 | PS3 USB PSP communication module HLE (porta `cellUsbpspcm.cpp` 239 linhas, **27 entries** all-stub UNIMPLEMENTED_FUNC). 27 entry points em ordem REG_FUNC cpp:212-238 (Init, End, CalcPoolSize, Register, Unregister, GetAddr, Bind, BindAsync/WaitBindAsync/PollBindAsync/CancelBind, Close, Send/SendAsync/WaitSendAsync/PollSendAsync, Recv/RecvAsync/WaitRecvAsync/PollRecvAsync, Reset/ResetAsync/WaitResetAsync/PollResetAsync, WaitData/PollData/CancelWaitData). MODULE_NAME="cellUsbPspcm" byte-exato cpp:4/210. **12 error codes byte-exato** 0x8011_0401..040C (NOT_INITIALIZED/ALREADY/INVALID/NO_MEMORY/BUSY/INPROGRESS/NO_SPACE/CANCELED/RESETTING/RESET_END/CLOSED/NO_DATA) cpp:7-21. **FSM inferida do vocab de errors**: ModuleState (Uninit→Initialized→Finalized); per-handle HandleState (Unbound/Binding/Bound/Resetting/Closed); 5 AsyncSlot por handle (bind/send/recv/reset/data_wait) cada uma {Idle/Pending/Completed/Canceled}. Handle registry cap MAX_HANDLES=16, HANDLE_BASE=0x1000_0000, addr alocado 0xA000_0000 + N*0x1000 deterministic. calc_pool_size fórmula `count*0x200+0x40` stable. Validation cascade rica: Bind/Send/Recv rejeita Resetting (→RESETTING), Closed (→CLOSED), Unbound (→INVALID); Register over cap→NO_SPACE; Unregister busy→BUSY ou resetting→RESETTING; Close pendente→BUSY; Recv vazio→NO_DATA; Send/Recv len=0→INVALID; CancelBind não-pending→INVALID. ResetAsync completion retorna **RESET_END** one-shot via consume_slot, alinhado com sub-range cpp:0x8011_040A. wait_data slot mgmt com estado Canceled vs Completed vs Pending re-arm. 5 inject_* test hooks (bind/send/recv/reset_complete + data_ready) + 27 per-entry counters. full_usbpspcm_lifecycle_smoke (init→register→bind_async+complete→send→recv_async+complete→poll_data→reset_async+complete→wait_reset_async=RESET_END→bind→close→end). | 31 |

| `rpcs3-hle-cellwebbrowser` | wave-7 | PS3 web browser sysutil sub-module (porta `cellWebBrowser.cpp` 372 linhas, **47 entries** REG_FUNC mas sem PRX próprio — registrado em cellSysutil via cellSysutil_WebBrowser_init). 47 entry points em ordem REG_FUNC cpp:325-371 (Activate/Config×17/Create×7/Destroy×2/Estimate×2/GetUsrdata/Initialize/Navigate2/SetLocalContents/SetSystemCallbackUsrdata/Shutdown/UpdatePointer/Wakeup + WebComponent×3). HOST_MODULE_NAME="cellSysutil"/SUBMODULE_NAME="cellWebBrowser". **6 event codes byte-exato** s32 `CellWebBrowserEvent` (INITIALIZING_FINISHED=1, SHUTDOWN_FINISHED=4, LOADING_FINISHED=5, UNLOADING_FINISHED=7, RELEASED=9, GRABBED=11) header:6-14. **ESTIMATE2_MEM_SIZE=1MB** preserva cpp:235 `*memSize = 1 * 1024 * 1024`. 7 placeholder error codes 0x8002_F701..F707 (NOT_INITIALIZED/ALREADY_INITIALIZED/INVALID_PARAMETER/NOT_ACTIVE/ALREADY_ACTIVE/BROWSER_NOT_FOUND/OUT_OF_BROWSERS) — upstream sem errors enum. Wire structs #[repr(C)] com size_of assert: **CellWebBrowserPos** 8 bytes, **Size** 8, **Rect** 16, **MimeSet** 8, **Config** 36, **Config2** 68 (PartialEq sem Eq — f32 resolution_factor). **FSM**: ModuleState (Inactive/Initialized/Shutdown) + per-browser BrowserState (Inactive/Active/Destroyed) + **BrowserVariant** enum (V1 de Create/CreateWithConfig/CreateWithConfigFull vs V2 de Create2/Render2/RenderWithRect2/WithRect2). Browser registry cap MAX_BROWSERS=8, BROWSER_ID_BASE=0x5001_0000. **Deferred callback queue** PendingSystemEvent{system_cb, userdata, event_code}: Initialize enfileira INITIALIZING_FINISHED com capturados system_cb+container; Shutdown enfileira SHUTDOWN_FINISHED usando stashed system_cb (mesmo sem init prévio, matching cpp:285-289). drain_pending via core::mem::take. **destroy vs destroy2 valida variant match** (mismatched→INVALID_PARAMETER). config_set_*2 setters com write-through opcional (heap_size/tab_count/functions/size_mode/view_restriction). deactivate flips Active→Inactive preservando Destroyed. set_system_callback_usrdata atualiza userdata singleton — próximo shutdown carrega novo userdata. 46 per-entry counters (Destroy/Destroy2 separados). full_webbrowser_lifecycle_smoke (estimate2 pre-init→init→set_usrdata→create2→mark_active→config_set_heap_size2+tab_count2 write-through→navigate2→deactivate→destroy2→shutdown→drain [init+shutdown events byte-exato]). | 22 |

| `rpcs3-hle-cellsysutilavc` | wave-7 | PS3 Audio/Video Chat sysutil sub-module (porta `cellSysutilAvc.cpp` 373 linhas, 20 entries registradas em cellSysutil PRX via cellSysutil_SysutilAvc_init). HOST_MODULE_NAME="cellSysutil"/SUBMODULE_NAME="cellSysutilAvc". 20 entry points em ordem REG_FUNC cpp:353-372. **12 error codes byte-exato COM GAPS preservados** 0x8002_B701..B710: UNKNOWN/NOT_SUPPORTED/NOT_INITIALIZED/ALREADY_INITIALIZED/INVALID_ARGUMENT/OUT_OF_MEMORY/BAD_ID (701-707), gap 708/709, INVALID_STATUS/TIMEOUT (70A/70B), gap 70C, NO_SESSION/INCOMPATIBLE_PROTOCOL (70D/70E), gap 70F, PEER_UNREACHABLE (710) — test assertion explicita os gaps. **16 event codes byte-exato** CellSysutilAvcEvent: 8 standard (Load/Unload/Join/Bye × Succeeded/Failed 1..8) + 8 system (0x1000_0001..0x1000_0008 NewMemberJoined..CameraDetected). **13 event params** CELL_AVC_EVENT_PARAM_ERROR_* (u64). **9 enums** byte-exato: TransitionType (6 values, None=0xFFFF_FFFF), WindowZorderMode, Attribute (8 values), LayoutMode (LEFT=0..BOTTOM=3), MediaType (VoiceChat=1/VideoChat=2), Video/VoiceQuality (DEFAULT=1 only), RoomPrivilegeType, VoiceDetect× 2. Constants byte-exato: VIDEO_MEMORY=26MB, VOICE=8MB, EXTRA_SHARING=2MB, OPTION_PARAM_VERSION=100, REQUEST_ID_SYSTEM_EVENT=0. Wire structs: OptionParam 12 bytes, SceNpId 32, VoiceDetectData 36, SceNpRoomId 16. **load_async validation cascade** preserva cpp:252-279 ordem exata: (1) media∈{VoiceChat,VideoChat}→INVALID_ARGUMENT (2) func!=0 && request_id!=null→INVALID_ARGUMENT (3) videoQuality==DEFAULT && voiceQuality==DEFAULT→INVALID_ARGUMENT (4) avc_cb!=0→ALREADY_INITIALIZED (5) stash callback+arg+alloc request id+enqueue LOAD_SUCCEEDED. **unload semantics cpp:92-97**: UnloadAsync só enfileira evento; callback limpa APENAS quando deliver_pending entrega UNLOAD_SUCCEEDED (unload_consumed flag). **Request ID counter** global monotonic atomic-style via wrapping_add, Load→0, Join→1, Bye→2, Unload→3. set_layout_mode range check 0..=BOTTOM(3), set_speaker_volume_level range [0..=10]. enum_players: null players_num→INVALID_ARGUMENT; null players_id→set *players_num=0; non-null players_id→no-op preserves players_num (cpp:149-158). Getters null-check output. 20 per-entry counters. full_sysutil_avc_lifecycle_smoke (load VoiceChat→layout Top→volume 7→show_panel→join_request→bye→deliver 3 events→unload→deliver UNLOAD→callback cleared→reload VideoChat rid=4). | 24 |

| `rpcs3-hle-cellsysutilavcext` | wave-7 | PS3 AVC Extension HLE (porta `cellSysutilAvcExt.cpp` 320 linhas, 30 entries no seu **próprio PRX** cellSysutilAvcExt — diferente de cellSysutilAvc que é sub-modulo de cellSysutil). 30 entry points em ordem REG_FUNC cpp:290-319. MODULE_NAME="cellSysutilAvcExt" byte-exato cpp:288. **Error codes shared com cellSysutilAvc** (facility 0x8002_B7__) — UNKNOWN=0x8002_B701, ALREADY_INITIALIZED=0x8002_B704, INVALID_ARGUMENT=0x8002_B705 redeclarados para auto-contained crate. **TransitionType enum byte-exato** Linear/Slowdown/FastUp/Angular/Exponent (0..=4) + None=0xFFFF_FFFF, TRANSITION_TYPE_MAX=4. **ZORDER_FORWARD_MOST=0x2/BEHIND_MOST=0x3** range inclusive. OptionParam mirror 12 bytes com size_of assert. SceNpId 32 bytes. **InitOptionParam** (cpp:253-276): option null→INVALID_ARGUMENT; version switch 100 (no-op extras) ou 180 (maxPlayers=16) ou default→UNKNOWN; sharingVideoBuffer unconditionally=false on success. Preserva ordem upstream: version stashed ANTES do switch (test verifica opt.version=42 mesmo em UNKNOWN path cpp:260-271). **LoadAsyncEx** (cpp:121-150): option null→INVALID_ARGUMENT; version 100|180 switch com `sharingVideoBuffer && media==VoiceChat→INVALID_ARGUMENT`; default→UNKNOWN; delegate to real LoadAsync via **LoadAsyncDelegate trait** + MockLoadAsync para testes. **TRANSITION strict check**: SetWindowAlpha/Size/Show/HideWindow rejeitam transition_type > EXPONENT — isto inclui TransitionType::None(0xFFFF_FFFF), preserved upstream bug behaviour documented em test `set_window_alpha_transition_range_check`. SetWindowZorder valida `[FORWARD_MOST..=BEHIND_MOST]` (2..=3, rejeita 0/1/4+). SetWindowPosition/Rotation sem validação (cpp:25-29/47-51 só logam). GetWindow* (Position/Size/Rotation/Alpha/ShowStatus) null-check todos ponteiros. GetSurfacePointer null-check **5 ponteiros** (player+ptr+size+size_x+size_y). IsMicAttached/IsCameraAttached preservam `ensure(!!status)` como Result idiomático (cpp:14/178 aborta na prod, aqui retorna INVALID_ARGUMENT). start/stop Camera/Mic/Voice Detection flip state flags. Nameplate show/hide + get_show_status roundtrip. SetChatMode/SetChatGroup persistem último mode. 30 per-entry counters. full_avcext_lifecycle_smoke (InitOptionParam v180+maxPlayers16→LoadAsyncEx VideoChat delegate→window config size/alpha/zorder+show→panel show→detection start×3→nameplate toggle→tear down + nameplate hide). | 27 |

| `rpcs3-hle-cellatracmulti` | wave-7 | PS3 ATRAC multi-track audio decoder HLE (porta `cellAtracMulti.cpp` 281 linhas, 25 entries). 25 entry points em ordem REG_FUNC cpp:248-280. MODULE_NAME="cellAtracMulti" byte-exato cpp:246. **22 error codes byte-exato** facility 0x8061_0B__ em 9 sub-ranges (_B0_ API_FAIL, _B1_ 5× format/buffer errors, _B2_ 3× decoder state, _B3_ 4× decoding errors, _B4_ 2× memory, _B5_ NONEED, _B6_ LOOP_NUM, _B7_ 2× sample, _B8_ 2× thread priority, _B9_ API_PARAMETER). **3 RemainFrame sentinels** signed negatives: ALLDATA_IS_ON_MEMORY=-1, NONLOOP=-2, LOOP=-3. CELL_ATRACMULTI_HANDLE_SIZE=512. Wire structs: **CellAtracMultiHandle** #[repr(C, align(8))] 512 bytes exato + 8-byte align assert; **CellAtracMultiBufferInfo** 16 bytes; **CellAtracMultiExtRes** 12 bytes. **Observable constants byte-exato** preservados: STUB_WORK_MEM=0x1000, STUB_WRITABLE=0x1000, STUB_VACANT=0x1000, STUB_CHANNEL=2, STUB_MAX_SAMPLE=512, STUB_BITRATE_KBPS=128. **Peculiaridade upstream cpp:175-181 preservada**: GetNextDecodePosition escreve `*puiSamplePosition = 0` E retorna `CELL_ATRACMULTI_ERROR_ALLDATA_WAS_DECODED` — único entry que sempre retorna erro. GetStreamDataInfo writes `*ppucWritePointer = pHandle.addr()` (handle_addr). CreateDecoder/Ext ambos memcpy 512 bytes de work_mem para handle.ucWorkMem. Decode sempre grava tupla fixa (samples=0, finish=1, remain=ALLDATA_IS_ON_MEMORY). IsSecondBufferNeeded retorna 0 (not_an_error(0) = false). GetSecondBufferInfo retorna pair (0, 0). InstanceState per-handle tracking (data_set flag, decoder_created flag, loop_num state) com map em Vec<(addr, state)>. Multi-handle independent state tracked (test verifica 2 handles com loop_nums diferentes). 25 per-entry counters. full_atracmulti_lifecycle_smoke (set_data→create_decoder+memcpy→get_channel/bitrate/max_sample→decode tuple→get_next_decode_position=ALLDATA_WAS_DECODED error→set_loop_num=3→delete_decoder clears instance). | 29 |

| `rpcs3-hle-cellsysutilavc2` | wave-7 | PS3 AVC v2 (voice/video chat deeper variant) HLE (porta `cellSysutilAvc2.cpp` 1195 linhas, 54 entries + header 302 linhas com 14 errors/16 events/11 enums/4 structs) — maior módulo portado. MODULE_NAME="cellSysutilAvc2" byte-exato cpp:1139. 54 entry points em ordem REG_FUNC cpp:1141-1194. **14 error codes byte-exato shared com cellSysutilAvc** (0x8002_B7__) + **AVC2-específicos** WINDOW_ALREADY_EXISTS (B70F), TOO_MANY_WINDOWS (B710), TOO_MANY_PEER_WINDOWS (B711), WINDOW_NOT_FOUND (B712). **16 event codes byte-exato** (8 standard Load/Unload/Join/Leave × Succeeded/Failed 1..8 + 8 system 0x1000_0001..0008). 11 enums (MediaType VoiceChat=1/VideoChat=0x10 **DIFERENTE cellSysutilAvc**, VoiceQuality/VideoQuality=1 each, FrameMode NORMAL=1/INTRA=2, VideoResolution QQVGA=1/QVGA=2, ChatTargetMode 4 values, Attribute 11, Transition 6, Zorder 2, 4 camera statuses, 4 mic statuses, 3 streaming modes, 4 video sharing). CELL_SYSUTIL_AVC2_INIT_PARAM_VERSION=140, accepted versions {100,110,120,130,140}. DEFAULT_SPEAKER_VOLUME_LEVEL=40.0 mirrors cpp:88. Wire structs: VoiceInitParam 32 bytes, VideoInitParam 32, StreamingModeParam 14, InitParam (composite), PlayerInfo 16 (size_of assert). **load_shared validation cascade** preserva cpp:800-961 ordem: (1) init_param null + version check → INVALID_ARGUMENT (2) media_type switch: VoiceChat / VideoChat / other→NOT_SUPPORTED. **VOICE_CHAT validation**: max_players ∈ [2,64], spu_load ≤ 100, voice_quality == NORMAL, max_speakers ∈ [1,16], streaming_mode version-gated (v120+ accepts DIRECT_LAN, v110 só NORMAL/DIRECT_WAN, v<110 herda atual), callback non-null, not-already-loaded→ALREADY_INITIALIZED. **VIDEO_CHAT validation**: callback MUST be null (!), max_windows ≤ (NORMAL?6:16), bitrate ∈ [1000, 512000], framerate ∈ [1, 30]; total_video_bitrate derivado via video_chat_total_bitrate formula alinhada a 1MB + 0x100000 buffer. **EstimateMemoryContainerSize** v100→0x400000; v110-140 VoiceChat→0x300000, VideoChat→formula estimate_video_chat_memory byte-exato cpp:370-410 (preserved), outra media→INVALID_ARGUMENT + `*size=0`. **Pending callback queue com auto-clear cpp:137-147**: LOAD_FAILED/UNLOAD_SUCCEEDED/UNLOAD_FAILED com `error_code < 2` auto-clears avc2_cb + arg. **UnloadAsync2** VoiceChat enqueues UNLOAD_SUCCEEDED(0) (auto-clear), VideoChat enqueues UNLOAD_SUCCEEDED(2) (NO auto-clear — test explícito). **SetVideoMuting `muting > 1`→INVALID_ARGUMENT** preserved (cpp:432 weird check documented). SetPlayerVoiceMuting dedup/remove via Vec<u16>. init_param escreve version antes do version switch falhar (preserva cpp:664-677). EnumPlayers null id→*num=1, non-null fills i+1. 54 per-entry counters. full_sysutil_avc2_lifecycle_smoke (init_param v140→load_async VoiceChat→mute config→players 5/9→join_chat_request→start_streaming2→deliver load+join→stop→leave_request→unload_async→deliver UNLOAD→callback cleared). | 42 |

| `rpcs3-hle-cellsailrec` | wave-7 | PS3 SAIL Rec (Recorder/Composer/Feeder) HLE (porta `cellSailRec.cpp` 423 linhas, 58 entries all-stub + 2 static side modules). MODULE_NAME="cellSailRec" byte-exato cpp:354. 58 entry points em ordem REG_FUNC cpp:359-421. **STATIC_SIDE_MODULES=["cellMp4", "cellApostSrcMini"]** — cellSailRec registra 2 módulos adicionais via `ppu_static_module` cpp:356-357 (preserved). **5 families organizadas**: 3× Profile setters (Es audio/video/stream params), 4× VideoConverter (canProcess/process/canGetResult/getResult), 6+6× FeederAudio/Video (Initialize/Finalize + 4 notifications each), 21× Recorder (lifecycle+config+profile×2+converter×2+stream×2+start/stop/cancel+composer×2+dump), 17× Composer (Init/Final+tri-fold Es audio/user/video × Getter/Parameter/Au/TryGetAu/ReleaseAu + 2 notifications). 6 placeholder errors 0x8061_4B01..4B06 (upstream sem error enum — NOT_INITIALIZED/ALREADY/INVALID_STATE/INVALID_PARAMETER/NOT_RUNNING/ALREADY_RUNNING). **FSM inferida 3-axis**: RecorderState (Inactive→Booted→Running→Booted→Finalized, Boot é required-step), FeederState por feeder (Uninit/Initialized/Finalized independentes audio vs video), ComposerState (Uninit/Initialized/Finalized). Flags rastreadas: feeder_audio_set + feeder_video_set (via SetFeederAudio/Video), composer_registered, stream_open, video_converter_active, profile_count (create/destroy saturating). recorder_stop e recorder_cancel só saem de Running para Booted (no-op se não-Running). 58 per-entry counters. full_sailrec_lifecycle_smoke (init all→set feeders→profile+create→video converter→register composer→open stream→boot→start→composer pulls Es AU→stop→close→unregister→destroy video converter→destroy profile→finalize all 4). | 20 |

| `rpcs3-hle-cellspursjq` | wave-7 | PS3 SPURS Job Queue HLE (porta `cellSpursJq.cpp` 449 linhas, 63 entries all-stub). MODULE_NAME="cellSpursJq" cpp:384. 63 entry points em 3 famílias: (a) **JobQueue**: 8× Attribute, SetWaitingMode, Shutdown, 2× Create, Join, 10× Push (JobList/Job/Job2/AndRelease/Body/Sync/Flush/AllocateDescriptor), 6× Get (Spurs/HandleCount/Error/MaxSize/JobQueueId/SuspendedSize), Open/Close, 3× Semaphore+SendSignal. (b) **Port**: GetJobQueue, 6× Push + Sync/TrySync + 3× CopyPush, Initialize/InitializeWithDescriptorBuffer/Finalize. (c) **Port2**: GetJobQueue, Push×2+Sync×2+Create/Destroy/AllocateDescriptor + 4× Push/Copy variants. 3× Exception event handlers. 7 placeholder errors 0x8061_5B01..5B07 (NULL_POINTER/NOT_INITIALIZED/ALREADY/BUSY/JOIN/FULL/EMPTY) — upstream sem error enum. FSM inferida: **JobQueueState** (Uninit→Created→Open→Created→Joined→Shutdown) com open/close toggling Created↔Open. PortState (Uninit/Initialized/Finalized) × 2 para Port e Port2 independentes. Push× tracked via pending_pushes counter — push_sync e push_flush drenam para 0. allocate_job_descriptor_body não incrementa pending (observable distinto). 63 per-entry counters. full_spurs_jq_lifecycle_smoke (Attribute init→Create→Open→Port init+push×3+sync→Port2 Create+push+sync→Semaphore init+signal→exception handler→direct pushes+sync→teardown). | 23 |

| `rpcs3-hle-scenpsns` | wave-7 | PS3 Sony NP SNS (Facebook integration) HLE (porta `sceNpSns.cpp` 333 linhas, 11 entries). MODULE_NAME="sceNpSns". **18 error codes byte-exato** facility 0x8002_45__ (6 general + 12 FB): UNKNOWN/NOT_SIGN_IN/INVALID_ARGUMENT/OUT_OF_MEMORY/SHUTDOWN/BUSY 01-06; FB ALREADY/NOT/EXCEEDS/UNKNOWN/ABORTED/ALREADY_ABORTED/CONFIG_DISABLED/FBSERVER/THROTTLE_CLOSED/OPERATION_INTERVAL/UNLOADED_THROTTLE/ACCESS_NOT_ALLOWED 11-1C. Constants byte-exato: INVALID_HANDLE=0, HANDLE_SLOT_MAX=4, PERMISSIONS/TOKEN_LENGTH=255, LONG_TOKEN=4096, OPTIONS_SILENT=1. Wire structs repr(C): SceNpSnsFbInitParams 8 bytes size_of assert, SceNpSnsFbAccessTokenParam (fb_app_id+permissions[256]+options). **Validation cascade cpp:42-317 preserved**: (a) **Init cpp:48-56** — init check PRECEDES null check (double-init com null params ainda retorna ALREADY_INITIALIZED, test explícito); (b) **CreateHandle cpp:95-98** — *handle IS written BEFORE the slot-exhaustion check (test: extra=5 after EXCEEDS_MAX on 5th create); (c) **GetAccessToken/Long cascade cpp:154-192** — param null→INVALID → result null→INVALID → fb_app_id==0→INVALID → !init→NOT_INITIALIZED → handle range→INVALID → handle alive→UNKNOWN_HANDLE → PSN offline→NOT_SIGN_IN; (d) **StreamPublish/LoadThrottle cpp:194-279** — NO init check, só handle range+alive; (e) **CheckConfig cpp:240-250** — NO null check, só init. PsnStatus plug para simular np_handler::get_psn_status (Online/Offline). Handle registry Vec<u32> com next_handle_id saturating. 11 per-entry counters. full_sns_lifecycle_smoke (init+pool→create 4 handles [1,2,3,4]→5th fails com EXCEEDS_MAX mas extra=5 written→use handle 2 (access_token+stream+load+check)→destroy 1+3→double destroy fails UNKNOWN→abort 2+4→term→post-term NOT_INITIALIZED). | 21 |

| `rpcs3-hle-scenpmatchingint` | wave-7 | PS3 internal NP matching (room/lobby wrappers) HLE (porta `sceNpMatchingInt.cpp` 100 linhas, 11 entry registrations). MODULE_NAME="sceNpMatchingInt". **Quirk crítico preservado**: 8 entries registradas via REG_FUNC + **3 entries via REG_FNID** (FNID público diferente do C++ symbol — `sceNpMatchingJoinRoomGUI` → OLD_sceNpMatchingJoinRoomGUI cpp:90, idem SetRoomInfoNoLimit cpp:92 e GetRoomInfoNoLimit cpp:96 — workaround para name conflicts com sceNp module). MatchingRegKind enum (Func/Fnid) + MatchingEntry tuple `(fnid, impl_symbol, kind)` exposed para preservar essa distinção. **MatchingBackend trait** com 6 métodos (get_room_member_list, join_room, set_room_info{no_limit:bool}, get_room_list{is_gui:bool}, get_room_info{no_limit:bool}, create_room) + NullBackend reference que captura args para verificação. **Quirk preservado cpp:46/54**: tanto `GetRoomListWithoutGUI` quanto `GetRoomListGUI` chamam `matching_get_room_list(..., false)` — ambos passam is_gui=false ao backend (test explícito captura ambos com vec![false, false]). **OLD_SetRoomInfoNoLimit cpp:38** sempre passa no_limit=false ao backend (despite o nome). 11 per-entry counters. full_lifecycle_smoke (create_room→get_room_list×2→get_member_list→get_room_info→join_room×2 variants→set_room_info_no_limit→send_message→cancels) passa. | 13 |

| `rpcs3-hle-scenpplus` | wave-7 | PS3 PlayStation Plus / NP manager subscription check (porta `sceNpPlus.cpp` 17 linhas — **MENOR módulo do workspace**, 1 entry point). MODULE_NAME="sceNpPlus". 1 entry: `sceNpManagerIsSP` retorna `not_an_error(1)` byte-exato cpp:11 — `SP_STATUS_TRUE=1` constante exposta. Quirk preservado cpp:10 TODO comment: PSHome trunca return para 1 byte (provavelmente bool em callers); test `return_value_fits_in_one_byte` valida que value cabe em u8. 1 per-entry counter. Usado por PSHome, store DLC checks, beta gates. | 5 |

| `rpcs3-hle-scenputil` | wave-7 | PS3 NP utility (bandwidth test) HLE (porta `sceNpUtil.cpp` 152 linhas, 4 entries). MODULE_NAME="sceNpUtil". 4 entry points: BandwidthTestInitStart/Shutdown/GetStatus/Abort. **2 errors re-exported sceNp.h facility 0x8002_AA__**: NOT_INITIALIZED=AA01, ALREADY_INITIALIZED=AA02. **3 status constants byte-exato** sceNpUtil.h:5-7: NONE=0, RUNNING=1, FINISHED=2. **Hard-coded fake bandwidth** preserved cpp:42-43: FAKE_UPLOAD_BPS=FAKE_DOWNLOAD_BPS=100_000_000.0 (100 Mbps). FAKE_TEST_TICKS=100 cpp:36 (100ms total fake test). Wire struct **SceNpUtilBandwidthTestResult** {upload_bps:f64, download_bps:f64, result:i32, padding:[u8;4]} 24 bytes size_of assert. **Thread model collapsed**: upstream spawns named_thread; port collapsa para sync state machine via `tick()` + `run_to_completion()`. BandwidthTest com status + abort_requested + shutdown_requested + finished + fake_sleep_count + test_result. tick(): finalize on abort/shutdown OR count > 100. shutdown_writes_result_when_provided test verifica null result tolerated cpp:117 TODO comment ("what happens when this is null?"). re_init_after_shutdown_works valida lifecycle cycling. abort_then_shutdown valida que abort não finaliza por si só (só set flag — finalize só na próxima tick OU shutdown). 4 per-entry counters + last_init_prio/stack capture. full_lifecycle_smoke (all-pre-init fail → init 0x100/0x4000 → status RUNNING → abort → status STILL RUNNING → shutdown captura result com 100Mbps+CELL_OK → re-init OK) passa. | 21 |

| `rpcs3-lv2-bdemu` | wave-7 | LV2 BD-emulator helper syscall (porta `sys_bdemu.cpp` 14 linhas, 1 entry sys_bdemu_send_command stub). MODULE_NAME="sys_bdemu". BdemuCommand record {cmd, a2, a3, buf_addr, buf_len} para test introspection. Upstream stub returns CELL_OK regardless. | 5 |
| `rpcs3-lv2-btsetting` | wave-7 | LV2 Bluetooth settings ioctl-style syscall (porta `sys_btsetting.cpp` 13 linhas, 1 entry sys_btsetting_if stub). MODULE_NAME="sys_btsetting". XMB BT settings interface. | 5 |
| `rpcs3-lv2-console` | wave-7 | LV2 system console write syscall (porta `sys_console.cpp` 14 linhas, 1 entry sys_console_write stub). MODULE_NAME="sys_console". Distinct from sysPrxForUser console_write user-mode shim. captured_text() helper concatena UTF-8 payloads para tracing. | 6 |

| `rpcs3-lv2-crypto-engine` | wave-7 | LV2 crypto engine syscalls (porta `sys_crypto_engine.cpp` 28 linhas, 3 entries: create/destroy/random_generate). Id allocator monotonic + double-destroy=ESRCH. | 7 |
| `rpcs3-lv2-gpio` | wave-7 | LV2 GPIO get/set syscalls (porta `sys_gpio.cpp` 38 linhas, 2 entries). Retail HW behavior byte-exato: device IDs LED=0x1/DIP_SWITCH=0x2; get sempre escreve 0; set LED=OK no-op, set DIP=EINVAL, unknown=ESRCH. | 9 |
| `rpcs3-lv2-trace` | wave-7 | LV2 trace syscalls (porta `sys_trace.cpp` 70 linhas, 10 entries). All entries returnam **CELL_ENOSYS=0x80010001** byte-exato — DEX/DECR-only syscalls, retail rejeita universal. cpp:10 TODO comment about DEX/DECR mode preserved. | 5 |

| `rpcs3-lv2-io` | wave-7 | LV2 I/O buffer syscalls (porta `sys_io.cpp` 75 linhas, 4 entries: buffer_create/destroy/allocate/free). idm allocator + per-buffer block tracking + destroy drains orphan allocations. 4 errors EFAULT/ESRCH/ENOMEM byte-exato. | 11 |
| `rpcs3-lv2-gamepad` | wave-7 | LV2 gamepad ycon dispatcher syscall (porta `sys_gamepad.cpp` 98 linhas, 11 entries — 1 dispatcher + 10 handlers). Dispatcher por packet_id 0..=9; unknown packet logs + returns CELL_OK. **C++ typo "initalize" preservado** cpp:7 (não "initialize"). | 4 |
| `rpcs3-lv2-sm` | wave-7 | LV2 System Manager syscalls (porta `sys_sm.cpp` 105 linhas, 6 entries: get_params/get_ext_event2/shutdown/set_shop_mode/control_led/ring_buzzer). **Shutdown rich op-code dispatch** byte-exato cpp:57-83: 0x100/0x1100=AppShutdown, 0x200/0x1200=AppReboot, 0x8201/2/4=ENOTSUP (LPAR), outros=EINVAL, sem root=ENOSYS. **get_params constants byte-exato** cpp:17-20: c=0x200, d=7. get_ext_event2 retorna `not_an_error(CELL_EAGAIN)` ("no event") on success. ShutdownOutcome enum tipa o dispatch. | 14 |

| `rpcs3-lv2-dbg` | wave-7 | LV2 debug syscalls (porta `sys_dbg.cpp` 131 linhas, 2 entries: read/write_process_memory). **Pluggable VmAccess trait** + MockVm para tests. Validation cascade EXATA cpp:21-43: pid!=1=INVALIDARGUMENTS, size==0/data==null=INVALIDARGUMENTS, !writable_dest=EFAULT, !readable_src=EFAULT. **Stack fast-path preservado** cpp:87-92: `address >> 28 == 0xD` usa raw memmove sem registro. **Executable page handling preservado** cpp:94-128: segmenta por boundary 64KiB + chama register_function_at após escrita exec. PageFlags com READABLE/WRITABLE/EXECUTABLE bits. CELL_LV2DBG_ERROR_DEINVALIDARGUMENTS=0x8001_0402 byte-exato Modules/sys_lv2dbg.h. | 12 |

| `rpcs3-lv2-hid` | wave-7 | LV2 HID manager syscalls (porta `sys_hid.cpp` 191 linhas, 8 entries: open/ioctl/check_focus/513/514/is_root/add_hot_key/read). **Realhw observable byte-exato preserved**: HID_HANDLE_INITIAL=0x100 cpp:31, SONY_VID=0x054C, DUALSHOCK3_PID=0x0268, IOCTL_PKG_ID_2_REALHW_TAIL 17-byte dump cpp:77. open valida device_type ≤ 3 (0=any, 1=pad, 2=kb, 3=mouse). check_focus retorna 1 byte-exato cpp:100. ioctl_pkg_id_2/5 returnam SysHidInfo2/5 com VID/PID realhw. is_process_permission_root retorna `root_perm` flag. read null buf=EFAULT. | 13 |

| `rpcs3-lv2-tty` | wave-7 | LV2 TTY read/write syscalls (porta `sys_tty.cpp` 205 linhas, 2 entries). 16 channels (0..=15), USER1=3 cutoff. read requires debug_console_mode (else EIO), invalid ch=EINVAL, null buf=EINVAL, null preadlen=EFAULT. **Quirk preservado cpp:65**: `input.substr(chars_to_read, ...)` deixa \n no front da queue → próximo read pega 0 chars (test explícito `read_leaves_trailing_newline_in_queue`). System channel warnings tracked. write trunca a `len`. | 16 |

| `rpcs3-lv2-interrupt` | wave-7 | LV2 interrupt tag/handler syscalls (porta `sys_interrupt.cpp` 303 linhas, 4 entries: tag_destroy/thread_establish/thread_disestablish/thread_eoi). **IntTag + IntServ + ThreadState** mirrors. **Validation order EXATA preservada** cpp:144-174: tag exists→ESRCH, thread exists→ESRCH, thread !running→EAGAIN, tag !handler→ESTAT. **Disestablish dual-path** cpp:213-221: try withdraw handler first → ThreadJoined fallback se ih é raw thread. EBUSY=0x8001_000A em tag_destroy se handler attached. eoi clears `executing` flag por thread_id. DisestablishOutcome enum tipa as duas paths (HandlerJoined com saved_r13 vs ThreadJoined). | 16 |

| `rpcs3-lv2-game` | wave-7 | LV2 game-related syscalls (porta `sys_game.cpp` 293 linhas, 8 entries: watchdog start/stop/clear, sw_version set/get, board_storage read/write, get_rtc_status). **Watchdog FSM** com WatchdogControl {needs_restart, active, timeout_us}: start bit math byte-exato cpp:177 (`timeout *= 1_000_000 & !63`), double-start=EABORT. **set_sw_version requires root** (CELL_ENOSYS senão). **board_storage 16-byte initialized 0xFF** cpp:64 byte-exato. read/write retorna status 0x00 OK / 0xFF err. get_rtc_status sempre escreve 0. clear é idempotente — só seta needs_restart se active && !already needing restart. | 19 |

| `rpcs3-lv2-overlay` | wave-7 | LV2 overlay PRX load/unload syscalls (porta `sys_overlay.cpp` 204 linhas, 3 entries: load_module/load_module_by_fd/unload_module). **OverlayLoader trait + MockLoader** plugável (decifração SELF/EDAT + ELF parse fica em rpcs3-loader-elf-self). Validation cascade EXATA cpp:133-176: !ppc_seg→ENOSYS, !path→EFAULT, signed offset<0→EINVAL, fd unknown→EBADF, fd closed→EBADF, unload unknown→ESRCH. **vpath construction byte-exato cpp:176**: offset==0 usa file.name; offset!=0 usa `name_x<offset_lowercase_hex>`. unload retorna Vec<OverlaySegment> para caller fazer vm::dealloc dos segs (cpp:196-199). | 17 |

| `rpcs3-hle-cellaudioout` | wave-7 | PS3 audio-out config + sound-mode negotiation HLE (porta dedicated `cellAudioOut.cpp` 590 linhas, 10 entries no `cellSysutil` PRX). 8 errors byte-exato 0x8002_B240..B247. Constants completos: PRIMARY/SECONDARY, 12 CODING_TYPE_*, 4 CHNUM, 7 FS bitmasks, 4 SPEAKER_LAYOUT, 3 COPY_CONTROL, 3 DOWNMIXER, OUTPUT_STATE Enabled/Disabled, 2 EVENT_*. SoundFormatFlags (lpcm_2/5_1/7_1/ac3/dts) + AudioFormat enum (Stereo/Surround51/Surround71/Automatic/Manual). **Init logic preservada cpp:35-180**: per format seeds sound_mode table com initial selection do primeiro suportado, sempre adiciona LPCM 2 ao PRIMARY, secondary só LPCM 2. get_number_of_device PRIMARY=1/SECONDARY=0. configure write-through channels/encoder/downmixer. get_sound_availability busca highest channel match; v2 exact channel match. set_copy_control valida valor. get_device_info só PRIMARY index 0. Callbacks 8-slot table. Diferente da cobertura parcial em cellavconf — port dedicado completo. | 18 |

| `rpcs3-hle-cellavconfext` | wave-7 | PS3 audio/video config extensions HLE (porta `cellAvconfExt.cpp` 617 linhas, 21 entries). MODULE_NAME="cellSysutilAvconfExt" cpp:594. **Cursor color conversion REAL preservada** cpp:240-276 — port byte-exato do template `convert_cursor_color<Is_Float, Range_Limited>` com 4 modes. libm_pow/ln/exp helpers Taylor series para no_std (gamma range 0.8..1.2). 9 errors. 4 in_device_modes (Single=0, Multi=1, Multi2=2, Multi10=10). Gamma roundtrip + screen_size requires stereo_enabled. AudioInRegisterDevice valida volume ≤ 5. cpp:430 strange-error-choice preserved (AudioOutRegister returns AUDIO_IN error). | 20 |

| `rpcs3-hle-scenp2` | wave-7 | PS3 NP v2 matching framework HLE (porta `sceNp2.cpp` 2062 linhas + sceNp2.h 1625 linhas, 80 entries — entries lista completa byte-exata da REG_FUNC ordem). MODULE_NAME="sceNp2". 13 errors críticos byte-exato facility 0x80022___ (Matching2: OUT_OF_MEMORY/ALREADY_INIT/NOT_INIT/CONTEXT_MAX/CONTEXT_ALREADY_EXISTS/CONTEXT_NOT_FOUND/CONTEXT_ALREADY_STARTED/CONTEXT_NOT_STARTED/SERVER_NOT_FOUND/INVALID_ARGUMENT/INVALID_CONTEXT_ID/INSUFFICIENT_BUFFER/REQUEST_TIMEOUT) + 2 Auth (OAUTH NOT/ALREADY_INIT). 4 lifecycles independentes: Np2 (Init/Term), Matching2 (V1/V2 mutually exclusive — Init1 e Init2 não coexistem), Auth OAuth, Context registry (Created→Started↔Stopped, MAX_CONTEXTS=8). np2_term cascata para limpar matching2. 80 stub_call generic counter para entries não-modeladas individualmente. | 11 |

| `rpcs3-hle-scenpclans` | wave-7 | PS3 NP clans (clan management) HLE (porta `sceNpClans.cpp` 1282 linhas, 39 entries). 10 errors byte-exato facility 0x80022700+ (ALREADY/NOT_INIT/NOT_SUPPORTED/OUT_OF_MEMORY/INVALID_ARG/EXCEEDS_MAX/BAD_RESPONSE/BAD_DATA/BAD_REQUEST/INVALID_SIGNATURE). Init/Term lifecycle + ClanRequest registry MAX=32 com Pending/Aborted state. 39 ENTRY_POINTS array byte-exato. | 9 |

| `rpcs3-hle-scenpcommerce2` | wave-7 | PS3 NP Commerce v2 (PSN store) HLE (porta `sceNpCommerce2.cpp` 1125 linhas, 52 entries). 10 errors byte-exato facility 0x80023___. Init/Term + Context/Request registries (CTX max 32, REQ max 64). BGDL availability flag. 52 ENTRY_POINTS array byte-exato. | 8 |

| `rpcs3-hle-scenptus` | wave-7 | PS3 NP Title User Storage HLE (porta `sceNpTus.cpp` 1478 linhas, 62 entries). Constants: DATA_INFO_MAX=384, MAX_CTX=32, MAX_SLOT/USER_PER_TRANS=64/101, MAX_FRIENDS=100. 6 OPETYPE + 4 SORT_TYPE byte-exato. Title/Transaction context registries. 62 entries array. | 8 |

| `rpcs3-hle-scenp` | wave-7 | PS3 NP main module HLE (porta `sceNp.cpp` 7590 linhas — **MAIOR módulo do workspace**, 239 entries). 239 ENTRY_POINTS array byte-exato. 10 errors críticos byte-exato facility 0x8002AA__ (NOT_INIT/ALREADY_INIT/INVALID_ARG/OUT_OF_MEMORY/ID_NO_SPACE/ID_NOT_FOUND/SESSION_RUNNING/LOGINID_ALREADY_EXISTS/INVALID_TICKET_SIZE/INVALID_STATE). Init/Term lifecycle com session_running guard. Basic handler register/unregister single-slot. | 7 |

| `rpcs3-hle-cellimejp` | wave-7 | PS3 Japanese IME utility HLE (porta `cellImeJp.cpp` 1295 linhas, 42 entries). 7 errors byte-exato facility 0x8002BF__ (ERR_ERR/CONTEXT/ALREADY_OPEN/DIC_OPEN_FAILED/PARAM/IME_ALREADY_IN_USE/OTHER). Open/Close lifecycle com single-context guard. 42 ENTRY_POINTS array byte-exato. | 7 |

| `rpcs3-hle-cell-freetype2` | wave-7 | PS3 FreeType2 font rendering HLE (porta `cell_FreeType2.cpp` 1096 linhas, 155 entries — todos stubs `UNIMPLEMENTED_FUNC` retornando `CELL_OK`). 155 ENTRY_POINTS array byte-exato preservando ordem REG_FUNC. Dispatch counter por slot + lookup por nome para telemetria. | 7 |

| `rpcs3-hle-cellatracxdec` | wave-7 | PS3 ATRAC3plus SPU-decoder HLE contract (porta `cellAtracXdec.cpp` 1015 linhas). 54 error codes ATX byte-exato facility 0x80612___. 4 CoreOps VNID registrations (2ch=0x076b33ab, 6ch=0x1d210eaa, 8ch=0xe9a86e54, default=0x4944af9a). 15 REG_HIDDEN_FUNC entries. `atracx_dec_get_spurs_mem_size` table-lookup (-1 sentinel via u32::MAX para 5ch/inválido). FSM savestate 7 estados. Constantes frozen: SPURS_STRUCTS=0x1cf00, SAMPLES_PER_FRAME=0x800, DECODER=0xa8, CONTEXT=0x268. Real FFmpeg/SPU path não portado — contract-only. | 10 |

| `rpcs3-hle-cell-l10n` | wave-7 | PS3 localization / codepage conversion HLE (porta `cellL10n.cpp` 2854 linhas — 3º maior módulo do workspace, 165 entries). Enum `L10nResult` (OK/SRC_ILLEGAL/DST_EXHAUSTED/CONVERTER_UNKNOWN). 8 detection flags bitmask (STR_UNKNOWN..STR_ERROR bit17). 52 `CodePages` ids (UTF8..CODEPAGE_869) com aliases colapsando (SHIFT_JIS=932, GBK=936, UHC=949, BIG5=950, JIS=ISO_2022_JP, MUSIC_SHIFT_JIS=RIS_506). UTF-16 surrogate mask helpers. 165 ENTRY_POINTS byte-exato em ordem REG_FUNC. Conversão real (simdutf deps) não portada — contract+ABI frozen. | 9 |

| `rpcs3-hle-celldmuxpamf` | wave-7 | PS3 PAMF MPEG-PS demuxer HLE (porta `cellDmuxPamf.cpp` 2906 linhas, 25 entries). `CellDmuxPamfError` 1/2/3/5/6 (gap no 4 preservado). `DmuxPamfStreamTypeIndex` signed com -1 sentinel. Start codes byte-exato (PACK=0x1ba, M2V_PIC=0x100, AVC_AU=0x109, PROG_END=0x1b9, VIDEO_BASE=0x1e0 + ch). AVC levels non-contíguos (21/30/31/32/41/42). LPCM ch/FS/bits. AC3 sync 0x0b77 / ATRACX 0x0fd0. 2 CoreOps VNIDs (pamf=0x28b2b7b2, raw_es=0x9728a0e9). 23 REG_HIDDEN_FUNC entries. `video_stream_start_code(ch)` const-fn mask. SPU path + AVC packet parser não portados. | 12 |

| `rpcs3-hle-cellgcmsys` | wave-7 | PS3 GCM (RSX command/display/tile) system HLE (porta `cellGcmSys.cpp` 1632 linhas, 100 entries). 6 errors byte-exato facility 0x80210___ (FAILURE=0xff fora de sequência, NO_IO_PAGE_TABLE/INVALID_ENUM/INVALID_VALUE/INVALID_ALIGNMENT/ADDRESS_OVERWRAP). 100 ENTRY_POINTS byte-exato — inclui `_cellGcm*` privados (Func1/2/3/4/12/13/15/38), flip command helpers e Gpad capture API. RSX runtime (FIFO/tiles/IO map) não portado — contract-only. | 7 |

| `rpcs3-audio-utils` | wave-8 | Volume/mute helpers de `rpcs3/Emu/Audio/audio_utils.cpp` — **primeiro módulo fora de Cell/Modules**. `get_volume()` retorna 0.0 quando muted, else volume/100. `change_volume(delta)` com non-linear scaling: volume<25 & abs(delta)>1 colapsa em ±1 (fine control), volume>75 & abs(delta)<5 dobra capped em ±5 (fast climb), else pass-through. Clamp em [min=0, max=200]. Enum `VolumeChange::Changed/NoOp` para UI drivers. Overlay/callbacks fora. | 12 |

| `rpcs3-audio-resampler` | wave-8 | SoundTouch wrapper params de `rpcs3/Emu/Audio/audio_resampler.cpp`. Enums `AudioFreq` (32K/44K/48K/88K/96K/176K/192K), `AudioChannelCnt` (Stereo=2/5.1=6/7.1=8), `AudioSampleSize` (Float=4/S16=2), `AudioStateEvent`. Defaults (48000/64/256/8). Tempo clamp [0.1, 1.0]. SoundTouch settings frozen (SEQUENCE_MS=40, SEEKWINDOW=15, OVERLAP=8, QUICKSEEK=0, AA_FILTER=1). `AudioResamplerState` backend-agnostic com buffered_samples + put/take/flush API. SoundTouch engine em si (15k+ LOC) fora. | 12 |

| `rpcs3-audio-dumper` | wave-8 | WAV file layout + bookkeeping de `rpcs3/Emu/Audio/AudioDumper.cpp`. `WavHeader` 56 bytes (compile-time asserted): RIFF(12)+FMT(24)+FACT(12)+data_id+size. AudioFormat=1 (PCM) / 3 (IEEE Float). `AudioDumper::open` aplica header, `write_data(size)` rejeita size não-múltiplo de block_size=ch*sample_size, `close` emite padding byte quando size é ímpar (WAV chunk word-alignment) + zera num_channels. Enum `WriteOutcome::{Wrote/Misaligned/Closed/Empty}`. File I/O e path construction fora. | 14 |

| `rpcs3-audio-backend` | wave-8 | DSP helpers de `rpcs3/Emu/Audio/AudioBackend.cpp`. `convert_to_s16` (float*32768.5 clamp[-32768,32767]). `apply_volume_static` com unity/mute fast paths. `apply_volume` com linear ramp 0.032s (VOLUME_CHANGE_DURATION), epsilon 1e-6 anti-loop, ch_cnt assert par>=2. `normalize` soft-clip tanh acima de 0.95, hard-clip em 1.0. `AudioChannelLayout` 8-variant enum (Automatic/Mono/Stereo/StereoLfe/Quadraphonic/QuadraphonicLfe/5.1/7.1). Tabela default_layout preserva quirk cpp:240 (7ch → 5.1, não 7.1). `setup_channel_layout` warning cascade como `ChannelLayoutSetup` struct. `max_channel_count_from_sound_modes` short-circuit em 8ch. | 20 |

| `rpcs3-io-buzz` | wave-8 | Logitech Buzz! buzzer USB controller emulator (porta `rpcs3/Emu/Io/Buzz.cpp` 210 linhas). Enum `BuzzBtn` (Red/Yellow/Green/Orange/Blue + Count). USB descriptor constants byte-exato: VID=0x054c, PID=0x0002, bcdDevice=0x05a1, HID interface class 0x03. Interrupt transfer preamble `[0x7f, 0x7f, 0x00, 0x00, 0xf0]` + latency 6ms. Button-bit packing `buf[2 + idx/8] |= 1 << (idx % 8)` onde `idx = btn + 5*player` (cpp:187..203). `controller_range(idx)`: 0 → (0,3), else → (4,6). `min_interrupt_buf_size` pelo max_index cpp. USB stack + pad_thread fora. | 14 |

| `rpcs3-io-ghltar` | wave-8 | Guitar Hero Live guitar controller emulator (porta `rpcs3/Emu/Io/GHLtar.cpp` 221 linhas). `GhltarBtn` 16-variant enum (W1/W2/W3/B1/B2/B3/Start/HeroPower/Ghtv/StrumDown/StrumUp/DpadLeft/DpadRight/Whammy/Tilt + Count). USB VID=0x12BA/PID=0x074B. 27-byte report com preamble fixo (buf[2]=0x0F dpad_none, buf[3/4/5/6/19]=0x80, buf[22]=0x01, buf[24/26]=0x02). Fret masks W1=0x01..W3=0x20 via `+=`. Button masks HeroPower/Start/GHTV/Sync. Whammy inverte como `~value + 1` u16 truncado para u8. Tilt snap em buf[5]=0x00/0xFF nos thresholds 0x10/0xF0. Latência interrupt 1ms (override de 6ms cpp:101). | 11 |

| `rpcs3-io-gametablet` | wave-8 | THQ uDraw Game Tablet emulator (porta `rpcs3/Emu/Io/GameTablet.cpp` 319 linhas). USB VID=0x20d6/PID=0xcb17, bcdDevice=0x0108. `GameTabletData` 27-byte packed struct (compile-time asserted): btn_bits0/1 (square/cross/circle/triangle/select/start/PS), dpad, 4 sticks, pen, pressure, pos_x/y hi+lo, 4 BE u16 accel/unk. Dpad 8-compass + 0x0F=None. Neutral defaults (sticks 0x80, pressure 0x72, pos 0x0FFF, accel 0x0200). `encode_dpad(u,r,d,l)` preserva cascade byte-exato cpp:263..280 (inclui quirk up+down → NORTH via `!left && !right`). `PenNoise` 1-bit LSB toggle. Tablet res 1920x1080. Pressure 0xbb ↔ botão primário. LED decode via buf[2] low-nibble. | 14 |

| `rpcs3-io-turntable` | wave-8 | DJ Hero Turntable USB controller emulator (porta `rpcs3/Emu/Io/Turntable.cpp` 299 linhas). `TurntableBtn` 17-variant enum. USB VID=0x12BA/PID=0x0140. 27-byte report com 6 platter masks (R_Green/Red/Blue + L_*), face buttons Square/Cross/Circle/Triangle, dpad 8-compass. Dpad state machine `dpad_X_fold` funções que consultam buf[2] para produzir diagonais (Up+Right=0x01, Up+Left=0x07, etc.). Quirk dupla-pressão: `buf[X] = ~buf[X]` aplicado duas vezes volta a 0 (ex: Blue+Square colapsam buf[7] para 0x00). RightTurntable `max(1, 255-value)` com 127→128 snap (DJ Hero refusa 0 e center-off-by-one). Crossfader inverte via 255-value split em 6+2 bits; EffectsDial não inverte. Latência 1ms override (cpp:100 — normally 10ms at 100Hz). | 13 |

| `rpcs3-io-guncon3` | wave-8 | Namco GunCon 3 light-gun emulator (porta `rpcs3/Emu/Io/GunCon3.cpp` 295 linhas). USB VID=0x0b9a/PID=0x0800, interface class 0xff (vendor-specific). `GunCon3Data` packed 15 bytes com 3 btn_bits bytes, gun_x/y/z i16 LE, 4 sticks, checksum, keyindex. **256-byte KEY_TABLE** byte-exato (cpp:39..56). `guncon3_encode(data, key)` cipher: initial_key_offset seed via `key[1..7] + data[14]`, 3 rounds × 13 bytes com op escolhido por `KEY_TABLE[offset] & 3` (0=add/1=sub/else=xor), key_index wrap em 7. Checksum cpp:121..123 byte-exato. | 8 |

| `rpcs3-io-kamenrider` | wave-8 | Kamen Rider Summoner rider-gate NFC portal emulator (porta `rpcs3/Emu/Io/KamenRider.cpp` 331 linhas). `generate_checksum` sum mod 256. `blank_response(cmd, seq)` com 5-byte preamble `[0x55, 0x02, cmd, seq, chk]`. `wake_response` 29-byte magic sequence byte-exato cpp:53..55 (`0x1a` length). `list_tags` bump reply[1]+=8 por figura presente com 9-byte records. `query_block` copia 16 bytes de `figure.data[sector*64 + block*16]` quando sector<5 && block<4. `write_block` mutação in-place. `figure_removed_response` 12-byte payload com checksum em reply[11]. Figure slot array com fall-back pro slot 7 (cpp:39). 320-byte figure data (5×4×16). | 12 |

| `rpcs3-io-dimensions` | wave-8 | LEGO Dimensions toypad com **TEA cipher real** (porta `rpcs3/Emu/Io/Dimensions.cpp` 400+ linhas). Constants byte-exato: COMMAND_KEY 16B, CHAR_CONSTANT 17B, PWD_CONSTANT 25B ("(c) Copyright LEGO 2014\xAA\xAA"). **TEA decrypt/encrypt** 32 rounds, delta=0x9E3779B9, sum pre=0xC6EF3720/post=0. **Jenkins small-noise PRNG** (init_a=0xF1EA5EED, 42 warmup rounds, rotl 21/19/6). `dimensions_randomize` com rotr(25)/rotr(10). `scramble(uid, count)` injeta 0xAA em buf[count*4-1] antes de randomize. `generate_figure_key` UID skip buf[3] (cpp:191). `get_figure_id` decrypt page 36 + branch character/vehicle em < 1000. Challenge/random reply 32-byte byte-exato com checksum em reply[11]. | 15 |

| `rpcs3-io-skylander` | wave-8 | Skylanders PortalMaster USB emulator (porta `rpcs3/Emu/Io/Skylander.cpp` 374 linhas). USB VID=0x1430/PID=0x0150. 8 figure slots × 1024-byte (0x40 × 0x10). `SkyPortal` com `activated`, `interrupt_counter`, `leds(r,g,b)`. `activate` enqueue 3,1 por figure presente. `deactivate` collapse queue para last, mask & 1. `get_status` pack 2-bit-per-slot from slot 7 → slot 0 em LE u16, bump interrupt_counter, magic `[0x53, status_lo, status_hi, 0,0, counter, 0x01]`. `query_block(slot, block)` reply `['Q', 0x10\|slot if present else slot, block, data..16]`. `write_block` similar com 'W'. `remove_skylander` push 2,0 queue + set status=2. `load_skylander` last_id match prefer sobre lowest-free. Replies byte-exato para A/R/M/J control transfers. | 13 |

| `rpcs3-io-infinity` | wave-8 | Disney Infinity Base com **SHA1 + AES-128 key derivation + custom Jenkins RNG** (porta `rpcs3/Emu/Io/Infinity.cpp` 488 linhas). USB VID=0x0E6F/PID=0x0129. `SHA1_CONSTANT` 32B byte-exato ("(c) Disney 2013" ASCII a partir do offset 16, último byte default-init 0). `SHA1_PREFIX` 31B é o slice que realmente entra na hash (cpp `end() - 1`). **SCRAMBLE_MASK=0x8E55AA1B3999E8AA** byte-exato. `scramble(num, garbage)` + `descramble(num)` bit-twiddling round-trip verified. `InfinityRng` Jenkins-variant com **23 warmup rounds** (vs dimensions 42), rotl(27)/rotl(17), transformation distinta. `derive_figure_position` 0-2→1, 3-5→2, 6-8→3, else 0. `aes_key_from_sha1_output` inverte byte-order dentro de grupos de 4. `extract_figure_number` 24-bit BE u24 de bytes[1..=3]. 7 reply layouts byte-exato (blank, next_and_scramble, descramble_and_seed, query/write block, figure_identifier, present_figures, figure_change). | 20 |

| `rpcs3-io-usio` | wave-8 | v406 USIO Namco/Bandai arcade I/O emulator (Taiko/Tekken, porta `rpcs3/Emu/Io/usio.cpp` 689 linhas). USB VID=0x0b9a/PID=0x0910, vendor-specific class 0xff. 3 endpoints (bulk OUT 0x01, bulk IN 0x82, interrupt IN 0x83). **C_HIT=0x1800** Taiko drum magic word. SRAM 1MB (16 pages × 64KB). `UsioBtn` 18-variant enum. Taiko bitmasks (service 0x4000, enter 0x200, up 0x2000, down 0x1000, test_on 0x80) + hit offsets 32/34/36/38 + per-player stride 8. Tekken 11 bitmasks + `per_player_shift = (p%2)*24`. Lightmask bitmasks para player 0 mirror. 8 channel-0 registers (SetSystemError/ClearSram(magic=0x6666)/SetExpansionMode/GetBuffer/CardReaderCheck1/CardReaderCheck2/GetTekkenInput). `hopper_index_from_reg` 4 hoppers × Request/Limit. GetBuffer 64B canned response byte-exato + CardReaderCheck1 16B. SRAM bounds via `sram_write_in_bounds(ch, reg, sz)`. | 18 |

| `rpcs3-audio-device-enumerator` | wave-8 | Device filter + sort shared entre Cubeb e FAudio enumerators (porta `cubeb_enumerator.cpp` 113L + `faudio_enumerator.cpp` 89L). `AudioDevice {id, name, max_ch}`. `normalize_cubeb(raws)`: skip UNPLUGGED + empty id, fallback name=id, sort by name asc. `normalize_faudio(raws)`: id=str(index), fallback name="Device {id}", sort by name asc. Raw scrape structs `CubebRawDevice` + `FaudioRawDevice`. Library FFI fica fora. | 11 |

| `rpcs3-loader-tar` | wave-8 | POSIX ustar reader (porta `rpcs3/Loader/TAR.cpp` 771 linhas). `TarHeader` 512-byte record layout compile-time asserted. `octal_text_to_u64(data)` parse com NUL/space term, `u64::MAX` sentinel para malformed. `block_aligned_size` rounds up a 512. `FileType` classificação (Regular/Hardlink/Symlink/Directory/Other) por character byte. `full_name(header)` concatena prefix + "/" + name. `read_cstr` strip at first NUL. Magic ustar "ustar\0". | 14 |

| `rpcs3-loader-trp` | wave-8 | PS3 Trophy archive (TRP) parser (porta `rpcs3/Loader/TRP.cpp` 221 linhas). `TRP_MAGIC=0xDCA24D00` BE. `TrpHeader` 64B + `TrpEntry` 64B compile-time asserted. `required_space` formula (cpp:160..166). `contains_entry`/`remove_entry`/`rename_entry` com bound check (name >= 32 → reject). `prepare_for_sha1` zera field sha1 em offset 28 pra v2 verification. `SCE_NP_TROPHY_INVALID_TROPHY_ID=0xFFFFFFFF` sentinel. | 14 |

| `rpcs3-loader-tropusr` | wave-8 | TROPUSR.DAT per-user trophy state (porta `rpcs3/Loader/TROPUSR.cpp` 394 linhas). Magic 0x818F54AD. `TropusrHeader 48B / TableHeader 32B / Entry4 96B (0x60) / Entry6 112B (0x70)` compile-time asserted. `TrophyGrade` Unknown=0/Platinum=1/Gold=2/Silver=3/Bronze=4. Unlock/lock transitions com timestamps. `LoadResult`. Defaults corretos (entry_type=4/6, entry_size=0x50/0x60). | 11 |

| `rpcs3-loader-iso` | wave-8 | PS3 3k3y/Redump ISO reader (porta `rpcs3/Loader/ISO.cpp` 1140 linhas). `ISO_SECTOR_SIZE=2048`, ISO 9660 magic "CD001" em offset 32769. `char_arr_be_to_uint` 4-byte BE→u32. `reset_iv(lba)` 16-byte IV com 12-zero prefix + 4-byte LBA BE. `touched_sector_count(offset, size)` math determinístico. Region parity quirk preservado: even=plaintext, odd=encrypted. `is_valid_region_count` 1..=127. `has_iso_magic` detect signature. | 10 |

| `rpcs3-loader-disc` | wave-8 | Disc-type classifier + SYSTEM.CNF parser (porta `rpcs3/Loader/disc.cpp` 149 linhas). Enum `DiscType` 5-variant (Invalid/Unknown/Ps1/Ps2/Ps3) com discriminantes positional matching cpp enum class. `PS3_DISC_CATEGORY = "DG"` pra PARAM.SFO check. `parse_system_cnf` split por '=' com trim. `classify_system_cnf` → BOOT2=PS2, BOOT=PS1, senão Unknown. | 10 |

| `rpcs3-loader-iso-cache` | wave-8 | FNV-1a-64 cache stem derivation + mtime freshness check (porta `rpcs3/Loader/iso_cache.cpp` 164 linhas). `FNV_SEED=14695981039346656037`, `FNV_PRIME=1099511628211`. `get_cache_stem(path)` produz 16-char lowercase hex. `is_entry_fresh(cached, fs)` comparação estrita. `is_stale_stem` pra cleanup. `stem_from_filename` strip last-dot ext. `IsoMetadataCacheEntry` shape (mtime, psf_data, icon_data, icon/movie/audio paths). | 9 |

| `rpcs3-np-countries` | wave-8 | Tabela 72-country PSN valid region (porta `rpcs3/Emu/NP/rpcn_countries.cpp` 82 linhas). Japan=0, United States=1, depois alfabético (Argentina..Vietnam). **Ordem é contrato de wire RPCN** — não sortable. Lookups bidirecional (code↔name). `is_valid_code` case-sensitive. | 10 |

| `rpcs3-spu-mfc` | wave-8 | SPU MFC DMA command opcodes (porta `rpcs3/Emu/Cell/MFC.cpp` 74 linhas). 39 opcodes byte-exato: PUT/GET familias (0x20/0x40 base), list variants (+0x04 bit), atomic ops (GETLLAR=0xD0, PUTLLC=0xB4, PUTLLUC=0xB0, PUTQLLUC=0xB8), SNDSIG (0xA0..0xA2), sync (BARRIER=0xC0, EIEIO=0xC8, SYNC=0xCC), SDCR* (0x80..0x8F). 5 feature masks (BARRIER/FENCE/LIST/START/RESULT). Tag layout 7-bit id + stalled flag bit 7. | 11 |

| `rpcs3-io-interception` | wave-8 | Global input-interception state (porta `rpcs3/Emu/Io/interception.cpp` 89L). `ActiveMouseAndKeyboard` enum (Emulated=0/Pad=1). `InterceptionState` com AtomicBool pads/keyboards/mice + AtomicU32 active_mkb. `set_intercepted_individual/all` match cpp overloads. `set_mouse_and_keyboard` retorna bool change signal. `toggle` swap Emulated↔Pad. | 8 |

| `rpcs3-io-usb-vfs` | wave-8 | USB mass-storage descriptor (porta `rpcs3/Emu/Io/usb_vfs.cpp` 64L). SMI Corporation / USB DISK. Mass-storage class 0x08/0x06/0x50 (SCSI bulk-only transport). Bulk IN 0x81 / OUT 0x02 com 512B packets. Quirk `bcdDevice = pid` preservado. | 5 |

| `rpcs3-ipc-config` | wave-8 | IPC server config (porta `rpcs3/Emu/IPC_config.cpp` 66L). Default server disabled, port 28012. Port bounds `[1025, 65535]` com `set_port` rejeitando fora de range. `clamp_port` helper. | 6 |

| `rpcs3-np-upnp-config` | wave-8 | UPnP DeviceUrl persisted config (porta `rpcs3/Emu/NP/upnp_config.cpp` 55L). Default empty (auto-discover). `upnp.yml`. | 4 |

| `rpcs3-io-recording-config` | wave-8 | Video/audio recorder config (porta `rpcs3/Emu/Io/recording_config.cpp` 37L + .h). 8 video bounds (framerate/width/height/pixel_format/codec/bps/b_frames/gop) + 2 audio (codec/bps). Defaults: 1280×720@30fps MPEG4 YUV420P, 4Mbps video / 192kbps AAC. `clamp_u32` helper. | 8 |

| `rpcs3-io-rb3drums-config` | wave-8 | Rock Band 3 drum MIDI mapper config (porta `rpcs3/Emu/Io/rb3drums_config.cpp` 39L + .h). Pulse_ms [1,100]=30. minimum_velocity [1,127]=10. combo_window_ms [1,5000]=2000. stagger_cymbals=true. 3 default combos HihatPedal×3+Snare/SnareRim/Kick. MIDI CC status=0xB0, number=4, threshold=64. | 5 |

| `rpcs3-io-mouse-config` | wave-8 | 8-button mouse binding config (porta `rpcs3/Emu/Io/mouse_config.cpp` 58L + .h). Defaults Button 1..3 = "Mouse Left/Right/Middle", 4..8 = "". `CELL_MOUSE_BUTTON_1..=8` bitmasks (1/2/4/8/16/32/64/128). `get_button(code)`/`get_button_mut` com None out-of-range. | 5 |

| `rpcs3-io-pad-config-types` | wave-8 | Pad handler enum + mouse movement mode + PadInfo (porta `rpcs3/Emu/Io/pad_config_types.cpp` 47L + .h). `PadHandler` 11 variants (Null=0/Keyboard/Ds3/Ds4/DualSense/Skateboard/Move/XInput/MMJoystick/Sdl/Evdev). `MouseMovementMode::Relative=0,Absolute=1` (repr i32). `PadInfo` 12B struct. Pretty-print strings byte-exato ("DualShock 3", "PS Move", etc). | 6 |

| `rpcs3-io-midi-config-types` | wave-8 | MIDI device types + serializer (porta `rpcs3/Emu/Io/midi_config_types.cpp` 50L + .h). `MAX_MIDI_DEVICES=3`. `MidiDeviceType` 4 variants (Keyboard=0/Guitar/Guitar22Fret/Drums). Separator `ßßß` (triple-sharp-s). `from_string`/`to_string_cpp` round-trip. Unknown type → default Keyboard fallback (cpp quirk). | 12 |

**Total: 🎉🎉🎉 215 crates, 🏆🏆🏆🏆 5027 testes verdes** sob MSVC + LTO-capable toolchain. 🏆 **MARCO 4000 TESTES atingido em iter #138.** 🏆🏆🏆 **MARCO 150 CRATES atingido em iter #146.** 🏆🏆🏆 **MARCO 150 ITERAÇÕES atingido em iter #150.** 🏆 **MARCO 170 CRATES atingido em iter #166.** 🏆🏆 **MARCO 4500 TESTES atingido em iter #167.** 🏆🏆 **MARCO 175 CRATES atingido em iter #171.** 🏆🏆🏆 **MARCO 180 CRATES atingido em iter #176.** 🏆 **MARCO 185 CRATES atingido em iter #181.** 🏆🏆 **MARCO 4700 TESTES atingido em iter #185.** 🎉🎉 **MARCO 190 CRATES atingido em iter #186.** 🏆 **MARCO 190 ITERAÇÕES atingido em iter #190.** 🏆🏆🏆 **MARCO 4800 TESTES atingido em iter #192.** 🎉🎉🎉🎉 **MARCO 200 CRATES atingido em iter #196.** 🏆🏆🏆 **MARCO 4900 TESTES atingido em iter #201.** 🎉🎉🎉 **MARCO 210 CRATES atingido em iter #206.** 🎉🎉🎉🎉🎉 **MARCO 5000 TESTES atingido em iter #211.** 🏆 **Pós-marcos: 211 iterações autônomas, ZERO regressões. Cobertura Cell/Modules 100%, wave-8 Audio 5 + Io 14 + Loader 8 + NP 2 + SPU 1 + IPC 1 = 31 crates (3 com crypto real).**

🎉 **MVP atingido**: `load_and_run_minimal_elf` prova que um ELF PPU sintético roda de ponta a ponta e termina com exit code verificável.

**Bug crítico encontrado e corrigido nesta wave**: `CpuFlag` em `rpcs3-emu-types` tinha ordem errada (faltava `Temp`/`Ret`, `Pause` no slot 3 em vez de 4). A correção bateu tanto na crate Rust quanto no `behavior-freeze/contracts/test_contract_cpu_flag.cpp` que era o oráculo para o build GTest do RPCS3. Exatamente o tipo de regressão ABI que o projeto de behavior-freeze pretende pegar — agora cobrindo todos os 19 flags com anchors frozen.

Próximas crates candidatas:
- **Onda 4b**: `rpcs3-memory-backing` — raw pointer access, reservation table (exige `unsafe` cuidadoso + `miri` tests)
- `rpcs3-loader-tar` (TAR extraction pro PUP install flow)
- `rpcs3-loader-self-decrypt` (scaffold, adiado por fixtures)
- `rpcs3-ppu-opcodes` (tabela de decode de opcodes PPU — grande mas alto valor)
- **Onda 4 (core)**: `rpcs3-cpu-thread` — `cpu_thread` base + `cpu_flag` bitset operations

Ver `../behavior-freeze/docs/PORT_PLAN.md` para sequência completa.

### Rodar só uma crate

```bash
cd rust
cargo test -p rpcs3-utilities       # 15 testes
cargo test -p rpcs3-config          # 21 testes
cargo test -p rpcs3-crypto          # 23 testes
cargo test -p rpcs3-loader-psf      # 19 testes
cargo test -p rpcs3-loader-elf-self # 14 testes
cargo test -p rpcs3-loader-pkg      # 16 testes
cargo test -p rpcs3-loader-pup      # 10 testes
cargo test -p rpcs3-emu-types       # 11 testes
cargo test --workspace              # todos (129)
```

### Build via CMake (smoke-test end-to-end)

Prova que corrosion + MSVC + rustc-msvc se entendem. Não precisa do RPCS3 C++:

```bash
cmake -S rust -B build-rust-smoke -G "Visual Studio 17 2022" -A x64
cmake --build build-rust-smoke --config Release
# Outputs: build-rust-smoke/rpcs3_utilities.lib, rpcs3_config.lib,
#          rpcs3_crypto.lib, rpcs3_loader_psf.lib, rpcs3_loader_elf_self.lib,
#          rpcs3_loader_pkg.lib, ...
```
